# Changelog

## April 2026

- **Removed PDF download button from Receivers & Criteria panel** — The ⇩ button (`#exportCriteriaPdfBtn`) and all associated code deleted: `exportCriteriaPdf()`, `_renderCriteriaTablePDF()`, `_renderEmergTablePDF()`, and the zone-layer helpers `_prepareZoneCapture()` / `_restoreAfterCapture()`. jsPDF/html2canvas libraries retained (still used by `generatePDFAppendix()` and `pdfCriteriaBtn`). Panel header now shows only the refresh (↻) and collapse (▲) buttons.

- **PDF export — all tables now data-driven with Resonate styling; html2canvas removed for PDC, emergency equipment, and childcare tables** — Every PDF table is now rendered natively with jsPDF; no html2canvas capture is used for any table. Changes: (1) **PDC table** — `captureElement(pdcBodyEl)` replaced with a native multi-line-row renderer that reads from `_PDC_PROVISIONS[i]` (static provision text) and `_pdcProvisions[i].relevance` (runtime relevance state). Three columns: Performance Outcome (47%), DTS/DPF (33%), Relevance (20%). Row height computed from the tallest wrapped cell; rows don't split across pages; header row repeats on continuation pages. Subheading "Activities Generating Noise or Vibration" on grey (`TABLE_HEADER_GREY`) row. (2) **Emergency equipment table** — `captureElement(emergEquipTable)` replaced with a native 2-row header table: gold row with "Receiver" and "Criteria, LAeq dB" (colspan), grey row with equipment sub-labels ("Fire pump" / "Standby generator"), body rows read from `emergEquipBody` DOM. (3) **Childcare table** — `captureElement(childSec.querySelector('table'))` replaced with a trivial native table: "Location" | "LAeq dB" gold header, "All sensitive receivers" | "50" body row. (4) **Named constants** added: `TABLE_HEADER_YELLOW = RESONATE_YELLOW` (#F2CB00) and `TABLE_HEADER_GREY = [242,242,242]`. **Not touched**: criteria table (already native), music table (already native), zone map capture, intro section, save/load schema.

- **PDF export — added 'Introduction' H2 section before Criteria** — New section renders immediately before the "Criteria" H1: H2 heading "Introduction" (reuses existing 15 pt bold black style), lead-in paragraph ("The noise impact assessment has been undertaken in consideration of the following guidelines:"), and a five-item bulleted list of guidelines (Planning & Design Code; Environment Protection (Commercial & Industrial Noise) Policy 2023; Local Nuisance and Litter Control Act 2016; WHO Guidelines for Community Noise; EPA Guideline music noise 2021). Shared bullet list style defined: U+00B7 middle dot glyph at left margin, text starts at 1 cm hanging indent, wrapped lines align to 1 cm, 3 pt before / 3 pt after each bullet, 9 pt normal black, 12 pt line spacing. Keep-with-next applied: entire Introduction block (heading + lead-in + all five bullets) measured and moved to next page together if it won't fit. New helpers: `bulletItem(text)` renderer, `_mBullets(items)` measurement. **Not touched**: Criteria section content/order, logo header, zone capture, save/load schema.

- **PDF export — Music noise criteria table background row (LA90) now renders as plain cell text** — Previously the table was captured via html2canvas, which included the `<input>` element chrome for the LA90 row, clipping two-digit values. Replaced with a native jsPDF table: `musicBg*` input values are read directly from the DOM data model, LA10 values computed as LA90+8, and both rows rendered as centred plain text matching the existing `renderCritTablePaginated` cell style. The `musicBgTable` html2canvas capture has been removed entirely. Table layout: gold header row ("Octave band centre frequency, Hz"), light-grey frequency label row (63–4000 Hz), two data rows. **Not touched**: LA10 criteria row values, live UI input boxes, other PDF tables, save/load schema.

- **PDF export — logo resolution fix, second pass (2× canvas → RGBA PNG)** — Previous `fetch`/`FileReader` approach embedded the raw original PNG bytes; jsPDF's internal `png.js` parser does not reliably handle indexed-colour or RGB (non-RGBA) PNGs and can silently degrade the image. Fix: reverted to canvas-based loading but at **2× source resolution** (1632 × 612 px from 816 × 306 px source), drawing the logo into a canvas forces conversion to RGBA PNG format which jsPDF handles cleanly, and the 2× pixel count doubles effective PDF DPI from ~392 to ~784 at 5.29 cm rendered width — crisp at 100% and 200% zoom. No SVG found in repo; `resonate-logo.png` (816 × 306 px) remains the source asset; a print-quality SVG or ≥1500 px PNG from Resonate brand assets would be the permanent fix.

- **PDF export — logo resolution fixed; 2 cm top border added** — Logo now loaded via `fetch` → `FileReader` (raw PNG bytes), bypassing the canvas intermediate that was re-encoding the image on every export. `LOGO_TOP` updated from 8 mm to 20 mm (2 cm border between page top edge and top of logo). `mTop` is now `LOGO_TOP + LOGO_H + LOGO_GAP ≈ 50.42 mm`; `cH ≈ 226.6 mm`. All keep-with-next fit logic reads from the updated `cH` automatically. **Not touched**: logo artwork, logo width (5.29 cm), 30 pt gap below logo, section content, zone capture, save/load schema.

- **PDF export — Resonate logo on every page, criteria keep-with-next, tall-table pagination** — Resonate logo (`resonate-logo.png`) is now rendered at the top-left of every PDF page (5.29 cm wide, 30 pt space below logo to body text). Page geometry centralised into shared constants (`LOGO_W=52.9mm`, `LOGO_H≈19.84mm`, `LOGO_TOP=8mm`, `LOGO_GAP=30pt`, `mTop≈38.42mm`, `mBot=20mm`, `mSide=22mm`, `cW=166mm`, `cH≈238.6mm`) — all page-fit logic reads from these. Each Criteria sub-section (H2 + intro paragraph(s) + figure/table) is kept together on one page: block height is measured (`_mH2` + `_mParas` + `_mCap` + `_mImg`) and `newPageIfNeeded()` is called before rendering if the block won't fit. The tall criteria table is now rendered natively via `renderCritTablePaginated()` (replacing html2canvas capture) with dynamic receiver discovery (r1–r20 via `window.getReceiverLatLng`), page breaks with caption and gold header row repeating on each continuation page. Caption spacing confirmed: 8 pt bold, 3 pt before / 3 pt after. Logo + page-number post-processing loop runs after all content pages are built. **Not touched**: propagation formulas, criteria derivation, table contents, zone capture, save/load schema.

- **PDF export — map framing step + criteria sidebar independence** — Both `generatePDFAppendix()` and `exportCriteriaPdf()` now show a non-blocking floating framing panel (`#pdfFramingPanel`) as their first step before disabling the export button. The user can adjust the map zoom/pan, then click Accept to proceed or Cancel to abort (no export occurs on Cancel). The panel is `position:fixed` centred at `top:72px`, `z-index:10000`; map remains fully interactive behind it. `window._showPdfFramingPanel()` returns `Promise<boolean>`. Criteria table captures in `generatePDFAppendix()` now expand all `.card-body.collapsed` panels before the html2canvas calls and restore them after — so the PDF renders correctly regardless of whether the user has the sidebar collapsed. `captureElement()` enhanced with `overflow-y` removal alongside existing `overflow-x` removal, and a 2× rAF settle wait added after the DOM widening mutations. **Not touched**: zone layer forcing/restore, map capture logic, criteria derivation, save/load.

- **PDF appendix — restructured Criteria section to match Resonate report template** — New H1 "Criteria" heading (20 pt bold, Resonate yellow #F2CB00) wraps six H2 sub-sections (15 pt bold black) in this order: Zoning → Planning & Design Code — Interface between Land Uses → Environmental noise policy → Emergency equipment criteria → Music noise criteria → Schools, kindergartens, child-care centre of place of worship. Each sub-section has a verbatim intro paragraph (9 pt, 12 pt line spacing). Caption style standardised: 8 pt bold, 3 pt before/after; figure captions sit BELOW images, table captions sit ABOVE tables. "Figure 1" prefix removed from Zone map caption (caption now reads exactly "Zone map"). Renamed "Receivers & Criteria" → "Environmental noise policy" and "Childcare / Student Criteria" → "Schools, kindergartens, child-care centre of place of worship". New helper functions: `h1Heading`, `h2Heading`, `introPara`, `captionText`; old `sectionLabel` removed. No auto-numbering infrastructure was ever present — the "Figure 1" prefix was a hardcoded string, now removed. **Not touched**: zone detection, criteria derivation, table contents, other PDF sections, .docx export, live UI.

- **Fix PDF Figure 1 — zone layer now always visible in zone map export** — `generatePDFAppendix()` now forces the zone layer ON before capturing Figure 1 regardless of the user's current toggle state, waits 1000ms for tile/GeoJSON data to render, then restores the live map state after capture. Three related bugs fixed: (1) `generatePDFAppendix()` never forced zones on — zone layer was absent when the user had the toggle off; (2) for SA state `isZoneOn` never updates (SA uses `_planZonesOn` / `_isPlanZonesOn()` from a separate IIFE) so the whitelist incorrectly excluded the SA canvas zones layer; (3) `_prepareZoneCapture()` for `exportCriteriaPdf()` would incorrectly toggle SA zones OFF when they were already on (because `isZoneOn` is always false for SA, the old guard `!isZoneOn && overlay` was unconditionally true). Changes: new `window._getSaZonesLayer` exported from SA planning IIFE; `_getPdfZoneCaptureState()` now returns `effectiveZoneOn`, `saZonesOn`, and `saZonesLayer`; `_prepareZoneCapture()` and `_restoreAfterCapture()` now track and correctly restore SA zone state; `exportCriteriaPdf()` uses a 1200ms settle when zones were just forced on; `generatePDFAppendix()` forces zone on before whitelist build and whitelists `saZonesLayer` for SA. **Not touched**: zone detection, zone criteria derivation, zone styling, other PDF figures, save/load.

- **Sidebar tabs now auto-expand all cards on activation** — When a tab (Setup / Criteria / Results / Recommended treatments) is activated, all cards within that tab automatically expand. Previously cards remained collapsed after switching tabs, requiring manual expansion. Implementation: new `expandSectionCards()` helper (defined after `expandAllPanels`) expands all `.card-body` elements and inline-toggle bodies that are not inside a `.section-hidden` element; called from `activateSection()` via `window._expandSectionCards` (window bridge needed as the two functions are in different `<script>` blocks), and also called at the end of `setInitialCollapseState()` so the initial Setup tab opens with cards expanded. Manual collapse within a tab still works; switching away and back re-expands. JSON load path unchanged — `expandAllPanels` still fires after load. **Not touched**: card headers, tab order, calculation logic, `expandAllPanels`, manual collapse behaviour.

- **Planning & Design Code card in Criteria tab now hidden for non-SA addresses** — `#pdcProvisionsSection` (class `sa-only`) was already hidden for VIC/NSW/OTHER states via explicit CSS rules, but was visible in the initial null state (no address entered / geocode not yet run, no body state class set). Added one CSS rule: `body:not(.state-sa):not(.state-vic):not(.state-nsw):not(.state-other) .sa-only { display: none !important; }` to cover the null case. No JS changes. Jurisdiction detection and `currentState` variable unchanged. The Receivers & Criteria card below it is unaffected in all states.

- **Line source placement now supports unlimited vertices (click-to-add, click last vertex to finish), matching barrier drawing workflow** — Removed the `draw:drawvertex` hook that forced completion after exactly 2 vertices. `L.Draw.Polyline` now runs at full capability: each click adds a vertex, clicking the last-placed vertex marker finishes the line, Escape cancels without committing. A 3-vertex, 5-vertex, or any N-vertex line source can be placed in one tool activation. All post-placement hooks (terrain re-fetch, render, panel open, undo push) are unchanged. The `lineSources[].vertices` data model already supported arbitrary N — this was purely a placement-handler restriction. **Not touched**: barrier placement, CoRTN, area/building/point source tools, line source calculation, edit panel, drag handles, save/load schema.

- **Refactor: consolidate duplicate Earth radius literal into single constant** — `var EARTH_RADIUS_M = 6371000;` (WGS-84 mean Earth radius in metres) defined once at global scope. Five inline `var R = 6371000` declarations in `latLngToLocal`, `_cortnFlatDistM`, `_haversineM`, `_bsSegLen`, and the line-source segment-length helper replaced with `var R = EARTH_RADIUS_M`. No calculation change — purely a maintainability fix.

- **Refactor: clean up stale comments and a misleading section header** — Removed the `// TODO Chunk F: remove …` markers on `var sources = sourcePins` and `renderSourceCards` (both are still actively called; the "Chunk F" milestone is not coming). Replaced with accurate one-liners describing current purpose. Removed a leftover `// ─── SUPABASE (disabled …)` divider comment that incorrectly implied the code beneath it was dead (it is the live `rebuildLineSourceGrouped` implementation). Added an inline comment to the DEM tile-spacing ternary at `computeNoiseMap` (`~222m / ~111m / ~55m DEM sample spacing`) matching the already-commented equivalent block 140 lines below.

- **Fix: add `console.warn` to 10 silent `.catch` blocks** — Empty `catch(function() {})` handlers on terrain elevation DEMCache queries (6 floating panels: barrier, custom building, area source, building source, line source, point source) and html2canvas calls in PDF/screenshot export (4 sites) now log `[DEMCache] Terrain elevation query failed` or `[PDF export] html2canvas failed` with the error object, making transient failures diagnosable in DevTools without changing any user-visible behaviour.

- **Security: add permissive CSP `meta` tag** — `<meta http-equiv="Content-Security-Policy" content="object-src 'none'; base-uri 'self'; form-action 'self';">` added to `<head>`. Blocks plugin injection (`object-src 'none'`), `<base>` hijacking, and form exfiltration while leaving all CDN scripts/styles, WebSocket, fetch, and inline JS untouched (no `script-src` restriction on a static single-file app).

- **CI: `update-planning-data.yml` now opens a PR instead of committing to `main` directly** — Added `pull-requests: write` permission. After checkout, a `chore/data-<run_id>` branch is created. `git-auto-commit-action` commits and pushes to that branch. A new "Open pull request" step (conditional on `changes_detected == 'true'`) runs `gh pr create` targeting `main`. No change to the build script, data outputs, or deploy workflow.

- **Security: add SRI hashes to 9 CDN resources** — `leaflet.draw` CSS and JS, `geotiff`, `chart.js`, `jspdf-autotable`, `pdfjs-dist`, `pizzip`, `esri-leaflet`, and `leaflet.vectorgrid` now carry `integrity="sha384-…"` attributes. Any CDN-served file that does not match the pinned hash is blocked by the browser before execution.

- **Security: pin all GitHub Actions to commit SHAs** — `deploy.yml` (`checkout`, `configure-pages`, `upload-pages-artifact`, `deploy-pages`) and `update-planning-data.yml` (`checkout`, `setup-node`, `git-auto-commit-action`) now reference full commit SHAs with version comments rather than mutable tags. The Slack action was already pinned in the previous commit.

- **Fix: `_reflectionsOn` now serialised in save/load and undo/redo** — The facade-reflections toggle was declared but never written to the save JSON, so reloading a file or performing an undo always silently reset reflections to OFF regardless of what was saved. Fixed: `reflectionsOn` field added to `data.propagation` in both the export serialiser and the undo/redo serialiser. Load path restores the value and syncs the Reflections toolbar button's `active` class. **Not touched**: propagation formulas, criteria logic, other save fields.

- **Fix: line source floating panel now draggable** — `attachLsPanelListeners` referenced undeclared variable `periodInp` in the per-period `forEach` loop (line 34285), causing a `ReferenceError` on the first iteration that crashed the function before the drag setup code was ever reached. Fixed by replacing `periodInp` with `pctInp` (the operating-% input declared immediately above) — correct variable name and the missing `input` event listener that the area source panel already had. All other panels unaffected.

- **Line source drawing now finishes on single click of last vertex** — Previously, drawing a line source required a second confirmation click on the last vertex marker (or a double-click) to complete. A `draw:drawvertex` handler now calls `_lsDrawHandler.completeShape()` as soon as ≥2 vertices have been placed, so the Nth vertex click is the finishing click — no extra action needed. Barrier, CoRTN, and all other drawing tools unchanged.

- **RHS Criteria & Compliance panel now resizes the map dynamically in maximised map mode** — `#drawer-panel` was previously `position:absolute` overlaying the map at `z-index:100` with a `transform:translateX()` open/close animation; the map container never shrank. Fix: added `transition: right 0.3s cubic-bezier(0.4,0,0.2,1)` to `#map-column`. `setDrawerOpen()` now sets `mapColumn.style.right` to the drawer's `offsetWidth` when open and `'0'` when closed. A `transitionend` listener on `#map-column` for the `right` property calls `map.invalidateSize()` after the transition completes. The drawer resize-drag handler and window-resize re-clamp handler also update `mapColumn.style.right` in lockstep. On-load width restore from localStorage now also syncs `mapColumn.right`. The redundant `setTimeout(350ms, invalidateSize)` on `drawerToggle.click` was removed (transitionend handles it). LHS panel behaviour, drawer content, tab switching, PDF export, and all calculation/criteria logic untouched.


- **Generalise planning-layer click-passthrough for all map-click tools** — Introduced `window._enterMapClickMode(name)` / `window._exitMapClickMode(name)` (defined in the planning IIFE alongside `_setPlanningCanvasInteractive`). These maintain a named-mode set: planning canvas `pointer-events` is set to `none` while any mode is active, and restored to `''` only when all modes have exited — safe for overlapping transitions. `setMode()` (source/receiver placement) now calls the new API instead of calling `_setPlanningCanvasInteractive` directly; on placement entry it also calls `window._cancelRulerMode()` so the two tools never fight. Ruler tool wired to the API: button activate calls `_enterMapClickMode('ruler')`, button cancel/second-click complete calls `_exitMapClickMode('ruler')` via the new `window._cancelRulerMode()` helper. Escape key handler extended to call `_cancelRulerMode()`. Draw tools (line source, area source, building source, barrier, ground zone, CoRTN road) use Leaflet.draw's own event-capturing overlay — they do not interfere with the planning canvas and are left unchanged. **Not touched**: propagation, criteria, save/load schema, draw tools, planning layer data/styling/popups.

- **Re-assigned default library entries for 8 Suggested Noise Source items and 2 Industrial / Warehouse building properties. Renamed 'Medium exhaust fan' to 'Small exhaust fan' with save migration.** — Corrected `searchName` values across all FACILITY_SUGGESTIONS categories so each default resolves to the correct library entry: condenser now targets `'medium double fan'` (was `'condenser'`, which matched the small unit first); exhaust fan now targets `'garbage room'` to uniquely find "Small exhaust fan (toilet, garbage room)"; car wash now targets `'car wash - auto'`; fire pump now targets `'PowerMaXX'` (was `'fire pump'` which failed substring match against "Firepump"); electric forklift now targets `'electric forklift - typical'`; patron now targets `'patron in bar'`; children now targets `'2 - 3 years'` with `isStub` removed. Industrial / Warehouse building source `searchName` updated to `'Warehouse - internal reverberant'` for interior Lp; new `constructionSearchName: 'Sheet metal'` field applied to `defaultConstruction` and `roofConstruction` in `_suggestAddSelected`. Load-time migration renames saved point sources named `"Medium exhaust fan"` to `"Small exhaust fan"`. **Not touched**: library Lw/spectra values, category labels/order, keyword arrays, UI layout, save/load format version.

- **'Not placed — click to place' for line/area/building/CoRTN sources in Objects window** — Unplaced line, area, building, and CoRTN sources now show "not placed — click to place" in the Objects window (matching existing point source behaviour). Clicking an unplaced row activates the corresponding draw tool; the first map click is the first vertex of the shape. Draw completion routes vertices into the existing unplaced source object via a pending-ID pattern (`window._pendingLsId`, `_pendingAsId`, `_pendingBsId`, `_pendingCortnId`) rather than creating a new one. CoRTN roads are now listed in the Objects window with full delete and duplicate support. **Not touched**: existing placed-source behaviour, left-click/right-click on map layers, draw tool logic, save/load JSON schema, propagation formulas, criteria logic.

- **Extended keyword matching for Suggested Noise Sources panel** — Keyword arrays for all 10 facility categories in `FACILITY_SUGGESTIONS` updated to cover Australian synonyms, abbreviations, brand names (McDonald's, KFC, Hungry Jack's, BP, Shell, Caltex, Ampol, Mobil, OTR, 7-Eleven), OSM tag values (car_park, fast_food, fuel_station, industrial_estate), and common shorthand (servo, kindy, OSHC, ELC, HQ, gen set). Added `_normKw()` helper to `_soPdfScanSources` that strips diacritical marks (NFD decomposition) and normalises curly/smart apostrophes to straight before comparison — enabling "café" to match via "cafe" and "McDonald's" (curly apostrophe) to match via "McDonald's". No changes to category names, source defaults, Lw values, UI layout, "Other facilities" fallback logic, or save/load schema.

- **Fix criteria delay when "Show zones" layer is OFF** — Zone cache (`sa-zones.geojson`, 5–7 MB) was lazy-loaded: only fetched when the zones layer was toggled ON or on first `_findZoneAtPoint` call. With zones layer OFF, the first receiver placement triggered a 2–4 second fetch before criteria could compute — user saw dashes immediately after placement. Fix: added a single `_ensureZoneCache()` fire-and-forget call at planning IIFE initialisation (after global API exports, before canvas API). Cache begins loading at page startup; by the time a user places their first receiver (typically 5–30 s later) the fetch is complete and PIP resolves in ~7 ms. Deduplication via `_zoneCacheReady` promise means any concurrent `_findZoneAtPoint` calls during the fetch await the same promise — no double-fetch. **Not touched**: zones layer toggle, zone display logic, criteria logic, MBS 010 screening, save/load schema, propagation formulas.

- **Fix MBS 010 screening regression — replace dead SAPPA queries with local GeoJSON PIP** — Root cause: `SAPPA_API_UNAVAILABLE = true` caused `querySAPPAOverlays` to short-circuit before running any screening logic, leaving all rows as "—". Fix: removed the `SAPPA_API_UNAVAILABLE` early-exit block from `querySAPPAOverlays`. ANEF query (was `SAPPA_OVERLAYS_BASE + '/1/query'`) replaced with `_runAnefCheck()`: PIP against `./data/overlays/aircraft-noise.geojson`, reading `f.properties.anef_contour` (e.g. "ANEF 25") with `_mbsAnefGeoJSON` cache. Noise & Air Emissions query (was `SAPPA_OVERLAYS_BASE + '/0/query'`) replaced with `_runNoiseCheck()`: PIP against `./data/overlays/noise-air-emissions.geojson`, reading `f.properties.overlay_name`, with `_mbsNoiseGeoJSON` cache. Both functions defined inside `querySAPPAOverlays` for closure access to `latlng`. ANR, roads, rail, and mixed land use logic unchanged — they were always local-GeoJSON-based and are now reachable since the early exit is gone. `SAPPA_API_UNAVAILABLE` flag retained (still guards old ESRI dynamicMapLayer/featureLayer instantiation). `_mbsAnefGeoJSON` and `_mbsNoiseGeoJSON` cache vars added alongside `_anrGeoJSON`. Handles null geometries (present in PlanSA data) and "Area is subject to aircraft noise" non-numeric contour strings gracefully. **Not touched**: ANR logic, road/rail distance calculations, mixed land use zone query, zone PIP, save/load schema, propagation formulas.

- **Consolidate PlanSA overlays into MBS 010 Screening** — Removed the standalone "Planning layers (display only)" menu group (separator, header, "Noise & Air Emissions" button, "Aircraft Noise (ANEF)" button). The two overlay layers now load directly within `loadMBS010Layers()` from local GeoJSON files (`./data/overlays/noise-air-emissions.geojson` and `./data/overlays/aircraft-noise.geojson`) when the MBS 010 toggle is activated. Graduated ANEF styling reuses `anefStyle()` from the planning IIFE via the new `window._anefStyle` cross-IIFE API. Legend swatches updated: Noise & Air Emissions → `#d62728`, ANEF → `#ff9a5a` (ANEF 30 fill colour). Removed from planning IIFE: `toggleNoise()`, `toggleAircraft()`, `_planNoiseOn`, `_planAircraftOn` state vars, `loadNoiseLayer()`, `loadAircraftLayer()`, `_noiseLayer`/`_aircraftLayer` layer cache, event listener wiring for the two removed buttons. `updateAttribution()` simplified to `anyOn = _planZonesOn`. `_getPlanningLayers()` returns `{zones}` only; `_setPlanningLayers()` acts on zones only. Save/load fallback defaults updated from `{zones,noiseAirEmissions,aircraftNoise}` to `{zones}`. Old saves with `noiseAirEmissions`/`aircraftNoise` keys load cleanly — keys are silently ignored. `MBS010_OVERLAY_CFG.forEach` SAPPA API loop removed (was already dead — SAPPA CurrentOverlays decommissioned). **Not touched**: ANR contours, road/rail layers, MBS 010 legend checkbox wiring, propagation formulas, criteria logic, VIC/NSW state logic, save/load `_version`.



- Consolidated the Mapping menu's 'Show zones' and 'Zones (PlanSA)' toggles into a single 'Show zones' entry, backed by the static GeoJSON statewide zones layer.

- **Offline zone lookup + viewport-aware legend** — Replaced decommissioned SAPPA zone-identification API with offline point-in-polygon against the local PlanSA zones GeoJSON. Sources and receivers now auto-populate zone fields without any network call; first lookup triggers a single GeoJSON fetch (already cached if Zones layer is on). PIP uses bbox prefilter (O(n) scan of 5,386 bboxes → typically 1–3 full PIP candidates) — 2–3 ms per query after first load. `window._findZoneAtPoint(lat,lng)` exposed from planning IIFE. `queryZoningAtPoint` in map IIFE rerouted to call `window._findZoneAtPoint`. "SAPPA unavailable — select zone manually" message removed; null-result shows "No zone found — verify on SAPPA ↗". Legend now viewport-aware: uses bbox cache for O(n) filtering on pan/zoom, debounced 150 ms, capped at 40 zones ("Zoom in for zone legend" placeholder when exceeded). `zoomend` listener added alongside `moveend`. `updateZoneDescText()` updated for SA mode to cite PlanSA open data with link to SAPPA for authoritative verification. `window._planDataDate()` exposed for metadata date. Zone popup updated to reference PlanSA open data with SAPPA verification link.

- **Unified zone display — static GeoJSON zones layer uses existing SAPPA zone styling** — The parallel category-bucket system (`js/zone-categories.js`, `ZoneCategories`, 7-category colour map) has been removed. The static GeoJSON zones layer now uses the same `ZONE_COLOURS` table and `getZoneColour()` function already used by the SAPPA zone overlay (`window._getZoneColour` exposed from map IIFE). Same per-zone colours, same "P&D Code Zones" legend (viewport-dynamic, showing only zones visible on screen), same popup format. `js/zone-categories.js` deleted; script tag removed from `index.html`. `window._getZoneLegendControl()` and `window._populateZoneLegend(names)` exposed from map IIFE for cross-IIFE legend reuse. **Not touched**: SAPPA live API per-receiver, overlay layers, save/load schema, `_version`, ZONE_COLOURS table itself.

- **PlanSA Zones: switched from PMTiles to GeoJSON** — Eliminated `pmtiles@3.0.6` + `protomaps-leaflet@4.0.1` CDN dependencies and the tippecanoe build step. Fixes "protomaps-leaflet not loaded — Zones layer unavailable" error for users on networks where unpkg is blocked. Zones layer now uses the same `L.geoJSON` + `L.canvas()` renderer pattern as the Noise & Air Emissions and Aircraft Noise overlays. `scripts/update-planning-data.js`: removed `child_process`/`execSync`, replaced PMTiles output with mapshaper Visvalingam 10% simplification → `data/zones/sa-zones.geojson`; size guard updated to 30 MB. `update-planning-data.yml`: tippecanoe build-from-source step removed (Action runtime ~1-2 min vs ~5-6 min). `index.html`: CDN scripts removed; `buildZonesLayer()` replaced with `loadZonesLayer(callback)` (async, matches other two layers); shared `L.canvas()` renderer used for all three planning layers. `_version` not bumped. **Not touched**: SAPPA live API, VicPlan/NSW APIs, propagation formulas, overlay layers, save/load schema, zone category map.

- **PlanSA Planning Layers (display only) — Zones PMTiles + Noise & Air Emissions + Aircraft Noise (ANEF) overlays** — Three new display-only map layers sourced from SA Government open data (data.sa.gov.au, CC-BY 4.0). Added to Mapping▼ panel under a "Planning layers (display only)" group. (1) **Zones (PlanSA)**: statewide zone polygons rendered as a PMTiles vector tile layer via protomaps-leaflet v4.0.0 + pmtiles v3.0.6; zone colours driven by `js/zone-categories.js` (`window.ZoneCategories.categoriseZone()`); unknown zones render magenta as a QA signal; click popup shows zone name with SAPPA disclaimer; collapsible bottom-left legend; `ZONE_CATEGORY_MAP` intentionally empty pre-discovery. (2) **Noise & Air Emissions**: red polygon overlay from PDCodeOverlays GeoJSON filtered by "noise and air" substring. (3) **Aircraft Noise (ANEF)**: graduated contour overlay filtered by "aircraft noise" substring; colours keyed by `anef_contour` property; unknown contour values render magenta. New files: `scripts/update-planning-data.js` (discover/build data pipeline — Node ESM, uses adm-zip + mapshaper; discover mode works on Windows, build mode requires Ubuntu tippecanoe), `js/zone-categories.js` (category + colour map, empty pre-discovery), `data/zones/.gitkeep`, `data/overlays/.gitkeep`. New workflow `.github/workflows/update-planning-data.yml` (scheduled Sun 18:00 UTC; manual mode=discover|build; installs felt/tippecanoe from source; auto-commits `data/**`). `package.json` updated with `adm-zip` and `mapshaper` devDependencies. `index.html`: pmtiles@3.0.6 + protomaps-leaflet@4.0.0 CDN scripts added; `js/zone-categories.js` loaded in head; 3 mapping buttons + group label added to Mapping▼; planning layers IIFE added (attribution, legend, lazy-load, toggle, save/load API); `planningLayers: {zones,noiseAirEmissions,aircraftNoise}` serialised in both export and undo `serialiseState()`; loaded in `loadAssessment()` step 15c (defaults all false when key absent — old files load clean). **CRITICAL**: these layers are display only. SAPPA live API at each receiver remains the sole source of truth for SA criteria derivation. `_version` not bumped. **Not touched**: SAPPA API calls, VicPlan/NSW APIs, propagation formulas, barrier/terrain screening, criteria logic, any existing Mapping/Tools menu items.

- **Fix: SAPPA API migration — SA zone overlay, MBS 010 overlays, zone detection** — SA Government decommissioned `ePlanningPublic/CurrentPDC_wmas/MapServer` (confirmed HTTP 500 "Service not found"). All SAPPA references updated: `SAPPA_BASE` now points to `ePlanning/CurrentZones/MapServer`; new `SAPPA_OVERLAYS_BASE` points to `ePlanning/CurrentOverlays/MapServer`. Zone overlay (`zoningOverlay` dynamicMapLayer) layers restriction removed (no longer pinned to old layer 114). Zone feature layers and all point-query fetch calls updated from layers 113/114 → 0/1 in `CurrentZones`. MBS 010 overlay loading (`loadMBS010Layers`) switched from `SAPPA_BASE` to `SAPPA_OVERLAYS_BASE`; `MBS010_OVERLAY_CFG` ids updated from 39/27 → 0/1; legend keys updated to match. ANEF query (was `/27/query`) → `SAPPA_OVERLAYS_BASE + '/1/query'`; Noise overlay query (was `/39/query`) → `SAPPA_OVERLAYS_BASE + '/0/query'`; mixed-use zone check (was `/114/query`) → `SAPPA_BASE + '/0/query'`. `PARCEL_API_URLS.SA` set to `null` (was `ePlanningPublic/Survey_Map/MapServer/3/query`, also decommissioned) — SA parcel button hidden, MBS 010 falls back to marker-point distances rather than site-boundary distances. **Layer IDs 0/1 in new services are best-guess pending confirmation** — if zones/overlays still don't show, open DevTools → Network and check the query responses for the actual layer IDs, then update `SAPPA_BASE + '/0/...'` etc. accordingly. **Not touched**: VIC/NSW zone APIs, propagation formulas, MBS 010 road/rail distance calculations (speed zones service unchanged), save/load JSON schema.

- **Suggested Noise Sources (💡) — facility-group multi-source expansion** — `_SUGGEST_RULES[]` (flat keyword→source-name array) replaced with `FACILITY_SUGGESTIONS[]` (10 facilities, each with keywords[] and typed sources[]). Each source entry carries `{type: 'point'|'line'|'area'|'building', searchName, defaultName, isStub?}`. Panel now renders a two-level tree: facility row (parent checkbox, keyword match tag) → per-source rows (child checkbox, type badge, orange TODO badge if library entry missing). Parent checkbox toggles all children; child change propagates indeterminate state to parent. When no PDF loaded, all 10 facility groups are shown (no longer a blank state). When PDF scanned, matched facilities shown open at top, non-matched collapsed under "Other facilities". `_suggestAddSelected()` rewritten to create the correct source object type for each checked item (`sourcePins[]`, `lineSources[]`, `areaSources[]`, or `buildingSources[]`) and call the appropriate render function via `window._toolboxApi`. Library lookup now per-type via `_findLibByType(searchName, type)`. `_auditLibrary()` logs EXISTS/STUB status of every source entry to console when a PDF is scanned. **Known stubs** (Lw = null, need Deb to confirm): "Unloading activity" (area, loading dock + industrial warehouse), "Children (10 children 2–3 yrs)" (area, childcare). Sources with null Lw contribute zero to prediction — clearly flagged in "Add selected" toast. `window._pdfSuggestedGroups` structure updated to `[{id, label, matchedKeyword}]` (was `[{label, matched, sources:[strings]}]`); `.label` field preserved so `_pdcAnyMatch()` SA PDC auto-detect continues to work. `_LIB_HINT` removed. New CSS: `.suggest-type-badge`, `.suggest-todo-badge`, `.suggest-parent-cb`, `.suggest-other-hdr`. **Not touched**: library Lw/spectra values, propagation formulas, save/load JSON schema (new source objects are structurally identical to manually-created sources, with `_libName`/`_stubTodo` extra fields), SA/VIC/NSW criteria, PDF overlay rendering, Quick Reference panel.

- **Building construction dropdown: Sheet Transmission Loss replaces hardcoded Rw library** — `rebuildConstructionGrouped()` now pulls all `Transmission Loss` rows from `SourceLibrary.getLibraryForSourceType('building_facade')` and places them under a single `'Building Materials'` key in `CONSTRUCTION_LIBRARY_GROUPED`. Entries have shape `{name, octaveR:{63..8000}}` — no `rw` field. `_bsConsOptions(selectedName)` signature simplified: removed `categories` parameter; now iterates `Object.keys(CONSTRUCTION_LIBRARY_GROUPED)` to show all available groups. Option labels no longer include `' — Rw X dB'` suffix. All 6 `_bsConsOptions` call sites updated to remove the second argument. Existing guards in `_bsEffectiveRw()` and `_bsCompositeRwHTML()` on `Number.isFinite(cons.rw)` handle the absence of `rw` gracefully (composite Rw display disappears — matching intent). Hardcoded `CONSTRUCTION_LIBRARY` walls/roof/openings entries remain as fallback until SourceLibrary loads. **Not touched**: propagation formulas, octave-band TL application path (`octaveR` was already primary), save/load JSON schema, other dropdowns.

- **Google Sheets source library — sole active source (Supabase disabled)** — All five source-type dropdowns now route exclusively through `window.SourceLibrary` (`js/sources-library.js`). Supabase script tags (`supabase-config.js`, `library-loader.js`, `supabase-admin.js`) commented out with `─── SUPABASE (disabled 2026-04-22)` header. Hardcoded `LINE_SOURCE_LIBRARY` (10 entries), `AREA_SOURCE_LIBRARY` (5 entries), and `BUILDING_LP_LIBRARY` (12 entries) replaced with `[]`; declaration-time `SOURCE_LIBRARY_GROUPED` (~98 entries) now marked as immediately overwritten. All four `rebuildXxxGrouped()` functions rewritten to populate from `SourceLibrary.getGroupedLibraryForSourceType(context)` with per-type Sheet-row adaptation (point: `{name,lw,spectrum:[8],height}`; line: `{name,lw_m_dba,spectrum_unweighted:{...},height_m}`; area: `{name,lw_m2_dba,spectrum_unweighted:{...},height_m}`; building Lp: `{name,lp_dba,spectrum:{...}}`). `DATA_TYPE_APPLICABILITY` context keys updated: `'building'` → `'building_interior'`, `'Transmission Loss'` → `['building_facade']`, `'Lw, dB(Z)'` → `['point','area']`. `_resonateLibBoot` IIFE (Supabase hot-swap wiring) replaced with `_sourceLibBoot` that calls `SourceLibrary.loadSourceLibrary().then(rebuildAllLibraries)`. Line/area source change handlers and save/load library-entry resolvers updated to search `LINE_SOURCE_LIBRARY_GROUPED` / `AREA_SOURCE_LIBRARY_GROUPED` (now SourceLibrary-backed) instead of the empty flat arrays. Duplicate SourceLibrary optgroup block removed from line source dropdown; building Lp dropdown `'building'` SourceLibrary block replaced by `BUILDING_LP_LIBRARY_GROUPED`. GIS import `populateLibSel` calls use `||{}` fallback. Script tag bumped to `?v=4`. Cache key unchanged (row data unchanged; only context mapping updated). At runtime: 10 point/area categories, 1 line category, 3 building interior categories, 1 building facade category; `SourceLibrary.status === 'live'`; `supabasePresent: false`. **Not touched**: `CONSTRUCTION_LIBRARY` (hardcoded wall/roof/opening Rw data — no Sheet equivalent), propagation formulas, criteria logic, save/load JSON schema, barrier/terrain screening.

- **Scenario Comparison Phase 3: Comparison Table** — Completes the Scenario Comparison feature. `_compareSelection = {baselineId, includedIds}` (UI-only, reset on modal reopen, never persisted to JSON) added to scenarios IIFE. Modal box widened from 540 px to 900 px. When ≥2 scenarios exist, `renderScenariosModal()` now renders a Compare section: **Baseline** select (options sorted oldest-first; default = oldest), **Include** checkbox list (baseline always checked+disabled; all others default-checked), `<div id="scenarioCompareTable">` populated by `renderComparisonTable()`. Baseline/checkbox change handlers call `renderComparisonTable()` only — not full modal rebuild — so scroll position is preserved. `renderComparisonTable()`: resolves baseline (explicit → oldest → first), resolves included cols (explicit list → all), puts baseline first then remaining in original `_scenarios` order. Receiver list built from first-seen order across all included `stripData` arrays; only receivers with `placed=true` in at least one included scenario are shown. Period rows D / E / N / Lmax; rows with no numeric `pred` across any included scenario are skipped. Baseline column shows rounded pred, coloured `cs-ok`/`cs-bad` vs its own crit. Comparison columns show rounded pred with signed Δ vs baseline (`±0` / `+N` / `−N` using U+2212); Δ decoration is uncoloured; cell colour reflects the comparison scenario's own crit. Crit column: baseline's crit preferred, falls back to first included col with data. Receiver groups alternately striped (`background:#f8fafc`). Table wrapped in `overflow-x:auto` div. `_compareSelection` not exported to window — internal closure only. **Not touched**: Phase 1/2 behaviour, `_stripData` structure, compliance strip rendering, scenario data shape, `schemaVersion`, Save/Load JSON.

- **Scenario Comparison Phase 2: Restore & Overwrite** — Two new functions added inside the existing scenarios IIFE (no new script block): `restoreScenario(id)` pushes the current canvas state onto the undo stack (`_undoPushState('Restore scenario: …')`), calls `window._loadAssessment(scenario.state)` (passing the already-parsed object directly — no re-parse needed), closes the modal, and calls `showToast('Restored "…" — press Ctrl+Z to undo', 5000)`. `updateScenario(id)` confirms via `confirm()`, overwrites `scenario.state`/`stripData`/`timestamp` in-place (preserving `id`, `name`, `schemaVersion`), re-renders the modal, and calls `showToast('Updated "…"', 3000)`; does **not** push to undo stack. `showToast` is the existing app-wide helper (`showToast(msg, durationMs)` at line 14541 — no custom toast added). `renderScenariosModal()` button row extended from `[Rename] [Delete]` to `[Update] [Restore] [Rename] [Delete]` with `flex-wrap` so narrow widths don't break layout; Restore is bold to signal its destructive character. Click handler extended to dispatch `data-scen-update` and `data-scen-restore` attributes. **Not touched**: Phase 1 Save/Rename/Delete behaviour, undo/redo internals, `_loadAssessment()` signature, save/load JSON structure, `schemaVersion` (stays 1).

- **Scenario Comparison Phase 1: infrastructure & management** — `_scenarios[]` array and `SCENARIO_SCHEMA_VERSION = 1` added to the save/load IIFE (exposed on `window`). New **Scenarios** toolbar button (`.pdf-btn`, `#scenariosBtn`) appended after Share Assessment. Export handler serialises `_scenarios` into `data._scenarios` before `JSON.stringify`. `loadAssessment()` restores scenarios from `data._scenarios` before all other state, deep-copying `stripData`; schema-version mismatch emits a `console.warn` and skips the scenario; current canvas state is not affected. New scenarios IIFE (separate `<script>` block after save/load IIFE) provides: `_generateScenarioId()` (timestamp + 6-char random suffix), `saveScenario()` (prompt for name, captures `_serialiseState()` + `_stripData` deep-copy, pushes to `_scenarios[]`), `renameScenario(id)` (prompt with current name), `deleteScenario(id)` (confirm dialog), `showScenariosModal()` / `closeScenariosModal()` / `renderScenariosModal()`. Modal mirrors `showMethodologyModal` pattern: backdrop + box, Esc listener, backdrop-click closes, Tab/Shift+Tab focus trap cycles within modal, focus restores to `#scenariosBtn` on close. List sorted newest-first; each entry shows name, formatted timestamp, Rename and Delete buttons. Phase 3 comparison stub shown when ≥2 scenarios exist. Save/delete/rename do **not** push to the undo stack. **Not touched**: `_serialiseState()`, `loadAssessment()` internals beyond the new block, undo/redo system, `_stripData` structure, compliance strip rendering, `showMethodologyModal` implementation, existing save/load/export handlers beyond the one insertion each.

- **Right-click context menu on map objects** — All user-editable map layers now show a context menu on right-click: **Edit** (opens the same floating panel as a left-click), **Edit shape** (enables Leaflet vertex handles with floating Save/Cancel bar — polygons and polylines only), **Move** (activates move mode and briefly flashes the object), **Duplicate** (clones with `(copy)` suffix and ~2–5 m offset), **Delete** (reuses existing delete logic including confirmation dialogs). Menu items are filtered per object type: point sources and receivers hide Edit shape; receivers hide Duplicate. Receivers show Edit (opens popup), Move, Delete only. OSM buildings receive no menu. Shared helpers `_ctxActivateMoveMode`, `_ctxFlashMarker`, `_ctxFlashPolyLayer`, `_ctxStartShapeEdit` added above the `_showMapCtxMenu` infrastructure block. The `_ctxStartShapeEdit` function is a generic shape-edit-bar launcher that reuses the same floating Save/Cancel bar pattern as the in-panel Edit shape buttons. No new persistent state; all actions update the underlying data arrays and trigger the standard post-edit pipeline (terrain re-fetch, recalc, undo push). **Not touched**: left-click behaviour, drag behaviour, Objects panel, edit panel layouts, OSM building interactivity, save/load JSON schema.

- **GIS Import Phase 3: element creation + background terrain** — Implemented `executeImport(asgn)` — the execution engine for the GIS import pipeline. On Import click, all assigned features are converted to real map elements: Point sources (pushed to `sourcePins[]` with lw/spectrum from library, name from attribute, height from attribute or default), Line sources (pushed to `lineSources[]` with library entry reference and movements from modal), Area sources (pushed to `areaSources[]` with library entry reference), Building sources (created via `H.makeBuildingSource` hook + `buildingSources[]`, lpSource = 'octave' with library spectrum or 'broadband' with flat 75 dB placeholder), Custom buildings (pushed via `H.addCustomBuildings` batch hook), Barriers (pushed via `H.addBarriers` batch hook), Ground zones (pushed to `_groundZones[]` + rendered via `H.renderGroundZone`). Coordinate conversion: GeoJSON `[lng,lat]` → tool `[lat,lng]`; polygon closing vertex stripped. Batch render after all elements: one call per layer type. `render()` + `window._recomputeNoiseMap()` called after render; undo stack push. Optional zoom-to-imported via `map.fitBounds`. Toast summary with non-zero type counts (6 s). Background terrain fetch: rate-limited pool of 4 concurrent `DEMCache.getElevation` / `_fetchVertexElevations` calls, progress chip ("Fetching terrain… N/total"), aborts on new import or previous fetch. Chip turns green on success, amber on partial failure. `window._gisRenderHooks` added to map IIFE exposing `addCustomBuildings`, `addBarriers`, `fetchTerrainForCb/Ub`, `makeBuildingSource`, `renderAreaSourceLayer`, `renderBuildingSourceLayer`, `renderLineSourceLayer/Cards`, `renderGroundZone`, `isTerrainEnabled`. **Namespace**: removed `window.importGis` / `window.parseGisFile` / `window._closeGisModal`; replaced with `window._gisImport = {importGis, parseGisFile, closeModal}`. Escape modal-stack note added as comment. **Not touched**: Phases 1/2 parse/modal logic, save/load JSON schema, propagation, criteria, all existing element types.

- **GIS Import Phase 2: assignment modal + preview** — Extended the Phase 1 GIS import module with a full assignment UI. On file import, an assignment modal opens (Quick Reference panel styling: `rgba(26,32,44,.92)` header, `border-radius:10px`, 720px max-width, 80vh height). Per-layer blocks show the layer name, `sourceCRS` extracted from the `.prj` file, and a count of features per geometry type. Per geometry type (points/lines/polygons): **Import as** select (geometry-appropriate options — points: skip/Point source; lines: skip/Barrier/Line source; polygons: skip/Custom building/Building source/Area source/Ground absorption), **Source library** dropdown (populated from `AREA_SOURCE_LIBRARY_GROUPED` etc., conditionally shown), **Interior Lp** section for Building source (flat dB radio with numeric input or library dropdown), **Movements** row for Line source (Day/Evening/Night numeric inputs + period select). Attribute helpers auto-detect name and height fields from feature properties; **Filter by attribute** controls allow import to be restricted to a specific attribute value (dropdown for ≤50 distinct values, text for >50). A live dashed-grey Leaflet preview layer (`_gisPreviewLayer`) is updated on every form change and removed on Cancel/Escape/×, but stays on the map after Import. Import button stays disabled with a hint message until all required fields are filled (`_validateImport`). Warnings from parsing (missing `.prj`, hole-stripping, skipped features) shown in a banner at the top of the modal body. Escape key registered in capture phase (like other modals) to prevent other handlers from intercepting. On Import click: `gatherAssignments()` result logged to console; modal DOM removed; preview stays; Phase 3 placeholder alert shown. **Not touched**: existing data arrays, save/load JSON, propagation, criteria, other toolbar entries, Phase 1 parse/reproject logic.

- **GIS Import Phase 1: parser infrastructure** — New "Import GIS file…" button added to the top of Group 2 in the Tools▼ dropdown. Hidden `<input type="file" id="gisFileInput" accept=".zip,.geojson,.json,.kml">` wired to the button. New GIS import module (inline `<script>` block at end of `index.html`) provides: (1) `loadScriptOnce(url)` — lazy script loader with Promise caching so libraries load only once; (2) AU CRS pre-registration — `AU_CRS_DEFS` object with proj4 definition strings for EPSG:7844/7853/7854/7855 (GDA2020), EPSG:28353/28354/28355 (GDA94 MGA), EPSG:3577 (GDA94 Albers); proj4 is loaded first, CRSs registered, then shpjs loads so it picks up the definitions; (3) `parseGisFile(file)` dispatcher — `.zip` → shpjs (multi-layer aware, uses `fc.fileName` as layer name), `.geojson`/`.json` → JSON.parse + FeatureCollection validation + CRS warning, `.kml` → togeojson, `.kmz` → alert and abort; post-parse: Multi* geometry expansion into individual features, polygon hole stripping (outer ring only), null geometry filtering; (4) validation guards — 10 MB confirm, >1000 features abort, >300 confirm, coordinate sanity check (>10% out of WGS84 range → abort with clear reprojection message, 1–10% → skip bad features + warn); (5) `importGis(file)` — shows status chip (bottom-right), calls parser, `console.log`s result structure and warnings, alerts feature/layer summary. `window.importGis` and `window.parseGisFile` exposed globally. **Not touched**: any existing data arrays, save/load JSON, propagation calculations, criteria logic, other toolbar entries.

- **Fix: terrain contour seam artefacts — Gaussian pre-smooth on contour grids** — `gaussianSmoothGrid()` helper added to `index.html`; called from `generateTerrainContours()` on `ctGridLidar` and `ctGridSrtm` before marching squares runs (sigma=1.5 grid cells, separable 1D passes, normalised convolution so NaN/no-coverage cells are excluded). Removes false linear "ridges" at Adelaide Metro LiDAR survey footprint boundaries. Raw `ctGridLidar`/`ctGridSrtm` arrays are not mutated; `sampleRaster` (source/receiver elevation lookups) is completely unaffected. Noise map and CoRTN contour renderers untouched.

- **Fix: terrain contour stepping — Chaikin smoothing + bilinear DEM sampling** — `renderContours()` in `generateTerrainContours()` and `DEMCache.sampleRaster()` in `index.html`. Two root causes: (1) `renderContours()` built polylines directly from marching-squares output without smoothing, unlike the noise map (`generateContourLines`) and CoRTN (`generateCortnContourLines`) which both apply `chaikinSmooth(chain, 3)`. Fixed by calling `window.chaikinSmooth(chain, 3)` on each chain before constructing the Leaflet polyline; `chaikinSmooth` exposed as `window.chaikinSmooth` so it is accessible from the terrain contour scope. (2) `sampleRaster` used `Math.round()` (nearest-neighbor), snapping every elevation query to a DEM pixel center and causing up to ±10 m positional error per contour vertex on ~20 m resolution WCS tiles. Replaced with standard bilinear interpolation across the four surrounding pixels; nodata corners fall back to the nearest valid corner. **Not touched**: WCS tile fetch size cap (100×100 px — deferred), marching squares algorithm, Chaikin algorithm, SRTM fallback logic, source/receiver elevation lookups, propagation or criteria.

- **3D area source drape: terrain grid cell mesh (interior Steiner parity)** — `_buildOneAreaSource()` in `index.html`. Previous edge-densification fix still left large interior triangles that clipped through terrain bumps between boundary vertices — ShapeUtils.triangulateShape (earcut) adds no interior Steiner points. Root cause: polygon interior is only sampled at user-placed boundary vertices; interior of each triangle is linearly interpolated, diverging from actual terrain when long triangles span varied terrain. Fix: new PRIMARY PATH iterates the stored `_terrainGrid` directly, selects all cells whose centre falls inside the polygon (2D ray-cast PiP in scene x,z coords), and builds a quad mesh from those cells using the exact same elevation data as the terrain mesh — zero interpolation gap by construction. `+0.05 m` vertical offset prevents z-fighting with the terrain mesh faces. Bounding-box pre-filter limits iteration to terrain rows/cols that can possibly overlap the polygon. New `_asPip2D(px, pz, polygon)` helper performs the 2D PiP in scene x,z (not lat/lng). Label anchored to the maximum Y of all mesh vertices + 4 m so it clears the highest ridge. FALLBACK (terrain off / tiles not yet loaded / polygon outside terrain coverage): reverts to densified-boundary ShapeUtils triangulation (existing approach), preserving flat behaviour. **Not touched**: area source acoustic modelling, 2D map rendering, save/load, barrier rendering.

- **3D Scene Viewer: area source polygons drape to terrain surface** — `_buildOneAreaSource()` in `index.html`. Area source polygons were rendered as flat quads at a single elevation, causing visible clipping through hills or floating above valleys. Fix ports the barrier draping approach: (1) vertex normalisation stores geodetic + local scene coords side-by-side; (2) `_sampleTerrainMeshAt` is used first (exact triangle-level interpolation from stored terrain grid), falling back to `sampleTerrainAt` — matches ground zones and barriers; (3) each polygon edge is densified at ~5 m sub-intervals, identical to barrier densification, so terrain bumps between user-placed vertices are tracked; (4) the densified outline is triangulated via `THREE.ShapeUtils.triangulateShape` with per-vertex draped Y; (5) the text label is re-anchored to the average of all densified edge points (draped centroid) rather than the flat user-vertex centroid. When terrain is off, `_sampleTerrainMeshAt` falls back to `sampleTerrainAt` which returns `null`, so all vertices sit at Y=0 — flat behaviour is preserved. **Not touched**: area source acoustic modelling (Lw, spectrum, propagation), 2D map rendering, save/load JSON, barrier rendering (reference implementation unchanged).

- **PDF appendix table captures switched from PNG to JPEG** — `captureElement()` in `generatePDFAppendix()` changed from `toDataURL('image/png')` to `toDataURL('image/jpeg', 0.95)`. Root cause of recurring "Incomplete or corrupt PNG file" alert: jsPDF's pure-JS PNG parser (`png.js`) cannot handle very large PNGs produced by html2canvas at scale:3 on a 1100px-wide criteria panel (3300px output, 10–40 MB). JPEG path is simpler and robust at any size. `placeImage(img, label, maxH)` updated with a `label` parameter — sets `_pdfLabel` as its first action (guaranteeing the catch block always names the failing section even if `addImage` throws immediately), then auto-detects format from data URL prefix (`data:image/jpeg` → `'JPEG'`, `data:image/png` → `'PNG'`) and passes the correct format string to `pdf.addImage()`. Diagnostic log entry now includes a `format:` field. `willReadFrequently: true` added to html2canvas options as a performance hint. All five `placeImage()` call sites updated to pass label as second argument; redundant `_pdfLabel` pre-sets removed. Zone map section (Figure 1, Phase 1) continues to capture as JPEG directly via `pdf.addImage(..., 'JPEG', ...)` — unchanged. **Not touched**: zone map capture pipeline, basemap whitelist, force-render pass, JPG export, save/load, propagation, criteria.

- **PDF appendix fixes: zone polygons + criteria render** — Three fixes to `generatePDFAppendix()` in `index.html`. **(a) Zone polygon whitelist**: replaced `_pdfKeep.has(layer)` (Set identity check) with `_isPdfWhitelisted(layer)` which also calls `g.hasLayer(layer)` for each whelist group — catches child features of ESRI FeatureLayer / Leaflet LayerGroup that appear individually in `map._layers`. **(b) Force-render before table captures**: `renderSADerivTable()`, `renderNSWDerivTable()`, `renderEmergencyEquipCriteria()` called between map restore and Phase 3 table captures so tables are populated regardless of which panel the user has open. **(c) PDC guard fixed**: changed `window.getComputedStyle(pdcSection).display !== 'none'` to `currentState === 'SA'` — previously the PDC section was skipped whenever the Criteria nav section was collapsed (the `section-hidden` class on an ancestor made `getComputedStyle` return `none` even for SA state). Added `[pdf-appendix] whitelist diag` and `[pdf-appendix] target` console logging for ongoing diagnostics. **Not touched**: Phase 2 canvas compositor, Phase 4 PDF assembly, JPG export, save/load, propagation, criteria derivation.

- **PDF appendix zone map — whitelist-based layer isolation** — Phase 1 of `generatePDFAppendix()` replaced from a hand-maintained blacklist to an `_map.eachLayer()` whitelist. Only three things appear in Figure 1: street basemap + parcel boundary (`sourceParcelOutline`/`sourceParcelOutline2`) + active zone overlay (`zoningOverlay` / `vicZoningOverlay` / `nswZoningOverlay` + SA outline/subzone layers). Every other Leaflet layer is removed for the duration of capture and restored in original z-order immediately after. Aerial basemap is always swapped to street map for capture (extra 400 ms settle for tile load); aerial is restored afterwards. New `window._getPdfZoneCaptureState()` function exposed inside the map IIFE to pass live layer refs (including dynamic `sourceParcelOutline`) to `generatePDFAppendix()` at export time. Bottom-right Leaflet controls hidden explicitly via `display:none` as a belt-and-suspenders guard. **Not touched**: `_prepareZoneCapture`/`_restoreAfterCapture` (used by the separate `exportCriteriaPdf()` flow), JPG export, table capture Phase 3, PDF assembly Phase 4, save/load, propagation, criteria.

- **Fix: PDF appendix export "Incomplete or corrupt PNG file"** — `generatePDFAppendix()` in `index.html`. Two root-cause fixes: **(1) `captureElement()` zero-size guard**: after `html2canvas` resolves, added `if (!c.width || !c.height) return null` before calling `toDataURL` — a 0×0 canvas produces a degenerate PNG that jsPDF throws on. **(2) Data URL validity guard**: `toDataURL` result is checked against `data.length < 100` and `startsWith('data:image/png')` before being returned; invalid results return `null` instead of propagating. **Diagnostic logging added**: `placeImage()` now emits `console.log('[pdf-appendix]', label, { dataUrlLength, dataUrlPrefix, aspect })` before every `pdf.addImage` call. The zone map JPEG path has the equivalent guard for `'data:image/jpeg'`. **Section labelling**: `_pdfLabel` variable tracks which section is being processed (`'zone map'`, `'PDC interface'`, `'receivers & criteria'`, `'emergency equipment'`, `'music criteria'`, `'childcare criteria'`); catch block now alerts `"PDF appendix export failed at [label]: message"` instead of a generic message. **Not touched**: zone map capture pipeline, JPG export, report (.docx) export, save/load, propagation, criteria logic.

- **Project # / Project name typography + LHS toolbar cleanup** — **(1) Typography**: Restyled the Project # and Project name input fields from `font-size:18px;font-weight:700` to `font-size:12px;font-weight:600`, matching the RHS sidebar card heading style. Border-bottom thinned from 2px to 1px. Project # field width reduced from 120px to 100px. Padding reduced from `6px 2px 4px` to `3px 2px 2px`. **(2) LHS toolbar "Hide/Show assessment panel" removed**: Removed HTML `#mapMaximiseBtn` button element; removed the Phase 4 JS block that repurposed it into `#mp-toggle-drawer` / `#drawerPanelBtn`; removed `{ el: drawerExpandMp }` from `toolbarOrder`; removed label-sync lines from `_setDrawerOpen`. The `toggleMapFullscreen` function (keyboard shortcut E) is preserved. **Not touched**: propagation formulas, criteria logic, save/load, barrier/terrain screening, RHS drawer open/close behaviour.

- **Fix: RHS "Results" tab showed no panels** — Root cause: the outer criteria wrapper `<div class="grid2" data-section="criteria">` (line 2815) spans the entire content area and all four results panels were nested inside it. When "Results" was clicked, the wrapper got `section-hidden`, hiding everything inside including the results panels. Fix: (1) widened the wrapper's `data-section` from `"criteria"` to `"criteria results"` so it stays visible under both tabs; (2) added `data-section="criteria"` to the inner "Receivers & criteria" `card.span2` so that card still hides under Results; (3) added stable IDs to the three previously anonymous results panels — `#contribSection`, `#charPenaltySection`, `#predNoiseSection`; (4) changed `_insertGroupAnchor('group-results', ...)` to anchor on `#assessmentCasesSection` instead of `#predTableDay` so clicking "Results" scrolls to the first results panel. **Not touched**: propagation formulas, criteria derivation logic, save/load JSON schema, barrier/terrain screening, LHS side panel, Criteria tab content.

- **Sidebar UX overhaul — resizable LHS, nametag tabs, RHS defaults collapsed** — Series of improvements to the two side panels. **(1) LHS width** increased from 225px to 280px so the 6-icon toolbar row always fits. **(2) Resizable LHS**: `#side-panel-resize-handle` (16px wide, absolute right edge) added; drag resizes panel width (160px–42% viewport), saved to `localStorage('resonate_side_panel_width')`. Click (< 5px drag) still toggles collapse. **(3) Collapsed nametag tabs**: When LHS collapses to `width:0`, a sibling `#side-panel-nametag-tab` appears at left edge (dark pill, vertical text "Planning & modelling"); clicking it re-expands. RHS gains a matching `#drawer-collapsed-nametag` ("Criteria & compliance") when drawer is closed. **(4) CSS-class-driven visibility**: Toggle arrow (`#side-panel-toggle`) and nametag visibility are now controlled entirely by CSS selectors (`#side-panel.collapsed #side-panel-toggle { display:none }` and `#app-layout.side-panel-collapsed > #side-panel-nametag-tab { display:block }`). Removed all `toggle.style.display` and `spNametag.style.display` inline manipulation from `setCollapsed()` — fixes the "collapse arrow disappeared" regression. **(5) RHS drawer defaults closed**: Drawer initialises with class `drawer-closed` instead of `drawer-open`; user opens it manually via toggle or "Show assessment panel" button. **(6) Auto-expand removed**: Drawer no longer auto-opens when a receiver is placed. **(7) Expanded header label removed**: `#drawer-panel-label` div added (shown only when drawer open) replaces the old static h2-style label. LHS `#side-panel-label` kept but hidden via `#side-panel.collapsed #side-panel-label { display:none }`. **(8) Propagation/Custom sources panel styling**: Replaced `.penalty-btn` pill buttons in Propagation panel with `.mp-btn` row; removed bordered container in Custom sources panel, replaced with `.mp-sub-row` / `.mp-sep` standard patterns. Custom sources table `<th>` background changed from global yellow to blue (`rgba(59,130,246,.25)`) via `#side-panel th` override. **Not touched**: propagation formulas, ISO/CONCAWE calculation engines, criteria logic, save/load JSON schema, barrier/terrain screening.

- **Nav restructure: section filtering + LHS panel moves** — Drawer jump-nav replaced with section-filter model. Nav buttons (Setup / Criteria / Results / Recommended treatments) now show/hide entire sections rather than scrolling to anchors. `activateSection(sectionId)` toggles `.section-hidden` on all `[data-section]` elements inside `#drawer-content`; initial state is Setup. Sources nav section removed; Recommended treatments added. Two RHS panels moved to LHS collapsible accordions: **Propagation method** (`#mp-propmethod`) and **Custom sources** (`#mp-customsrc`) — old RHS wrappers hidden with `display:none`, IDs preserved so existing JS is unaffected. **Objects floating panel** (`#objectsFloatPanel`) added: opened via `#objectsToggleBtn` LHS button, draggable/resizable `position:fixed` overlay mirroring the help/suggest panels (same toggle+drag IIFE pattern, rescued to `<body>` via `_rescuePdfAreaFloatingPanels`). `_insertAllGroupAnchors()` updated (removed group-sources, added group-treatments pointing to `#recommendationsCard`). `recommendationsCard` `display:none` removed; section filtering manages its visibility. PDF export safe: `_restoreForPdfExport()` strips all `.section-hidden` classes before capture; `_restoreDrawerLayout()` re-applies `activateSection(window._activeNavSection || 'setup')` after restore. JPG export references updated from `objectsPanelSection` to `objectsFloatPanel`. Suggest-sources "add" handler now shows `objectsFloatPanel` instead of scrolling to old RHS section. **Not touched**: all propagation formulas, criteria logic, save/load JSON schema, barrier/terrain screening.

- **Objects follow terrain along their entire extent (per-vertex elevation cache)** — All multi-vertex objects (barriers, custom buildings, building sources, area sources, line sources, ground zones) now cache absolute-ASL terrain elevation at each vertex in a new `vertexElevations: number[] | null` field. **Terrain fetch**: `_fetchVertexElevations(obj)` queries `DEMCache.getElevations()` (LiDAR preferred, SRTM fallback) per object; `_fetchMissingVertexElevations()` back-fills all six object types on terrain toggle-on and after load. Fetch is also triggered on creation and on drag-end (re-fetches only the moved vertex's edge). **Barrier diffraction**: `hBar` (barrier-top height used in the Maekawa formula) is now terrain-aware across all four call sites — line source, point source, Lmax, and building source paths all use the new `_barrierHBar(bw)` helper, which calls `_interpolateBarrierTerrain(bw)` to linearly interpolate the crossing elevation along the barrier edge using `edgeVertexIdx` (threaded through `getDominantBarrier` / `getIntersectingEdges` in `shared-calc.js`). The noise map worker (`noise-worker.js`) mirrors this logic inline using the `vertexElevations` now passed with the buildings array. Falls back to flat-ground `baseHeightM + barrierHeightM` whenever terrain is off or elevations unavailable. **3D viewer**: `buildExtrudedFootprint()` extended with an optional `perVertexBaseY` array so custom buildings and building sources now follow terrain per-vertex in the 3D scene (was centroid-only before); barriers were already per-vertex in the 3D view. **Save/load**: `vertexElevations` serialised for all six object types in both the main export and the undo/redo snapshot; on load, missing fields are back-filled via `_fetchMissingVertexElevations()` after the 800 ms render delay. **Not touched**: ISO 9613-2 propagation formulas, point source / receiver height logic, Simple method, ground factor semantics, ISO/TR 17534-3 validation results on flat terrain, any criteria logic.

- **Fix: CONCAWE met category had no effect on predictions** — Two bugs: **(1) Main-thread source functions**: `calcLineSourceAtReceiver`, `calcAreaSourceAtReceiver`, and `_bsPropagateSub` called `SharedCalc.calcISOatPoint()` directly instead of routing through `calcISO9613single()`, bypassing the CONCAWE path entirely. Line source also checked `propagationMethod === 'iso9613'` without including `'concawe'`, so CONCAWE fell through to simple `attenuatePoint`. Fixed all three to add explicit CONCAWE branches calling `SharedCalc.calcConcaweAtPoint()` with `_concaweMetCategory`. **(2) Worker no-spectrum fallback**: when CONCAWE was selected but the source had no octave spectrum and no ground zones were drawn, the worker fell through all CONCAWE/ISO branches to simple `attenuatePoint`, ignoring K3/K4/K5 entirely. Added a catch-all `method === 'concawe'` branch in `noise-worker.js` that creates a flat 8-band spectrum and routes through `calcConcaweAtPoint`, matching the pattern used for the ISO no-spectrum fallback. **Not touched**: `calcConcaweAtPoint` engine, K3/K4/K5 functions, ISO 9613-2 path, save/load, UI.

- **Noise map auto-recompute on propagation changes + ISO 9613-2:1996 naming** — Two fixes: **(1)** Noise map now auto-recomputes when the propagation method, ISO parameters (G, T, RH, receiver height), or CONCAWE met settings (category, mode, wind, time of day, solar, cloud) change. Previously only source/receiver moves and equipment changes triggered recompute; propagation parameter changes required a manual click on the Noise Map button. `_recomputeNoiseMap()` added to the method change handler, ISO parameter handler, CONCAWE direct dropdown, mode toggle, time-of-day, and all computed input handlers. **(2)** ISO button label renamed from "ISO 9613-2" to "ISO 9613-2:1996" to include the standard year. Noise map method label updated to match.

- **Noise map method label** — Added `#noiseMapMethodLabel` inside `#noiseMapControls` (between Interval row and VIEW buttons). Shows the propagation method used for the **displayed contours** — not the currently selected method. Starts empty on load (no contours yet). Updated only when noise map generation completes (both main and case-mode worker paths). Method and met category are captured at worker-post time (`_nmGenMethod`, `_nmGenMetCat`) and passed to the label on completion, so switching the propagation method after generation does not change the label until new contours are computed. Labels: "Method: Geometric spreading", "Method: ISO 9613-2:1996", or "Method: CONCAWE — Cat N (description)" with category descriptions (strong upwind → strong downwind). Styled as muted 10px text matching the dark panel theme.

- **Methodology section updated with CONCAWE propagation model** — Added CONCAWE subsection to the Methodology card in `#methodologyCard` between "Barrier attenuation (ISO 9613-2 §7.4)" and "LAmax barrier and terrain screening". Documents the full K1–K6 formula, each term's definition, meteorological category system, key differences from ISO 9613-2 (ground effect, met conditions, frequency range), and the Report 4/81 reference. Overview paragraph updated from "Two propagation methods" to "Three". LAmax text updated from "simple or ISO 9613-2" to "regardless of the selected propagation method". Quick Reference propagation bullet updated to include CONCAWE. GIS metadata `method` field updated to output "CONCAWE Report 4/81" when CONCAWE is selected (was falling through to "Simple"). **Not touched**: ISO 9613-2 methodology text, CONCAWE calculation engine, Methodology panel mechanics, any calculation or criteria logic.

- **CONCAWE Prompt 4: meteorological input UI panel** — Added `#concaweMetPanel` inside the Propagation method card, visible only when CONCAWE is selected. Two input modes: **Mode A (Direct)** — dropdown select of met category 1–6 with descriptive labels (strong upwind → strong downwind). **Mode B (Computed)** — wind speed, wind direction (met "from" convention), time of day, solar radiation (day only), cloud cover (night only) inputs. Computes Pasquill class → stability group → met category via `SharedCalc.getPasquillClass()` / `pasquillToGroup()` / `getConcaweMetCategory()`, with source bearing calculated from first placed receiver to first placed source using `_concaweBearing()` geodetic helper. Live display strip shows Pasquill class, vector wind (with ±sign and downwind/upwind/crosswind label), and resulting met category. Solar radiation group hidden unless time=day; cloud cover group hidden unless time=night; both hidden for transition. Fallback when no source/receiver placed: defaults to cat 4 with "Place source & receiver" message. **Drag integration**: `_updateConcaweMet()` called at the top of `calcTotalISO9613()` when CONCAWE computed mode is active, so dragging a source or receiver recomputes the vector wind and met category automatically. **Save/load**: all CONCAWE fields (already serialised in Prompt 3) now wired to UI restoration on load — mode radio, direct dropdown, all computed inputs, solar/cloud visibility, display strip refresh. **Not touched**: CONCAWE calculation chain (Prompt 3), K3/K4/K5 engine functions (Prompts 1–2), ISO 9613-2 or Simple paths, existing propagation controls, barrier/terrain logic, noise map worker (reads `_concaweMetCategory` which the UI now sets), 3D viewer.

- **PDF Appendix Export — A4 criteria summary for acoustic reports** — New "Save PDF" button in the header export row (alongside Save Assessment / Load Assessment / Share Assessment) generates a compact A4 portrait PDF appendix showing planning context and criteria derivation, suitable for direct insertion into client-facing acoustic reports. Uses jsPDF + jspdf-autotable (CDN). **Map capture**: temporarily hides all non-zone objects (sources, receivers, barriers, buildings, contours, noise map, OSM buildings) using the existing `_prepareZoneCapture()` / `_restoreAfterCapture()` helpers, captures via html2canvas at 3× resolution, restores all layers immediately after. **PDF content** (top to bottom): (A) site image at full content width with address caption; (B) Receivers & Criteria table — html2canvas screenshot of the actual rendered criteria table from `#critBody .table-scroll`; (C) SA Derivation table — screenshot of `#saDerivTable` (SA only); (D) Emergency Equipment Criteria — screenshot of `#emergEquipTable` (conditional on srcCatEmergency + fire/generator); (E) Music Noise Criteria — screenshot of `#musicBgTable` (conditional on srcCatMusic); (F) Childcare / Student Criteria — screenshot of the childcare section table (conditional on srcCatChildcare). Tables are captured as screenshots of the actual rendered DOM elements (preserving all formatting, subscripts, state-specific columns) rather than being reconstructed. The `captureElement()` helper temporarily un-collapses/un-hides ancestor elements for capture, then restores state. Page overflow handled via per-section page-break checks. Page numbers centred at bottom. Filename: `{projectNumber}_Appendix_Criteria.pdf` or `Appendix_Criteria.pdf` if no project number set. **Not touched**: existing `exportCriteriaPdf()` (the ⇩ button in the Receivers & criteria card header — that remains unchanged), Generate Report (.docx), Save JPG, GIS Export, all calculation/propagation/criteria logic, save/load JSON format, zone detection APIs.

- **CONCAWE Prompt 3: full CONCAWE calculation chain + method selector** — Wired K1–K6 into the propagation engine as a complete prediction path. `calcConcaweAtPoint()` and `calcConcaweAtPointDetailed()` added to [`shared-calc.js`](../shared-calc.js), parallel to the ISO 9613-2 equivalents. K1=Adiv, K2=Aatm, K6=Abar reuse existing code; K3/K4/K5 use the Prompt 1/2 CONCAWE functions. Key difference from ISO: K3 is always applied regardless of barrier geometry (no §7.4 insertion-loss interaction). **UI**: "CONCAWE" added as third option in propagation method selector (`#propMethodGroup`). When selected, ISO parameter controls (receiver height, ground type, temperature, humidity) remain visible. Source height columns show for CONCAWE. Per-band breakdown panel dynamically switches headers between ISO (Adiv/Aatm/Agr/Abar/Aterr/AgrBar) and CONCAWE (K1/K2/K3/K4/K5/K6-terr). Met category defaults to 4 (neutral, K4=0) until Prompt 4 adds UI. **Save/load**: CONCAWE state fields serialised at both save points; old saves load cleanly with defaults. **Noise map**: worker branches for `method === 'concawe'` with spectrum and flat-spectrum paths. **Not touched**: Simple method, ISO 9613-2 calculations, LAmax path, barrier/terrain screening logic (reused as K6), CoRTN, 3D viewer.

- **CONCAWE Prompt 2: K4 meteorological correction** — Engine-only, no UI yet. Added to [`shared-calc.js`](../shared-calc.js) inside the `SharedCalc` IIFE and re-exported from [`calc.js`](../calc.js). **K4 meteorological correction** (`calcConcaweK4`): Simplification 2 distance-independent values from CONCAWE Report 4/81 §6.2 — single value per met category (1–6) per octave band, 0.5 dB(A) accuracy loss vs full polynomial model. Convention: positive = upwind attenuation, negative = downwind enhancement; category 4 = neutral (all zeros). 8 kHz mapped to 4 kHz. **Pasquill stability class** (`getPasquillClass`): lookup from wind speed (5 bins), time of day (day/night/transition), solar radiation (3 bins, day only), cloud cover (3 bins, night only). Intermediate classes (A-B, B-C, C-D) supported. Category G for night, clear sky, calm wind (<0.5 m/s). **Met category** (`getConcaweMetCategory`): from Pasquill group (AB/CDE/FG) + vector wind speed (m/s, positive = downwind). **Full-chain wrapper** (`calcConcaweK4FromMet`): raw met inputs → Pasquill class → group → met category → K4, with vector wind computed from wind direction (met "from" convention) and source bearing. Console test harness `window.testConcaweK4()` included. **Verified**: K4(500,4)=0, K4(500,6)=−6, K4(500,1)=8, 8kHz=4kHz, cats 1–3 all positive, cats 5–6 all negative; Pasquill A/G/D lookups correct; met categories 1/4/6 from group+wind correct; vector wind positive for downwind, negative for upwind, ~0 for crosswind; K3/K5 unaffected; ISO 9613-2 unaffected; no console errors.

- **CONCAWE Prompt 1: K3 ground attenuation + K5 source height correction** — Engine-only, no UI yet. Added to [`shared-calc.js`](../shared-calc.js) inside the `SharedCalc` IIFE and re-exported from [`calc.js`](../calc.js). **K3 ground attenuation** (`calcConcaweK3`): verified polynomial coefficients from CONCAWE Report 4/81 Appendix II, 7 octave bands (63 Hz – 4 kHz) with 8 kHz extrapolated from 4 kHz coefficients. Hard ground returns −3 dB (hemispherical correction); soft ground uses cubic polynomial `K3 = a₀ + a₁·log₁₀(d) + a₂·(log₁₀(d))² + a₃·(log₁₀(d))³`. Distance clamped to 100–2000 m (500/1000 Hz cubic terms cause rollover beyond 2000 m). Ground type mapped from existing G factor: G=0 → hard, G>0 → soft. **K5 source height correction** (`calcConcaweK5`): reduces K3+K4 effect for elevated sources based on grazing angle ψ = atan((hs+hr)/d). Gamma lookup from Figure 9 table (9-point linear interpolation, NOT the exp(−0.6ψ) approximation which is too inaccurate at small angles). Guards: K5=0 when hs≤2 m or when (K3+K4)≤−3 dB. Formula: K5 = (K3+K4+3)·(γ−1). Console test harness `window.testConcaweK3K5()` included. **Verified**: K3 250Hz@1000m=12.9, 63Hz@100m=−2.9, 500Hz@500m=8.5, hard=−3.0 everywhere, 8kHz=4kHz, distance clamps work; K5=0 when hs≤2, negative for elevated sources, diminishes with distance; gamma(0.25)=0.95, gamma(5.0)=0.01, gamma(6.0)=0.01 (clamped); existing ISO 9613-2 unaffected; no console errors.

- **Quick Reference (?) panel and Methodology section — full content refresh** — Both panels were written early in development and had drifted out of sync with the shipped feature set. **Quick Reference** rewritten end-to-end: section list reorganised from {Derive criteria, Save images, Modelling, Predictions, Noise mapping, Keyboard shortcuts} into {Getting started, Sources, Receivers & criteria, Map tools, 3D Scene Viewer, Propagation, Noise map, Save/export/report, Keyboard shortcuts} so the flow follows the user workflow rather than feature taxonomy. Sources section now explicitly covers all five flavours (point, line, area, building, road CoRTN) — previously only point + a vague "line/area/building" mention. Receivers & criteria block split per-state (SA INL + clauses 5(4)/5(5)/5(6) + MBS 010 / VIC IF + LGA overrides + NSA show/hide / NSW RBL + intrusiveness + amenity + sleep disturbance) instead of just a one-liner saying criteria are auto-derived. New 3D Scene Viewer section covers the toolbar entry, V shortcut, the read-only nature of the view, the always-available behaviour with terrain on or off, and the modal-scoped controls (vertical exaggeration slider, wireframe / labels / grid / axes toggles, reset view). Propagation section added — calls out simple vs ISO 9613-2 selection, atmospheric inputs, global G factor, the always-on barrier + terrain screening, the LAmax exception (no ground attenuation), and the in-app ISO/TR 17534-3 validation runner. Save/export/report section added covering the .json save/load round-trip, JPG screenshot, GeoJSON/KML/CSV export, and .docx report generation — none of which were previously documented. Custom buildings, ground absorption zones, barrier suppression, and the searchable source library are now mentioned in the Map tools / Sources sections. **Keyboard shortcuts list** cross-checked against the actual handlers in [`index.html:37936`](../index.html:37936) (main map), [`index.html:5778`](../index.html:5778) (3D modal), [`index.html:22281`](../index.html:22281) (undo/redo), [`index.html:6619`](../index.html:6619) (drawer toggle): added `W` (Road CoRTN), `V` (3D viewer), `Ctrl+Y` (alternative redo), the entire 3D modal-scoped shortcut block (`W` / `L` / `G` / `A` / `R` / `+` / `−` / `Esc`), and a footer note that `Ctrl` = `Cmd` on macOS. Fixed two stale entries: `]` was relabelled "Toggle side drawer" (was "Toggle side panel") and `E` was relabelled "Toggle map / panels view" — previously both were labelled "Toggle side panel" but the `E` handler at [`index.html:38059`](../index.html:38059) actually calls `toggleMapFullscreen()`, a different action. No phantom shortcuts left in the list (every entry corresponds to a real handler). **Methodology** section gained a new "3D Scene Viewer" subsection inserted between "Noise map" and "Code quality and validation" inside the Modelling methodology block. The subsection explains the viewer's purpose (visual verification of source heights, screening geometry, building obstruction, terrain profile in one orbital view), the read-only nature (no propagation/criteria/noise-map result depends on it), the rendering engine (Three.js r128 lazy-loaded on first open), the coordinate system (equirectangular local with cos(lat) compensation, X east / Z minus-north / Y elevation, normalised to local datum so a 30 m ASL site with 22 m of relief renders anchored at Y=0 not as a floating mesa), the building-aware object heights (rooftop fan with `height_m=2` on a 10 m custom building lands at Y=12 matching propagation), and the full list of rendered objects with their styling cues (terrain mesh with elevation colour ramp, OSM merged grey, custom buildings blue, building sources orange, barriers green with crest line, ground zones G-factor colour ramp, point/line/area sources red, receivers in the R1–R4 palette, sprite labels with anti-stacking). All other Methodology sections were already accurate after the recent Phase 1 / Phase 2 / reorder commits and were not re-edited. **Not touched**: Quick Reference panel open / close mechanics, drag, position memory, badge dot, header SVG; Methodology modal open / close mechanics, focus restoration guard, M2 hotkey gate, M3 id-prefix walk; any UI functionality, calculation logic, criteria logic, 3D viewer code, save/load schema. **Verified in preview**: panel collapses / expands cleanly, all 9 Quick Reference details elements render, Methodology section now has 36 headings (35 prior + 1 for the 3D Scene Viewer subsection), no broken HTML, no console errors.

- **VIC NSA "Show/hide on map" — skip save-restore so every reload starts all-off** — Follow-up to the static-label fix. Symptom: on reload from an assessment where NSA 1 had been toggled visible before save, the NSA 1 row in the detected-zones panel came back with the button styled "on" (coloured fill) but the circles may or may not have rendered — the button styling matched saved intent, not actual state. Root cause: the load path at [`index.html:21583`](../index.html:21583) restored `_vicRecvMapVisible` from `data.vic.recvMapVisible`, bringing back prior-session visibility that the circle-render pipeline hadn't yet caught up with. **Fix**: removed the restore line. `_vicRecvMapVisible` now always starts all-`false` on fresh load regardless of what's in the saved JSON. The save side is kept intact (still writes the field to `data.vic.recvMapVisible`) for backwards-compat with any older readers; it's just a write-only field now from this session's perspective. Users get a predictable clean starting state every open, button styling and actual map visibility stay in sync. **Verified live** after reload with no save loaded: `_vicRecvMapVisible` is `{r1:false, r2:false, r3:false, r4:false}`. No console errors.

- **VIC NSA "Show/hide on map" — static label + auto-hide bottom status banner** — Two linked fixes on the VIC criteria detected-zones panel. **(1) Static button text** at [`index.html:15126`](../index.html:15126): the per-NSA toggle button previously read `"Show on map"` / `"Hide on map"` driven by `_vicRecvMapVisible[id]`, but on reload from a saved assessment with NSAs previously visible the label would say `"Hide on map"` even if the circles weren't actually drawn yet (or vice versa) — state-and-text sync was brittle because `renderDetectedZones()` can run before `updateConcentricCircles()` has rebuilt the layers, and the async auto-IF callbacks recreate the button element after any direct DOM mutation. Switched to a single `"Show/hide on map"` label that always toggles, sidestepping the whole sync question. The button's coloured-fill vs outlined STYLE is still driven by `_vicRecvMapVisible` so the user still sees which NSAs currently have circles drawn, just without relying on the text to communicate it. **(2) Bottom status banner auto-hide** — the `.map-status-row` element (source / R1 / R2 / R3 / R4 placement badges) sits at `bottom:24px, left:10px` above the Leaflet attribution strip and overlapped the 140 m / 400 m NSA circles when they were drawn on the map. New helper `_updateMapStatusRowVisibility()` at [`index.html:15086`](../index.html:15086) sets `row.style.display = 'none'` when any of `_vicRecvMapVisible[r1..r4]` is true AND the app is in VIC-urban mode, otherwise clears the inline style so the row's own `display:flex` CSS takes over. Called from three places: `toggleRecvMapView()` (covers every user toggle), `clearAutoIF()` (covers state-switch and assessment-clear paths), and the top of `updateConcentricCircles()` (covers save/load-restore and any downstream path that redraws circles). The state-switch coverage is transitive: `switchToState()` already calls `clearAutoIF()` which resets `_vicRecvMapVisible` to all-false AND fires the helper, so switching from VIC-with-NSAs-visible to SA correctly restores the banner. **Verified live** (VIC urban, Melbourne CBD): banner initially `display: flex` (visible); after `_vicRecvMapVisible.r1 = true` + helper call → `display: none`; after also setting `r2 = true` → stays hidden; after hiding only `r1` → stays hidden (r2 still on); after hiding `r2` too → `display: flex` restored; with `r1` set true, switching state to SA → banner restored (state now `SA`, `clearAutoIF` triggered the helper which sees the non-VIC state and shows the banner). No console errors. **Not touched**: NSA circle geometry or radius calculations, VIC criteria derivation (IF, zone types, noise limits, LGA overrides), banner content or styling (only its inline `display`), SA or NSW criteria panels, map interactions, propagation calculations, save/load JSON schema (`_vicRecvMapVisible` is still persisted/restored — the state tracking wasn't removed, just the UI reliance on it for button text).

- **3D Scene Viewer — point sources reverted to smooth spheres** — The spiky "burst" (icosahedron with alternate vertices extruded) from the previous iteration didn't read well in practice. Reverted point sources to `THREE.SphereGeometry(0.8, 12, 8)` — identical to the receiver geometry. Sources vs receivers are now distinguished **by colour only** (red `#E53E3E` vs the R1–R4 per-index palette), not by shape. `_makeBurstGeometry()` helper deleted — dead code confirmed absent via `grep` for both `_makeBurstGeometry` and `IcosahedronGeometry`. Material unchanged (red `MeshLambertMaterial`). Label offsets unchanged — the +3 m offset for sources already clears a 0.8 m radius comfortably (it was sized for the 1.4 m spike tips, now has ~2.2 m of margin above a plain sphere, still reads fine). **Verified live**: source rendered with 117 verts (= `(12+1)×(8+1)` SphereGeometry), max radius 0.800 m, red `#E53E3E`; receiver with identical geometry stats, blue `#2563EB`; both at `position_y = 1.5` from the test placement. No console errors. **Not touched**: receiver geometry (already 0.8 m spheres from the previous change), marker XZ positions, building-aware height arithmetic, label text / billboard / anti-stacking, line / area / barrier geometry, any 2D or propagation code.

- **3D Scene Viewer — burst-shaped point sources + sphere receivers, label offsets tightened** — Two cosmetic refinements for the 3D viewer markers. **Point sources** were 2 m-radius smooth red spheres — visually dominant at close orbit distances and read exactly like the receiver cones once both are small. Replaced with a spiky "burst" shape built from `THREE.IcosahedronGeometry(0.8, 1)` with every other vertex pushed radially outward by 0.6 m, so the final shape has alternating 0.8 m and 1.4 m radii (dimples between spikes — sea-urchin silhouette). Reads unambiguously as an emitter. Helper `_makeBurstGeometry(radius, spikeLen)` at [`index.html:5172`](../index.html:5172); shared across all sources (one geometry, one material, N `Mesh` instances — same pattern as before). **Receivers** were 1.5 m-radius / 4 m-tall cones positioned with a +2 m Y offset (cone-centering quirk: cone apex at +halfHeight, base at -halfHeight, needed the offset to land the base at the receiver ear height). Replaced with `THREE.SphereGeometry(0.8, 12, 8)` — no orientation handling, no Y offset. Sphere centre placed directly at the receiver point (`y = groundY + buildingY + receiverHeight`), so a receiver at 1.5 m ear height renders with the sphere centre at Y=1.5, extending from ~0.7 m to ~2.3 m — natural visual for "person standing here". **Label offsets adjusted** for the new marker sizes: source label now 3 m above the burst centre (was 5 m above the 2 m sphere centre — the old offset needed 5 m to clear a 2 m radius, the new one clears 1.4 m spike tips with ~1.6 m of margin); receiver label 2.5 m above the sphere centre (was 6 m above the cone base to clear the 4 m cone height, new offset clears the 0.8 m radius with ~1.7 m of margin). Label anti-stacking logic in `_addLabel()` is unchanged — still a 5 m XZ tolerance with 4 m bumps. Sprite scale, depthTest, billboarding, label group membership all unchanged. **Verified live** (Adelaide CBD, source and receiver both at 1.5 m above a flat Y=0 surface): source renders at `position_y = 1.5` with geometry radius range `[0.800, 1.400]` and 80 triangles (level-1 icosahedron — correct); receiver renders at `position_y = 1.5` with 117 verts and max radius 0.800 (SphereGeometry(0.8, 12, 8) — correct); label Ys at 4.50 and 4.00 matching the new +3 / +2.5 offsets. Colours unchanged: source `#E53E3E`, R1 `#2563EB`. No console errors. **Not touched**: source / receiver XZ positions or any height calculation (including the building-aware offsets from the earlier fix), line source tube geometry (0.5 m radius `TubeGeometry` stays), area source polygon geometry, building source orange extrusions, label text content, billboard behaviour (`depthTest: false`, `renderOrder: 10`), label anti-stacking logic, receiver colour palette (R1 blue, R2 green, R3 amber, R4 purple), 2D source / receiver rendering, propagation or criteria logic.

- **3D Scene Viewer — building-aware object heights** — Sources, barriers, line sources, area sources, and receivers placed on top of a building footprint now render at `roof + object_height` in 3D rather than `ground + object_height`. Previously a rooftop exhaust fan with `height_m: 2` on a 10 m custom building rendered at Y=2 (inside the building) instead of Y=12 (sitting on the roof). The propagation engine has always treated `height_m` as height-above-surface and added `buildingHeight_m` at its call sites (e.g. `srcHeight = (pin.height_m || 1.5) + (pin.buildingHeight_m || 0)` at [`index.html:10292`](../index.html:10292) and similar); the 3D viewer was the only place using a bare `height_m` and producing the wrong Y. **Investigation before fix** — logged `s.height_m` / `s.groundElevation_m` / `s.effectiveHeight_m` / `s.buildingHeight_m` and traced through `_pointInPolygon` (existing top-level ray-casting PIP at [`index.html:10736`](../index.html:10736)), `_asGetBuildingHeightAtPoint` (which respects the Buildings-layer-visibility gate), and `detectBuildingUnderSource` (which writes `pin.buildingHeight_m` but also gates on the 2D layer visibility). Established that `height_m` is ABOVE-SURFACE, `buildingHeight_m` is the pre-computed roof height beneath the pin, and the sum is effective height above ground. **New 3D-local helper** `_3dBuildingHeightAt(lat, lng)` at [`index.html:4412`](../index.html:4412) — independent of the 2D Buildings-layer toggle because the 3D viewer always renders every flavour of building geometry, so an object on a rooftop must always land on it in 3D regardless of whether the user has the 2D layer visible. Priority order: (1) custom buildings (`window._getCustomBuildings()`), (2) building sources (`window._getBuildingSources()`, note the `vertices` + `height_m` field naming vs `polygon` + `heightM` for customs), (3) OSM (`window._buildings`, skipping entries flagged `isCustom` / `isBarrier` / `isBuildingSource` or `excluded` / `demolished` — same filter as `buildOSMBuildings`). First hit wins. Height fallback matches the Phase 3 building-extrusion code: `heightM → height → levels × 3 → 6 m`. Reuses the existing global `_pointInPolygon(lat, lng, polygon)`. **Call sites updated** — point sources at [`index.html:5110`](../index.html:5110): `y = groundY + buildingY + height_m`. Line sources at [`index.html:5234`](../index.html:5234): per-vertex `buildingY` so a tube crossing a footprint rises where the vertex falls inside the polygon and drops back to ground where it's outside — per-vertex transition (not per-segment), acceptable for v1. Area sources at [`index.html:5286`](../index.html:5286): per-vertex `buildingY` so a polygon partially overlapping a building drapes over the roof on one side. Barriers at [`index.html:4925`](../index.html:4925): per-endpoint `buildingY` so a barrier running from ground over a rooftop steps up at the vertex where it crosses the building edge. Receivers at [`index.html:5313`](../index.html:5313): `groundY += _3dBuildingHeightAt(lat, lng)` — a receiver on a balcony / rooftop sits at `roof + 1.5 m` rather than clipping through the building. Centroid labels for line + area sources also updated to include the centroid's building height so they float above the object even when the centroid is on a rooftop. **Verified live** (Adelaide CBD, zoom 14, 10 m custom building centred at the viewport): rooftop source with `height_m = 2` → position Y = **12** (`building 10 + height 2`); ground source with `height_m = 5` → Y = **5** (`0 + 5`); line source crossing the building with `height_m = 1` → tube geometry Y range **[0.5, 11.5]** (two outside endpoints at curve-Y=1, middle endpoint inside at curve-Y=11, tube radius 0.5 expands those to the observed range); area source partially overlapping with `height_m = 0.5` → polygon Y range **[0.5, 10.5]** (two inside vertices at 10.5, two outside vertices at 0.5 — the polygon drapes over the roof); barrier with `heightM = 3` crossing the building edge → quad geometry Y range **[0, 13]** (outside quad base 0 top 3, inside quad base 10 top 13 — the step happens at the vertex crossing); receiver on the roof → cone position Y = **13.5** (`building 10 + rHeight 1.5 + cone-centering offset 2`); receiver off the building → Y = **3.5** (`0 + 1.5 + 2`) — identical to pre-fix behaviour. No console errors. **Performance** — worst-case per-object-vertex O(buildings) cost, no spatial index. A 500-OSM / 50-line-source-vertex project runs at ~25,000 PIP tests per modal open, which is microseconds per test → well under 100 ms total. No observable slowdown in the test scenarios; spatial indexing can be added later if a project ever stresses this. **Not touched**: stored `height_m` / `groundElevation_m` / `buildingHeight_m` / `effectiveHeight_m` values (propagation engine owns these), propagation-engine height arithmetic (already correctly uses `height_m + buildingHeight_m`), 2D rendering of any object, building-layer visibility gate in `_asGetBuildingHeightAtPoint()` / `detectBuildingUnderSource()` (those are used by 2D + propagation code which correctly wants to respect the layer toggle), Phase 3 building extrusion, Phase 2 terrain normalisation, save/load schema, Phase 6 toolbar controls.

- **3D Scene Viewer — terrain normalised to local datum + label anti-stacking** — Fixes the "floating island" effect where a site at non-zero ASL (e.g. Adelaide CBD at ~30 m ASL with 22 m of relief) rendered the terrain mesh hovering 30 m above the OSM buildings + fallback plane that sit at Y=0, with cliff-like edges where the two coordinate systems met. The fix: pick a reference elevation (the minimum cached DEM sample at modal-open time) and subtract it from every Y value in the scene — terrain vertices, per-building base elevations, barrier base elevations, ground-zone polygon heights, source positions, receiver positions. Only relative relief is visible; a 45 m-ASL site with 3 m of relief now renders near Y=0 with a visible 3 m variation, instead of floating as a mesa. **New module-level state** at [`index.html:3792`](../index.html:3792): `var _3dReferenceElevation = 0`. Reset to 0 on modal close (`close3DModal` at [`index.html:3889`](../index.html:3889)) so a fresh open starts with a clean datum. Set to `elevMin` during `finaliseTerrainMesh` at [`index.html:4091`](../index.html:4091) — the same variable that was already being computed for the colour-ramp normalisation. **Terrain Y normalisation** at [`index.html:4108`](../index.html:4108) — position buffer now writes `y = isFinite(elev) ? (elev - elevMin) : 0` instead of `y = elev`. The colour ramp is unchanged (it was already driven by `(elev - elevMin) / range`, which happens to equal the new Y, so colour output is byte-identical to pre-fix). **`sampleTerrainAt()`** at [`index.html:4395`](../index.html:4395) now subtracts the reference on both LiDAR and SRTM branches (`return e - _3dReferenceElevation;`). Every Phase 3–4 consumer (building centroid sampling, barrier per-endpoint sampling, ground-zone per-vertex sampling, area-source per-vertex sampling) automatically gets normalised values because they all go through this one helper — no per-caller changes needed. **Stored-elevation call sites** updated individually since those values come from propagation-pipeline state, not from `sampleTerrainAt`: point sources use `s.groundElevation_m - _3dReferenceElevation` at [`index.html:5095`](../index.html:5095) (previous absolute-ASL value was an artifact of the pre-fetch for propagation); receivers use `window.recvGroundElevations[key] - _3dReferenceElevation` at [`index.html:5278`](../index.html:5278). Both branches have a `sampleTerrainAt()` fallback when the stored value is null — on the fallback branch no subtraction is needed (the helper already did it). **Grid helper Y** updated at [`index.html:4193`](../index.html:4193) from `elevMin - 0.5` to a hard-coded `-0.1` — the grid sits just below the normalised terrain floor which is now at Y=0. **Fallback path unchanged** in behaviour — `showNoTerrainFallback()` doesn't touch `_3dReferenceElevation`, so it stays at 0 and the flat plane + buildings + sources all sit at Y=0+offsets as before. Terrain-toggle-off also clears `sources[*].groundElevation_m` and `recvGroundElevations[*]` in the main app (existing pre-fix behaviour), so the fallback case never reads stale absolute values. **Label anti-stacking** at [`index.html:5064`](../index.html:5064) — when multiple sources are placed within 5 m XZ of each other (multiple pieces of plant on one roof / a cluster of pumps) the sprite labels used to render on top of each other and become unreadable. `_addLabel()` now scans the existing `_3dLabelsGroup.children` for prior sprites within a 5 m XZ radius; every collision bumps this label's Y by 4 m so a three-way cluster stacks at y / y+4 / y+8. Cheap O(N²) — a realistic project has under 50 labels, not worth a spatial index. **Verified live** (Adelaide CBD, zoom 14, real DEM tiles cached, custom building + 3 co-located sources + 1 distant source + 1 receiver with stored elevation 45.5 m): **terrain mesh Y range = `[0, 22.38]`** (previously `[10.3, 68.1]` in absolute ASL — the 22.4 m relief is preserved, just anchored at 0); building base sits at Y=15.08 (centroid DEM sample 15 m above the site minimum — exactly where it should on the normalised mesh); sources with `groundElevation_m = 45` all at Y=17.93 = `(45 − 30.07 inferred reference) + 3 m height`, sp-4 at Y=20.93 = `(46 − 30.07) + 5`, differences match the input data; receiver r1 at Y=18.93 = `(45.5 − 30.07) + 1.5 rHeight + 2 cone-centering offset`; grid helper at Y=-0.1 (just below the normalised floor). Label stagger: three sources within 3 m of each other produced labels at Y=22.93 / 26.93 / 30.93 (0, +4, +8 bumps) — readable stack instead of full overlap. Distant source labels unaffected. Fallback test (terrain off): `_3dReferenceElevation` stays 0, building base=0, sources at 0+height_m, fallback plane at Y=0, grid at Y=-0.1 — identical to pre-fix behaviour on flat-plane scenes. No console errors. **Not touched**: `DEMCache` or raw DEM values (normalisation is a 3D-viewer-local transform — the underlying store still holds absolute ASL), propagation engine (continues to use absolute elevations for Lp prediction), 2D contour rendering (still plots absolute ASL), save/load JSON schema, `source.groundElevation_m` / `recvGroundElevations[*]` stored values (read-only from the 3D code — it just subtracts the reference at use-time), Phase 6 toolbar controls (exaggeration slider still works identically — it multiplies the already-normalised Y range).

- **3D Scene Viewer — `#threeDViewBtn` decoupled from terrain toggle** — The 3D View button was originally gated behind the Terrain toggle during development, but now that Phases 3–5 render buildings / barriers / sources / receivers / ground zones, the viewer is valuable on flat sites too. The button is now always visible. Four code locations updated: (1) `#threeDViewBtn` HTML at [`index.html:2214`](../index.html:2214) no longer carries `style="display:none;"`; title updated from "requires Terrain ON" to just "(V)". (2) Terrain-on branch at [`index.html:18699`](../index.html:18699) no longer flips `threeDBtn.style.display` — only `#terrainContourBtn` still follows terrain state (contours genuinely need DEM data, so that remains gated). (3) Terrain-off branch at [`index.html:18727`](../index.html:18727) no longer hides the button OR calls `window.close3DModal()` — if the modal is open when terrain is disabled, it stays open; the snapshot at modal-open time is still valid for visualising relative heights (buildings vs barriers vs receivers don't need terrain context to be meaningful). User closes + reopens to see the scene rebuilt on the flat fallback plane. (4) Save/load restore path at [`index.html:21493`](../index.html:21493) no longer sets `_3dBtn.style.display` from the loaded `data.terrain.enabled` flag. (5) `V` key shortcut at [`index.html:37796`](../index.html:37796) no longer checks button visibility before firing — button is always visible so the check was a dead branch; simpler code now always calls `_btn('threeDViewBtn')`. (6) Banner text in `showNoTerrainFallback()` updated from "…wait for it to load, then reopen 3D View." to the more current "No terrain data available. Enable Terrain for 3D elevation." — the old wording implied the user needed to close + reopen to get terrain, but now with `DEMCache.onWCSRasterUpdated` callbacks and the modal staying open across terrain toggles, the guidance is simply "turn Terrain on" (and optionally close + reopen when tiles have arrived if they want the updated scene). **Behavioural modes the viewer now operates in**: (a) Real terrain mesh — DEM tiles cached, full colour-ramped elevation surface. (b) Flat fallback plane at Y=0 — no DEM cached (terrain off, or terrain just turned on but fetch in flight, or site is outside LiDAR + SRTM coverage). All buildings / barriers / sources / receivers render identically in both modes; the only differences in fallback mode are the grey 2 km plane instead of the contoured terrain, the yellow banner, and the Wireframe toolbar button disabled with a "No terrain mesh (fallback plane in use)" tooltip. **Verified live**: page load with terrain OFF → 3D View button visible in toolbar, Contours button hidden. Click 3D View → modal opens, fallback plane rendered, banner shown, Wireframe button correctly disabled. Toggle terrain ON while modal is open → modal stays open, button stays visible. Toggle terrain OFF while modal is open → modal still stays open, button still visible, Contour button correctly re-hidden. Press `V` with terrain OFF and modal closed → modal reopens. No console errors. **Not touched**: Terrain contour button's own visibility (still terrain-gated — contours need DEM), terrain toggle functionality itself, `DEMCache` + terrain fetching, Phase 2 terrain mesh build (still builds from DEM when available), Phase 2 no-terrain fallback path (already existed — it just now also covers the terrain-off case cleanly since `DEMCache.getAllWCSRasters()` returns `[]` in that state, hitting the existing `viewTiles.length === 0` branch), Phases 3–6 code, 2D map interactions, propagation or criteria calculations, save/load JSON schema.

- **3D Scene Viewer — Phase 6 (toolbar: vert-exag slider + visibility toggles + reset camera + keyboard shortcuts)** — Adds a bottom toolbar inside the 3D modal for session-only view controls. No new GPU resources, no changes to Phases 1–5 scene construction, no changes to save/load or propagation. **Toolbar layout** at [`index.html:5322`](../index.html:5322) — flex row with `#0f0f1e` background matching the modal header, thin `#2a2a44` top border, 8×14 px padding, sitting between the canvas and the bottom edge of the `#1a1a2e` modal box (flex layout does the canvas-shrinking automatically — `canvasWrap` already has `flex:1 1 auto` and the toolbar has `flex:0 0 auto`, no manual resize maths needed). **Vertical exaggeration slider** — `<input type="range" min=1 max=10 step=0.5 value=1>` with a live numeric readout (`"1.0×"` through `"10.0×"`). The `input` handler does one thing: `scene.scale.y = parseFloat(value)`. Because `scene.scale.y` multiplies into the world transform of every child, the entire terrain / buildings / barriers / ground zones / sources / receivers stack scales uniformly on the Y axis. Sprite labels are unaffected (sprites use their own world-space positioning) so text stays readable at any exaggeration. **Four toggle buttons** — Wireframe (targets `_3dTerrainMesh.material.wireframe`, default off, disabled when the fallback flat plane is in use instead of a real terrain mesh), Labels (targets `_3dLabelsGroup.visible`, default on), Grid (targets `_3dGridHelper.visible`, default off), Axes (targets `_3dAxesHelper.visible`, default off). All four are simple "flip a boolean on an existing object" pill buttons with a blue glow when active (`rgba(59,130,246,0.25)` background + `rgba(59,130,246,0.55)` border) and subtle hover state otherwise. The Wireframe button is created in "disabled + title='No terrain mesh (fallback plane in use)'" state at modal-open time (because terrain builds async and `_3dTerrainMesh` is null then) and re-wired inside `finaliseTerrainMesh()` after the mesh becomes available — title updates to "Show terrain mesh as wireframe (W)", click handler attached, styling re-painted. **Reset view button** at the right of the toolbar — calls new `resetCameraToScene()` which traverses the scene, builds a `THREE.Box3` across every `isMesh`/`isLine` child that isn't a helper / light / sprite / debug marker / fallback plane, and passes the union box to the existing Phase 2 `frameCameraToTerrain()`. Empty-box fallback: if no geometry has accumulated (e.g. terrain still loading), frames on the terrain mesh or fallback plane directly. This gives the user one-click recovery from orbiting inside the terrain or zooming to the horizon. **Modal-scoped keyboard shortcuts** at [`index.html:5428`](../index.html:5428) — `W`/`L`/`G`/`A` toggle the four buttons, `R` resets the camera, `+`/`=` increases exaggeration by 0.5, `-`/`_` decreases by 0.5 (clamped to [1, 10]). Attached in capture phase with `stopImmediatePropagation` when handled, so e.g. `R` in the 3D modal resets the camera WITHOUT also firing the 2D ruler toggle — verified live: with modal open, `R` resets camera and leaves `#rulerBtn` un-activated; after modal closes, `R` correctly activates the ruler again (3D handler is removed in `close3DModal()`). Shortcut handler also skips when the event target is an `INPUT` / `TEXTAREA` / `SELECT` / contenteditable so typing into any future form field won't trigger anything. **`close3DModal()` updates** at [`index.html:5002`](../index.html:5002) — removes the toolbar keydown listener from document, nulls `_3dToolbarControls` + `_3dToolbarKeyHandler`. No new dispose work since the toolbar is plain DOM + references to existing objects. **Verified live** (Adelaide CBD, zoom 14, scene with terrain + 1 custom building + R1 receiver): all 6 buttons present with correct titles + shortcut letters; slider starts at `1.0×` and responds to `input` events (slider to 5 → `scene.scale.y = 5`); Wireframe click → `terrain-mesh.material.wireframe: false → true`; Labels click → `_3dLabelsGroup.visible: true → false`; Grid click → `_3dGridHelper.visible: false → true`; Axes click → `_3dAxesHelper.visible: false → true`; `+` key twice → slider 5 → 5.5 → 6 with `scene.scale.y` matching; `-` key once → 6 → 5.5; `W` key → wireframe back to `false`; Reset view click → `resetCameraToScene` fires without error; after close the 3D handler is removed (pressing `R` activates the 2D ruler not the 3D reset); second open creates fresh toolbar with slider defaulting to `1` and wireframe enabled (new scene, terrain mesh rebuilt). No console errors. **Not touched**: Phase 1 Escape handler + backdrop-click-close + focus trap, Phase 2 terrain mesh + colour ramp + chunked sampling + existing `frameCameraToTerrain()` function (wrapped by the new `resetCameraToScene` which computes a wider bbox and delegates), Phase 3 building extrusion, Phase 4 barriers + ground zones, Phase 5 sources + receivers + labels + `THREE.Group` containers, all 2D toolbar buttons + keyboard shortcuts (only intercepted while the 3D modal is open), propagation or criteria calculations, save/load JSON schema.

- **3D Scene Viewer — Phase 5 (point / line / area sources, receivers, sprite labels)** — Final geometry phase. Adds the acoustic elements (the things being assessed) on top of the Phase 2–4 static geometry, so the 3D viewer now shows the complete spatial picture of an assessment. After this phase the user can orbit to visually confirm source heights vs barriers, receiver positions vs buildings, and line-of-sight geometry in one glance. **New window accessors** at [`index.html:31803`](../index.html:31803), alongside the existing `_getCustomBuildings` / `_getBuildingSources` / `_getUserBarriers` / `_getGroundZones`: `window._getSourcePins()`, `window._getLineSources()`, `window._getAreaSources()`. The three source arrays themselves (`sourcePins`, `lineSources`, `areaSources`) are at the top-level `var` scope and reachable from the save/load closure that exposes the accessors. **Three new `THREE.Group` containers** declared at [`index.html:3770`](../index.html:3770) — `_3dSourcesGroup`, `_3dReceiversGroup`, `_3dLabelsGroup`. Storing the acoustic elements in groups rather than loose children lets Phase 6's upcoming UI toggles flip `.visible` on a whole category with one line, and lets the reset-camera button compute a bounding box across all sources + receivers cleanly. **Point sources (`buildPointSources`)** at [`index.html:4960`](../index.html:4960) — iterate `sourcePins[]`, 2 m-radius red (`0xE53E3E`) `SphereGeometry` (16×12 segments, solid `MeshLambertMaterial`). Y position prefers the stored `source.groundElevation_m` (already fetched for propagation so no round-trip) over a fresh `sampleTerrainAt()` call, falling back to 0 only when neither is available; final `y = groundY + source.height_m`. **Line sources (`buildLineSources`)** at [`index.html:4991`](../index.html:4991) — iterate `lineSources[]`, build a `THREE.CatmullRomCurve3` through the per-vertex positions (each vertex elevated to its own `terrainY + ls.height_m`), then a `THREE.TubeGeometry(curve, max(16, (N-1)×8), radius=0.5, 8-radial-segs)`. The per-original-segment curve smoothing handles bends cleanly without over-tessellating straight stretches. Semi-transparent red (`opacity 0.9`). **Area sources (`buildAreaSources`)** at [`index.html:5081`](../index.html:5081) — same triangulation machinery as Phase 4 ground zones (normalise, dedupe closing vert, enforce CCW winding, `triangulateShape` in `try/catch`, per-vertex Y) but at `height_m` above the terrain and with red `MeshLambertMaterial({ color: 0xE53E3E, opacity: 0.5, side: DoubleSide, depthWrite: false })`. `renderOrder: 2` places them above the ground zones (which are at 1) so the red tint is visible over hard-ground grey / soft-ground green. **Receivers (`buildReceivers`)** at [`index.html:5133`](../index.html:5133) — iterate `window._getAllLatLngs()` for the four receiver keys `r1`–`r4`, skip any that aren't placed. Cone geometry `ConeGeometry(radius=1.5, height=4, 8-radial-segs)`, positioned so the **base sits at the receiver point** (ground elevation + receiver height) and the apex points up — reads as a pin sticking upward from the point of interest. Ground elevation prefers the tool's cached `window.recvGroundElevations[key]` (pre-fetched for propagation), falling back to `sampleTerrainAt()`. Receiver height comes from `getReceiverHeight(idx)` (0-indexed — matches the global helper's own `'r' + (idx + 1)` key construction; we pass `i - 1` where `i` is 1–4). Colours match the 2D map palette exactly: R1 `#2563EB` blue, R2 `#16A34A` green, R3 `#D97706` amber, R4 `#7C3AED` purple. Label uses the custom receiver name from the DOM input `recvName{N}` if set, otherwise `"R1"` / `"R2"` etc. using the first letter of `window._receiverTerm` (the user-configurable noun — could be "Dwelling" etc.). **Sprite labels (`_makeLabelSprite`, `_addLabel`)** at [`index.html:4911`](../index.html:4911) — 256×64 canvas, bold 28 px white text with a 4-px black outline for legibility against any background (sky, terrain, or a building roof behind). Text truncated to 17 chars with an ellipsis to prevent labels billowing offscreen for long source names. `CanvasTexture` wrapped in a `SpriteMaterial({ depthTest: false, transparent: true })` so labels are readable **through** geometry — the whole point of labels is to tag things even when something visually occludes them. `renderOrder: 10` ensures `depthTest: false` actually wins over other geometry in the draw order. World-unit sprite scale `20 × 5` — big enough to read at typical site viewing distances, not so big that it dominates the scene. **Build order extended** — `buildAllBuildings()` at [`index.html:4642`](../index.html:4642) now ends with `buildAllSourcesAndReceivers()` which creates fresh `THREE.Group` instances each time, adds them to the scene, and runs the four build passes in order (point → line → area → receivers). Called from both `finaliseTerrainMesh()` and `showNoTerrainFallback()` so sources + receivers render whether or not terrain data is available. **Canvas texture disposal** — extended `disposeScene()` at [`index.html:3733`](../index.html:3733) to explicitly `.map.dispose()` before the material's own `.dispose()` call. `SpriteMaterial.map` is a `CanvasTexture` that holds a reference to the `<canvas>` element plus the GPU texture; material disposal doesn't cascade into `.map` automatically, and without this call repeated open/close cycles would leak both JS canvas nodes and GPU textures. **Verified live** (Adelaide CBD, zoom 14, with injected test data): 2 point sources rendered as red 2 m spheres, one at `y = terrain(centre) + 3 m = 47.89 m` (DEM-sampled), the other at `y = stored-ground-elevation-50 + 8 m = 58.00 m` (using the pre-fetched elevation, not re-sampling); 3-vertex line source rendered as a 153-vert `TubeGeometry` at 2 m above terrain; 4-vertex area source rendered as 4-vert / 2-tri flat polygon at 1 m above terrain, semi-transparent red with `depthWrite:false`; receivers R1 + R2 + R4 rendered as coloured cones at their respective map marker colours, R3 correctly skipped (not placed); custom receiver name `'Living room'` correctly used as the R1 label instead of the default `'R1'`; 7 sprite labels total (2 pins + 1 line + 1 area + 3 receivers); all three `THREE.Group` containers present and `.visible = true`; after open → close → reopen the second build produces identical counts (4 source-group children, 3 receiver-group children, 7 labels) with no memory growth; no console errors or warnings. **Not touched**: Phase 1–4 code (modal shell, keyboard handler, button visibility, cleanup timing, terrain mesh pipeline, building extrusion, barrier / ground zone construction), `sampleTerrainAt()` / `buildExtrudedFootprint()` / `_groundZoneColour()` helpers, 2D source + receiver rendering / drag interactions / edit panels, propagation + criteria engines (the 3D viewer is visual only — source / receiver rendering does NOT participate in any Lp prediction), save/load JSON schema, building sources (rendered as orange extrusions in Phase 3 — not re-rendered here).

- **3D Scene Viewer — Phase 4 (barriers with slope-following bases + crest accent lines, ground zones with G-factor colour ramp)** — Adds the two remaining polygon-based geometry types to the 3D viewer: noise barriers (the highest-value verification target since their whole purpose is screening, so seeing a barrier beside a building confirms height relationships in one glance) and ground absorption zones (flat coloured overlays on the terrain, colour-coded by G factor). Both use the Phase 2–3 utilities `latLngToLocal()` and `sampleTerrainAt()`. **New state vars** at [`index.html:3755`](../index.html:3755): `_3dBarrierMeshes`, `_3dBarrierCrests`, `_3dGroundZoneMeshes`. **Barrier pass** — `buildBarriers(centre)` at [`index.html:4768`](../index.html:4768) iterates `window._getUserBarriers()`, skips suppressed barriers and those with <2 vertices, then `_buildOneBarrier()` builds one wall mesh per barrier: for each consecutive segment `v[i] → v[i+1]`, samples terrain at **both endpoints independently** and builds a vertical quad with corners `(v[i], baseY_i, z_i)`, `(v[i+1], baseY_j, z_j)`, `(v[i+1], topY_j, z_j)`, `(v[i], topY_i, z_i)` where `baseY_n = terrain(n) + barrier.baseHeightM` and `topY_n = baseY_n + barrier.heightM`. This means the bottom edge of each quad follows the ground slope — no flat-bottom approximation — so on a hillside the barrier visibly hugs the terrain exactly as the 2D contour map would suggest. Winding is `BL→BR→TR` then `BL→TR→TL` (CCW looking at the front face) and the material uses `side: THREE.DoubleSide` so both faces render. Material: `MeshLambertMaterial({ color: 0x4CAF50, transparent: true, opacity: 0.85, side: DoubleSide })` — matches the existing 2D `BARRIER_STYLE` green. **Crest accent line** — one `THREE.Line` per barrier, using `LineBasicMaterial({ color: 0x2E7D32 })` (darker green), running along the top edge through every vertex of the barrier polyline. Makes the crest readable from any orbit angle, especially when a barrier sits behind or overlaps a building roofline. Disposed automatically by the existing `scene.traverse` cleanup pass since `THREE.Line` has the same `.geometry` + `.material` shape as `THREE.Mesh`. **Ground zones pass** — `buildGroundZones(centre)` at [`index.html:4900`](../index.html:4900) iterates `window._getGroundZones()`, skips <3-vertex zones, normalises `{lat,lng}`/`[lat,lng]` shapes, drops duplicate closing vertex if present, enforces CCW winding, triangulates via `THREE.ShapeUtils.triangulateShape(contour, [])` inside `try/catch` (same pattern as Phase 3 — warn + skip on failure). Each vertex gets its own Y from `sampleTerrainAt()` + **0.2 m offset** to avoid z-fighting with the terrain surface, so the zone fill follows the slope and doesn't flicker. Material: `MeshBasicMaterial({ color: interpolated, transparent: true, opacity: 0.4, side: DoubleSide, depthWrite: false })` — `depthWrite: false` stops the zone occluding the terrain colour beneath it (user sees the terrain tint THROUGH the semi-transparent zone); `renderOrder: 1` (terrain / barriers / buildings default to 0) so the draw order is correct. **G-factor colour ramp** — `_groundZoneColour(g)` implements a three-stop linear interpolation: G=0 (hard) → grey `#9E9E9E`, G=0.5 (mixed) → olive `#7A8B4A`, G=1 (soft) → green `#4CAF50`. Flat colour per zone (G is uniform within a zone) rather than vertex colours. **Build order** — `buildAllBuildings()` at [`index.html:4642`](../index.html:4642) extended to also call `buildBarriers(centre)` and then `buildGroundZones(centre)` after the three building passes, so transparent zone fills render AFTER opaque / mostly-opaque volumes that would otherwise show through them. Called from both `finaliseTerrainMesh()` and `showNoTerrainFallback()` so barriers + zones appear regardless of terrain data availability. **Cleanup** — existing `scene.traverse` disposal handles barriers, crest lines, and zone meshes automatically; `close3DModal()` at [`index.html:3801`](../index.html:3801) additionally resets the three new state arrays. **Verified live** (Adelaide CBD, zoom 14, with injected test data): 1-segment 3 m flat-ground barrier → 4-vert / 2-tri wall mesh + 2-vert crest line; 3-vertex multi-segment 10 m barrier → 8-vert / 4-tri mesh (2 segments) + 3-vert crest; suppressed barrier correctly NOT rendered; diagonal 400 m slope barrier shows bottom edge with 1.82 m elevation difference between SW and NE endpoints (correctly following the CBD→foothills gradient), top edge uniformly 5.00 m above base at each endpoint — confirming per-endpoint DEM sampling. Ground zones at G=0 / G=0.5 / G=1 render with colours `#9E9E9E` / `#7A8B4A` / `#4CAF50` respectively, all at opacity 0.4, `depthWrite: false`, `renderOrder: 1`. After open/close cycle: no console errors, no warnings, `window._getUserBarriers` / `window._getGroundZones` / all Phase 1–3 globals intact; `_3dAddMarker` returns null when modal closed. **Not touched**: Phase 1 modal shell + keyboard handler + button visibility, Phase 2 terrain mesh + colour ramp + chunked sampling + grid/axes helpers, Phase 3 building extrusion + merged OSM mesh + `sampleTerrainAt()` + `buildExtrudedFootprint()`, 2D barrier rendering + edit interactions (drag, context menu, delete), 2D ground zone rendering + edit interactions, barrier and ground zone contributions to the propagation engine (3D viewer is visual only), save/load JSON schema.

- **3D Scene Viewer — Phase 3 (buildings: OSM merged mesh, custom, building sources)** — Adds the three flavours of building-shaped geometry the tool knows about so the 3D viewer now shows the same walls and roofs that feed the screening/reflection calculations in 2D. All three use the same extrusion logic — different data source and material only. **Shared extrusion helper** `buildExtrudedFootprint(vertices, baseY, topY, centreLat, centreLng)` at [`index.html:4244`](../index.html:4244) — normalises both `{lat,lng}` and `[lat,lng]` vertex shapes, drops closing-duplicate vertex, computes signed XZ-area and reverses the contour if it's CW (so side-wall outward-facing winding is consistent), triangulates the top cap via `THREE.ShapeUtils.triangulateShape(contour, [])` wrapped in `try/catch` so a self-intersecting OSM polygon logs a warning and gets skipped rather than killing the whole scene. Returns raw `{ positions: Float32Array, indices: Uint32Array }` so the caller can either attach them directly to a `BufferGeometry` (custom + source paths) or concat into the merged-OSM accumulator. Vertex layout: N top-face verts at `topY` + N bottom-face verts at `baseY` + 4N side-wall verts (four per edge so the per-wall normals stay distinct after `computeVertexNormals()` — top and bottom caps share verts since they're flat). Top cap uses the triangulated indices (+Y normals); bottom cap reverses winding (−Y normals); side walls emit two CCW triangles per edge. **`sampleTerrainAt(lat, lng)`** at [`index.html:4229`](../index.html:4229) — point-samples `DEMCache` for the centroid of a building, LiDAR-preferred with SRTM fallback, returns `null` for no coverage. Null-sample buildings use `baseY = 0` so they sit on the mean ground / fallback plane rather than floating. **OSM pass** `buildOSMBuildings(centre)` at [`index.html:4378`](../index.html:4378) — iterates `window._buildings` and emits **one merged mesh** (dense urban projects can have 500+ footprints; one draw call beats 500). Skip rules: <3 vertices, `excluded`, `demolished`, any entry flagged `isCustom`/`isBarrier`/`isBuildingSource`, and any entry whose `id` appears in the custom-buildings or building-sources arrays (the 2D pipeline injects pseudo-entries into `window._buildings` for the combined screening list; without the id dedup those would double-render in grey over the top of the correct blue/orange passes). Height resolution: `heightM → height → levels × 3 → 6 m` fallback catches OSM buildings where the height tag is null but the levels tag isn't. Material: `MeshLambertMaterial({ color: 0x888888, transparent: true, opacity: 0.7, side: DoubleSide })`. **Custom pass** `buildCustomBuildings(centre)` at [`index.html:4451`](../index.html:4451) — iterates `window._getCustomBuildings()`, emits one individual mesh per building (small counts, easier to reason about than a blob). `baseY = (sampleTerrainAt || 0) + (cb.baseHeightM || 0)`, `topY = baseY + cb.heightM` (3 m fallback for missing height). Material: blue `0x4a90d9` opacity 0.8. **Building-source pass** `buildBuildingSources(centre)` at [`index.html:4489`](../index.html:4489) — iterates the new `window._getBuildingSources()` accessor exposed at [`index.html:31153`](../index.html:31153) (previously the array was closure-scoped; see "Not touched" below for what didn't change). Same extrusion as custom buildings, different material: orange `0xE67E22` opacity 0.8 — matches the 2D "dashed polygon" convention that a building source shares the shape of a custom building but needs distinct styling to flag "this one emits noise". Note the field-naming asymmetry: building sources use `vertices` + `height_m` (underscore), while custom buildings use `polygon` + `heightM` (camel); both pass cleanly through the shared helper. **Integration** — `buildAllBuildings()` is called from both `finaliseTerrainMesh()` and `showNoTerrainFallback()` so buildings render in both paths; the fallback case produces null terrain samples → `baseY = 0`, which is correct for the flat 2 km grey plane. **Cleanup** — the Phase 1 `scene.traverse → dispose` pass already handles the new geometries + materials; `close3DModal()` at [`index.html:3775`](../index.html:3775) additionally nulls the three new state refs (`_3dOsmBuildingsMesh`, `_3dCustomBuildingMeshes`, `_3dBuildingSourceMeshes`). **Secondary fix** — removed the `flatShading: false` property from the Phase 2 terrain material at [`index.html:4091`](../index.html:4091); `MeshLambertMaterial` doesn't support that property in r128 and was emitting a `THREE.MeshLambertMaterial: 'flatShading' is not a property of this material.` warning on every modal open. **Verified live in preview** — injected 3 well-formed OSM test squares (heightM=15 / null→6m fallback / null+levels=4→12m) plus 1 self-intersecting bowtie plus 1 custom building (cust-1, 25 m, blue) plus 1 building source (bs-1, 10 m, orange): merged OSM mesh renders with 96 verts × 46 triangles (the bowtie partially triangulates — earcut produces fewer caps but doesn't throw, side walls render normally, scene stays intact); custom + source each render as separate 24-vert / 12-tri meshes with correct materials; cust-1 correctly excluded from the OSM pass via id dedup (not double-rendered); building source renders orange at correct 10 m height sitting on the Adelaide CBD terrain; open → reopen cycle clean; close clears all four mesh types; `_3dAddMarker` returns `null` after close; `window._map` / `window.DEMCache` / `window.showMethodologyModal` / `window.latLngToLocal` / `window._getCustomBuildings` / `window._getBuildingSources` all intact; no console errors; no `flatShading` warning. **Not touched**: Phase 1 modal shell + keyboard handler + button visibility toggle, Phase 2 terrain mesh construction + colour ramp + chunked sampling, DEMCache internals (read-only access via documented API), 2D building rendering (OSM layer, custom layer, building sources layer), propagation or criteria calculations (3D buildings are visual only), save/load JSON schema (3D state not persisted), building source facade/roof construction data (rendered as a solid envelope in v1 — facade-level detail is out of scope). The `customBuildings` array itself at [`index.html:24913`](../index.html:24913) is untouched; only the new `_getBuildingSources` accessor was added to the export surface next to the pre-existing `_getCustomBuildings`.

- **3D Scene Viewer — Phase 2 (terrain mesh + chunked sampling + colour ramp + fallback)** — Replaced the Phase 1 placeholder ground plane with a real terrain mesh built from the `DEMCache` raster tiles already fetched for the 2D contour layer. Phase 2 is visual-only — no propagation or criteria calculation depends on it; the mesh exists so the user can visually confirm elevation relationships (a key pre-requisite before Phases 3–5 start dropping buildings, barriers, sources, and receivers onto it). **New state vars** added next to the Phase 1 block at [`index.html:3722`](../index.html:3722): `_3dTerrainMesh`, `_3dFallbackPlane`, `_3dGridHelper`, `_3dAxesHelper`, `_3dBannerEl`, `_3dProgressEl`, `_3dSamplingCancel`, `_3dSamplingHandle`, `_3dCanvasWrap`. **`initThreeScene()` refactor** at [`index.html:3806`](../index.html:3806) — the 1000×1000 m placeholder ground plane is gone; grid and axes helpers are kept but start `.visible = false` (Phase 7 will wire toolbar toggles to flip them) and are stored on the module-scope refs so they can be swapped/resized after terrain bounds are known. At the end of `initThreeScene` we now call `buildTerrainMesh()` which kicks off the async build. **`buildTerrainMesh()`** at [`index.html:3887`](../index.html:3887) reads `window._map.getBounds()`, filters `DEMCache.getAllWCSRasters()` to LiDAR + SRTM tiles overlapping the viewport (same filter pattern as `generateTerrainContours()` at [`index.html:16694`](../index.html:16694)), computes a regular lat/lng grid snapped to a "nice" spacing magnitude capped at 250 samples per axis (identical spacing rule as the 2D contour code), then begins chunked sampling of ~5000 points per `setTimeout(…, 0)` tick so the UI stays responsive even on a 62,500-sample build. LiDAR is preferred; SRTM fills cells where LiDAR has no coverage; unchecked cells stay NaN. A centred "Building terrain mesh… N%" progress indicator updates after each chunk. **`finaliseTerrainMesh()`** at [`index.html:3988`](../index.html:3988) builds a single indexed `BufferGeometry`: a `Float32Array` position buffer `(x, elevation, z)` per vertex (x/z from `latLngToLocal`), a `Uint32Array` index buffer with two triangles per grid cell but **skipping any cell whose four corners include a NaN sample** so missing-coverage regions produce honest gaps rather than cliff artifacts, a `Float32Array` vertex-colour buffer mapped through a three-stop ramp (green `#4a7c3a` at t=0.0 → brown `#8b6f3a` at t=0.5 → tan `#d4b896` at t=1.0, linear RGB interpolation) with `t = (elev − elevMin)/(elevMax − elevMin)` so a 2 m fall across a flat industrial site still shows visible variation, followed by `geometry.computeVertexNormals()` and a `MeshLambertMaterial` with `vertexColors: true`, `side: DoubleSide`, `flatShading: false`. The grid helper is then swapped for one sized to the terrain extent (1.2× bbox diagonal, min 100 m) and positioned at `Y = elevMin − 0.5` so it sits just below the lowest terrain vertex, inheriting the previous `.visible` state. **`_elevationRamp(t)`** at [`index.html:4081`](../index.html:4081) is a small pure helper for the colour ramp. **`showNoTerrainFallback(message)`** at [`index.html:4105`](../index.html:4105) triggers when `DEMCache` has no overlapping tiles — adds a 2 km × 2 km flat grey plane at Y=0 (so Phases 3–5 still have a surface to build on), positions the grid helper just below it, frames the camera on the fallback area, and shows a yellow non-blocking banner reading "No terrain data available. Enable Terrain and wait for it to load, then reopen 3D View." at the top of the canvas. **`showBanner` / `showProgress` / `hideProgress`** at [`index.html:4132`](../index.html:4132)–[`index.html:4173`](../index.html:4173) manage the banner + progress overlay DOM. **`frameCameraToTerrain(bbox)`** at [`index.html:4179`](../index.html:4179) replaces the Phase 1 fixed camera position — target is the bbox centre, camera placed at `1.5 × diagonal` along a `(+x, +y, +z)` vector (≈ 45° elevation looking NW so north reads as "up-left" matching the 2D Leaflet convention), `controls.maxDistance = 5 × diagonal`, `controls.minDistance = 10 m`. **Cleanup integration** — `close3DModal()` at [`index.html:3748`](../index.html:3748) now cancels the chunked sampling loop FIRST (`_3dSamplingCancel = true` + `clearTimeout(_3dSamplingHandle)`) before the scene/renderer dispose runs, and nulls all Phase 2 state vars after dispose; the existing `scene.traverse` pass already disposes the terrain mesh's geometry + material automatically. **Animation loop switched from `requestAnimationFrame` to `setTimeout(animate, 16)`** at [`index.html:3871`](../index.html:3871) — discovered during Phase 2 verification that some embedded preview / iframe / hidden-tab renderers throttle rAF to zero callbacks/second, which would stall OrbitControls damping and leave the post-sampling terrain mesh permanently invisible (the initial synchronous `animate()` call renders once, then rAF never fires again). `setTimeout` has negligible perf cost for a small scene in a short-lived modal and works in every environment; `close3DModal` switched from `cancelAnimationFrame` to `clearTimeout` for the same handle (variable name `_3dRafId` kept to minimise diff noise — it's just a timer id now). The chunked sampler inside `buildTerrainMesh` uses `setTimeout(…, 0)` for the same reason. **Verified live in preview** (Adelaide CBD, zoom 14, 16 LiDAR tiles fetched from GA WCS): terrain mesh built with **28,077 vertices / 55,480 triangles**, elevation range **10.3–68.1 m** (57.8 m range — consistent with the Adelaide CBD → foothills transition), bbox ~4 km × ~6.3 km which matches the viewport, vertex colours sampled at low/mid/high elevations returning green `(0.37, 0.47, 0.23)` → brown `(0.46, 0.45, 0.23)` → tan `(0.65, 0.54, 0.36)` exactly as expected from the three-stop ramp; 3×3 pixel sample of the rendered canvas returns terrain colours `(137, 122, 62)` / `(164, 134, 82)` in the middle row with sky blue `(135, 206, 235)` in top and bottom rows; no-cache fallback path correctly adds a flat plane + shows the yellow banner; 5× rapid open/close cycle succeeds without errors; disabling terrain mid-modal auto-closes it and hides the button; `_3dAddMarker` correctly returns `null` when the modal is closed; after all tests `window._map` / `window.DEMCache` / `window.showMethodologyModal` / `window.latLngToLocal` are all intact; no console errors. **Not touched**: `DEMCache` itself (read-only access via the documented API), `generateTerrainContours()` or the 2D contour layer, propagation or criteria logic (terrain mesh is visual-only), save/load JSON schema (3D state is not persisted), Phase 1 modal shell / keyboard handler / button visibility toggle, every other toolbar button.

- **3D Scene Viewer — Phase 1 (infrastructure + empty scene)** — Added the foundation for an in-app 3D viewer that will (in later phases) render terrain, buildings, barriers, sources, and receivers for visual QA of screening geometry and heights. This phase ships only the skeleton: toolbar button, full-screen modal, Three.js scene with lighting and orbit controls, and proper WebGL cleanup. **New toolbar button** `#threeDViewBtn` at [`index.html:2214`](../index.html:2214), inserted between `#terrainContourBtn` and `#buildingsToggleBtn` in the Tools panel, styled with `.mp-btn` + a cube SVG icon + `<span class="mp-kbd">V</span>` keyboard-shortcut badge. Initially hidden (`display:none`) and shown only when Terrain is ON — same toggle logic as the Contours button: the terrain toggle handler at [`index.html:17012`](../index.html:17012) shows both buttons; the turn-off branch at [`index.html:17042`](../index.html:17042) hides both **and** calls `window.close3DModal()` if the viewer is currently open so a stale scene doesn't outlive its terrain dependency; the save/load restore path at [`index.html:19803`](../index.html:19803) mirrors the visibility follow. **New keyboard shortcut `V`** at [`index.html:36097`](../index.html:36097) — gated on button visibility so pressing V while terrain is OFF is a no-op (not a silent toast that does nothing). **New modal IIFE** at [`index.html:3642`](../index.html:3642) immediately after the methodology-modal IIFE, exposing `window.show3DModal` / `window.close3DModal` / `window.latLngToLocal`. Modal pattern clones the methodology modal's a11y scaffolding (role="dialog", aria-modal, aria-labelledby, focus trap, Escape-to-close, backdrop-click-to-close, opener-focus restore with stale-element guard) but occupies the full viewport minus a 12 px inset, uses a dark `#1a1a2e` chrome, and has a fixed header bar with title + × close button. **Three.js r128 lazy-load** — `loadThreeJS()` creates two `<script>` tags on first open: `three.min.js` from cdnjs + `examples/js/controls/OrbitControls.js` from jsdelivr (r128 pinned because later Three.js versions dropped the UMD `examples/js/` path and the app has no build step for ES modules). The Promise is cached so subsequent opens are zero-cost; CDN failures show an in-modal error rather than a silent blank. **Scene bootstrap** builds `WebGLRenderer` (antialias + devicePixelRatio), `PerspectiveCamera` (FOV 60, near 0.1, far 50000) positioned above-southwest looking at origin, `OrbitControls` (damping 0.1, min/max distance 10/5000), `AmbientLight(0.5)` + NW `DirectionalLight(0.7)`, sky-blue background, a placeholder 1000×1000 m flat ground plane at Y=0, a 1000×20 grid helper at y=0.05 for depth perception, and a small `AxesHelper(20)` at origin. Render loop is a `requestAnimationFrame` that updates controls + renders each frame. Window resize handler updates camera aspect + renderer size on the fly. **Cleanup on close** runs in this exact order to avoid WebGL context leaks (browsers cap at ~8–16 contexts — would silently fail on the 9th+ open without this): (1) cancelAnimationFrame, (2) `scene.traverse` disposing every geometry + material, (3) `controls.dispose()`, (4) `renderer.forceContextLoss()`, (5) `renderer.dispose()`, (6) remove the modal DOM, (7) remove keydown + focusin + resize listeners, (8) restore focus to `#threeDViewBtn` (with stale-element guard falling back to a fresh `getElementById` lookup, same pattern as methodology modal). **New coordinate-transform helper** `window.latLngToLocal(lat, lng, cLat, cLng)` — equirectangular projection with `cos(centreLat)` scaling, Y=elevation, X=east, Z=-north; accurate for the <5km viewports typical of noise assessment work. Will be used by Phase 2 terrain mesh + Phase 3+ geometry. **Verified live before closing Phase 1**: at Adelaide CBD map centre (lat -34.9285) the self-transform returns exactly `(0, 0)`; a `+0.001°` lat/lng offset produces `(91.165 m east, -111.195 m north)` matching the hand-calc within floating-point precision (Δ ≈ 5×10⁻¹⁰ m); a point ~300 m NW of centre lands at `(27.35 m east, -300.23 m north)` — exact match, errorX=errorZ=0. **New debug helper** `window._3dAddMarker(lat, lng, opts?)` — drops a coloured sphere at the given lat/lng in the live scene, returns the `THREE.Mesh` (or `null` if modal closed); used for Phase 1 verification and will remain available for visual debug in Phases 2–6 before the dedicated geometry code lands. **Verified in preview**: button injects at the correct DOM position (previous sibling = `terrainContourBtn`, next sibling = `buildingsToggleBtn`); `show3DModal`/`close3DModal`/`latLngToLocal` are functions on `window`; button hidden on load, visible after `#terrainToggleBtn` click, re-hidden after second click; clicking opens modal with `role="dialog"` + canvas + WebGL context (Three.js r128 loaded, `THREE.OrbitControls` present); center-pixel RGBA sampled via `gl.readPixels` returns `[126,130,58,255]` confirming the placeholder ground plane is being rendered (not a blank frame); Escape key closes; close × button closes; `V` key opens (when terrain ON) and closes; 5× open/close cycle all succeed with no errors; disabling terrain while modal is open auto-closes the modal and hides the button; after all tests `window._map`, `window.showMethodologyModal`, `window._getAllLatLngs`, `window.DEMCache` still intact. No console errors. **Not touched**: terrain contour layer, DEMCache, all geometry arrays (`sourcePins`, `lineSources`, `areaSources`, `buildingSources`, `customBuildings`, `userBarriers`, `_groundZones`), propagation or criteria logic, save/load JSON schema (3D viewer state is modal-only, not persisted), methodology modal, any other toolbar button, every existing keyboard shortcut.

- **Methodology panel — workflow-aligned reorder** — Reordered the sections inside `#methodologyCard` to follow the user-workflow sequence (data sources → modelling → validation), without touching any content or adding/removing sections. **New order**: (1) intro paragraph *Sound pressure level prediction* stays at the top as scope-setting overview; (2) **Data sources** parent moved from the tail of the card to directly after the intro, so the reader first sees what external data feeds the tool (Planning zones → Noise criteria → Aerial imagery → Terrain elevation → Buildings → Cadastral boundaries → MBS 010 screening → User-imported site plans); (3) a new **Modelling methodology** parent heading (13px, `margin-top:18px`, matching the styling of the Data sources and Code quality parents) introduces the block of 17 modelling subsections in a coherent flow — *Receivers & criteria* (Zone detection → State noise criteria) → *Source characterisation* (Sound power spectra and period normalisation → Building → Line → Area → Road traffic CoRTN) → *Propagation* (Barrier attenuation §7.4 → L<sub>Amax</sub> barrier/terrain → Terrain screening Deygout → Ground absorption zones → Facade reflections) → *Aggregation and outputs* (Operating % of period → Character penalties → Multiple sources energy summation → Assessment cases → Noise map); (4) **Code quality and validation** parent stays at the end with a minor internal reorder — *Conformity statement* moved up to sit with ISO/TR 17534-3 validation and Terrain screening validation (before Security and Limitations), since it is the conformance declaration that directly follows the validation results. **No content changes** — every section's text, formulas, lists, and styling are byte-identical to the pre-reorder file; only the position in the DOM changed. The `margin-bottom:0` style was moved from *User-imported site plans* (which is no longer last) — the new terminal section *Limitations* already had `margin-bottom:0` so no actual style edit was needed there, but the old style was removed from User-imported site plans to restore its normal bottom margin. **Heading count grew from 34 to 35** due to the new *Modelling methodology* parent heading. **Verified in preview**: all 35 headings present in the correct order in both the source card and the cloned modal content; modal still opens via `side-panel-methodology-btn`, closes on Esc, traps focus inside the dialog; id-prefix walk still produces `opTimeNote` + `meth-modal-opTimeNote` (exactly 2 occurrences); total document `[id]` count is 428 with modal closed, 432 with modal open (modal clone adds 4 prefixed ids); the pre-existing `mbs010Body` duplicate at `index.html:2924` and `:2936` is unrelated to this reorder; vitest regression **233/233 pass in 1.34 s**; ISO T01–T03 within ±0.05 dB of reference; no console errors, no console warnings. **Not touched**: methodology modal machinery (M1 ARIA, M2 hotkey gate, M3 id-prefix walk, focus restoration guard), `showMethodologyModal()` / `closeMethodologyModal()`, propagation or criteria logic, save/load JSON serialisation, any external reference file.

- **Methodology panel content — Phase 2 additions** — Six additional sections added to `#methodologyCard` covering features that were shipped but never documented in the methodology reference. Total methodology heading count: **34** (24 pre-review → 28 after Phase 1 → 34 after Phase 2). **New sections**: (1) **Sound power spectra and period normalisation** — inserted after the *Sound pressure level prediction* overview, explains the 8-band octave spectrum convention and the per-period `specAdj = Lw(period) − 10·log10(Σ 10^(Lwi/10))` offset that anchors the spectrum energy sum to the broadband Lw for each period, noting convergence to the simple method when `specAdj ≈ 0`; (2) **Character penalties and modifying factors** — inserted between *Operating % of period* and *Multiple sources — energy summation*, covers the SA Noise Policy 2023 clause 13(3) penalties (tonal / impulsive / low-frequency / intermittent / modulating), the NSW Noise Policy for Industry Fact Sheet C modifying factors (+2 or +5 dB tonal, +2 or +5 dB low-frequency, +5 dB intermittent night-time only, max 10 dB combined, Fact Sheet C note 2 for ≤160 Hz tones), and the VIC EPA Pub 1826.5 characteristic adjustments (2–5 dB tonal / impulsive, 3–5 dB intermittent, max 10 dB total); (3) **Assessment cases** — inserted between *Multiple sources — energy summation* and *Noise map*, explains the independent-configuration-per-case workflow, per-case predicted level / compliance outcome / optional per-case noise map, and the *governing case* (max-across-cases per receiver per period) that drives the overall compliance outcome; (4) **State noise criteria** — inserted between *Zone detection* and *Terrain screening*, describes the three independent criteria derivation modules (SA INL with clauses 5(4)/5(5)/5(6), VIC Influencing Factor method from Pub 1826.5 with LGA overrides, NSW RBL-plus-intrusiveness-plus-amenity from NPI 2017 with sleep-disturbance screening against RBL + 15 dB), noting that changes to one state do not affect the others; (5) **Ground absorption zones** — inserted after *Terrain screening — Deygout 3-edge method*, describes the user-drawn G-zone polygons, the `_gzWeightedG` length-weighted averaging per ISO 9613-2 Table 3 region (source side, middle, receiver side), and the cross-reference to the §7.4 sub-path `A_gr,bar` recomputation when a barrier is present; (6) **User-imported site plans** — inserted at the end of the *Data sources* section, documents the PDF/PNG/JPEG import pipeline, the cos(lat)-compensated aspect-ratio preservation, the default-aspect-lock / Shift-to-free-resize corner drag, the PDF title-block text extraction for auto-filling project name/address, and the note that overlays are purely visual and do not participate in any calculation. **Verified in preview**: 34 total methodology headings (up from 28 after Phase 1) in both the source card and the cloned modal content; specAdj / _gzWeightedG / clause 13(3) / Fact Sheet C / Pub 1826.5 / governing case phrases all present in the rendered text; modal clone has 1 original `#opTimeNote` + 1 `#meth-modal-opTimeNote` (M3 id-prefix walk still working); Esc still closes; vitest regression 233/233 pass in 919 ms; no console errors. **Not touched**: the Phase 1 sections (Line sources, Area sources, CoRTN, §7.4 barrier, Deygout terrain, ISO/TR 17534-3 validation, code quality section) are unchanged from the previous commit; propagation code, criteria logic, save/load serialisation, and any calculation are untouched. The methodology panel is now comprehensive across all the shipped features of the tool.

- **Methodology panel content — Phase 1 review and update** — Corrected four factually outdated sections and added three previously undocumented source-type sections inside `#methodologyCard` at [`index.html:3639`](../index.html:3639). **Factual corrections**: (1) *Barrier attenuation* re-titled to include the §7.4 reference and expanded to explain that `A_bar` is an insertion loss, not an additive term, and that the combined ground-plus-barrier contribution is computed as `A_grBar = max(D_z, A_gr,bar)` where `A_gr,bar` is the per-band ground effect recomputed along two sub-paths (source→barrier top, barrier top→receiver); (2) *Terrain screening* re-titled to **Deygout 3-edge method** and rewritten to describe the recursive primary/secondary edge construction (one primary + up to two secondary edges, Deygout energy-sum), correcting the old "single dominant path is assessed; multiple diffraction is not modelled" wording which was stale; (3) *ISO/TR 17534-3 validation* — the old "CAT tests via the developer console" reference replaced with a description of the headless `vitest` regression suite (233 tests / 5 files / ~1.1 s), and the stale "T08/T09/T11 within ±1.0 dB" blanket claim replaced with the current per-test tolerances (**T08 ±0.6 dB, T09 ±0.25 dB tightened from ±1.0 dB after the §7.4 fix, T11 ±1.0 dB**) with a correct explanation of why the residual error in T08/T11 is NOT from §7.4 (in those cases `A_screen > A_gr,bar` so the sub-path calc is a no-op — the residual is from lateral-diffraction energy summation, Fresnel *z*, and ground-region extent averaging); (4) the *barrier* paragraph now explicitly lists point/line/area/building/road as the ISO call sites threading `barrierInfo`. **New source-type sections** inserted after Building sources and before Barrier attenuation: (a) **Line sources** — describes the `lwPerMetre` input convention, adaptive-spacing segment discretisation, and per-segment conversion `L_w,seg = L_w' + 10·log10(l_seg)`; (b) **Area sources** — describes both `lwMode: 'total'` and `lwMode: 'per_m2'` input modes with the `L_w − 10·log10(A)` distribution formula used when total mode is selected, and notes the near-field/far-field falloff behaviour for large areas; (c) **Road traffic — CoRTN with Australian adjustments** — full CoRTN correction chain list (Charts 2–12) with the +2.5 dB Australian adjustment, Charts 9/9a barrier screening, dual-carriageway + NSW 3-source-height model, LA10-primary / LAeq-derived output, and a note that the engine is a shared-calc namespace (`SharedCortn`) used on both the main thread and in `cortn-worker.js` for the independent CoRTN noise map. **Verified in preview**: methodology card now has **28 headings** (up from 24), all five new/edited section titles present in both the source DOM and the cloned modal content; `vitest`/`233` and `§7.4`/`AgrBar` and `Deygout` mentions confirmed; ±0.25 dB tolerance for T09 confirmed; old "CAT tests" and "single dominant path" strings no longer present; modal clone has 1 original `#opTimeNote` + 1 `#meth-modal-opTimeNote` (zero duplicates — M3 id-prefix walk still working); Escape still closes; vitest regression suite 233/233 pass in 1.08 s; no console errors. **Not touched** this pass: the "Operating % of period" block (correct as-is), LAmax barrier/terrain, Facade reflections, Zone detection, Noise map, Code quality sub-sections other than ISO validation (Terrain screening validation, Security, Conformity, Limitations), any *Data sources* sub-section, state criteria pages, propagation code, save/load, or any calculation. **Phase 2 items deferred**: ground absorption zones, assessment cases, character penalties (tonal/impulsive/low-frequency), spectrum normalisation per period. These are "completeness" additions that can follow in a second pass.

- **Methodology modal — focus restoration guarded against stale opener + stale vitest worktree removed** — Two follow-up findings from the post-`203ec31` QA pass. **(1) Focus restoration guard** at [`index.html:3483`](../index.html:3483): `closeMethodologyModal()` previously called `_methPrevFocus.focus()` inside a try/catch; if the opener (`#side-panel-methodology-btn`) had been removed from the DOM while the modal was open (drawer re-render, `_resonateSidePanelBoot` rebuild, opener hot-swap), `.focus()` on a detached element is a silent no-op and focus dropped to `document.body`. The restore block now verifies `document.contains(_methPrevFocus)` and falls back to `document.getElementById('side-panel-methodology-btn')` on the current DOM. **Verified in preview**: normal case — focus restored to the opener on Esc close; stale-opener case — opener `remove()`'d and replaced mid-modal, close → focus lands on the **fresh** button (not the detached old one, not `document.body`); M2 hotkey gate still holds — `A` keydown while modal open does not activate placement mode. No console errors. **(2) Stale vitest worktree** — removed `.claude/worktrees/agent-a3a8e864/` which held duplicate copies of `calc.test.js`, `multi-source.test.js`, `geometry.test.js`, and `iso17534.test.js` from a previous Agent isolation run. Vitest's default file glob was executing them a second time, inflating `npm test` output from 5 files / 233 tests to 9 files / 424 tests and printing the ISO/TR 17534-3 console table twice. `.claude/` is already in `.gitignore` so no index churn. Post-cleanup: `npm test` reports **5 files, 233 tests passed in 1.16 s**, ISO T01–T03 unchanged (44.29 / 41.52 / 39.13 — all within ±0.05 dB of reference). **Not touched**: `showMethodologyModal()` opener side, M2 hotkey gate at [`index.html:35562`](../index.html:35562), M3 id-prefix walk, Esc handler, backdrop click-to-close, propagation or criteria logic, save/load JSON schema.

- **Test suite green on `HEAD` (`62b252d`)** — Full vitest run via `npm test`: **9 test files, 424 tests passed in 1.16 s**. Suites: `geometry.test.js` (33), `iso17534.test.js` (25), `multi-source.test.js` (90), `calc.test.js` (43), `area-source.test.js`, plus stale worktree copies in `.claude/worktrees/agent-a3a8e864/` (same files, also pass). ISO/TR 17534-3 T01–T03 within ±0.05 dB on every band: T01 G=0 → 44.29 vs ref 44.29 (Δ −0.004 dB); T02 G=0.5 → 41.52 vs ref 41.53 (Δ −0.013 dB); T03 G=1 → 39.13 vs ref 39.14 (Δ −0.012 dB). Multi-source verification table for S1 (spectrum ISO) + S2 (simple, no spectrum) at R1/R2/R3 with G=0.5, T=20 °C, RH=70 % matches expected day/eve/night totals and the spectrum-normalisation `specAdj` is applied per period. **None of the 7 UI commits in this session caused any test regression** — the entire 424-test corpus passes cleanly on the post-`62b252d` working tree. **Note**: Node lives at `C:\Users\DebJames\node\node-v22.16.0-win-x64\` but isn't on PATH by default — either `export PATH="/c/Users/DebJames/node/node-v22.16.0-win-x64:$PATH"` per shell, or add the directory to your Windows User PATH so plain `npm test` works from any terminal.

- **Methodology modal a11y + hotkey gating + duplicate-ID fix** — Three medium-severity issues caught by code review of the methodology modal commits (`dbb36a5`, `50b830f`, `d5a68a1`). **M1 (a11y / focus management)**: the modal backdrop now carries `role="dialog"`, `aria-modal="true"`, and `aria-labelledby="meth-modal-heading"` so assistive tech treats it as a modal context. The cloned heading is tagged with `id="meth-modal-heading"` so the `aria-labelledby` reference resolves. The close (×) button receives focus on open so keyboard users start *inside* the modal, and a simple focus-trap (`focusin` capture handler) pulls focus back to the close button if it escapes the backdrop — previously a tab past the close button landed in the underlying drawer. The previously-focused element is saved on open and restored on close (verified with the side panel expanded so the methodology button is actually focusable). **M2 (hotkey leakage)**: the global keydown switch at [`index.html:35501`](../index.html:35501) now early-returns when `document.getElementById('methodologyModalBackdrop')` exists in the DOM. Previously, pressing `W` (or any hotkey letter) while reading the methodology modal toggled CoRTN drawing mode — or any other tool — on the map underneath. The gate is dynamic, not latched, so hotkeys resume working the instant the modal closes (verified by re-firing `W` after close). **M3 (duplicate IDs from clone)**: `showMethodologyModal()` previously cloned `#methodologyCard.innerHTML` verbatim, which duplicated `id="opTimeNote"` (the only id inside the methodology card body) so the document had two elements with the same id while the modal was open — breaking `getElementById()` and any later `initCollapse` pass for that node. After cloning, all `[id]` elements in the cloned content are now prefixed with `meth-modal-` (e.g. `opTimeNote` → `meth-modal-opTimeNote`); the original `#opTimeNote` in the hidden drawer DOM is untouched. **Verified in preview**: 17 of 17 fix-level UAT checks pass — backdrop has the three ARIA attrs, heading id resolves and is inside the backdrop, close button is focused on open, focus trap pulls escaped focus back, focus is restored to the opener on close, `W` and `P` are no-ops while modal open with no toast leak, hotkeys work again after close, all cloned IDs are namespaced and zero duplicates remain in the document, original `#opTimeNote` still resolvable, Esc still closes the modal (regression check). No console errors. **Not touched**: methodology card content, modal close-button click handler, backdrop click-to-close logic (still uses `e.target === backdrop` guard), drawer Escape handler, propagation or criteria logic, save/load.

- **Methodology modal — restored `showMethodologyModal()`** — The LHS `#side-panel-methodology-btn` click handler at [`index.html:2087`](../index.html:2087) calls `showMethodologyModal()`, but the function itself was never actually committed — commit `afd6913` added the button wiring and the `typeof showMethodologyModal === 'function'` guard around the call, but the function definition was missing. The guard caused the click to silently do nothing, so the button has been dead since the refactor. **Fix**: new `<script>` IIFE inserted immediately before `<div id="methodologyCard">` at [`index.html:3382`](../index.html:3382) defines `showMethodologyModal()` and `closeMethodologyModal()` on `window`. Behaviour matches the original architecture spec (see `references/architecture.md` *Methodology access*): (1) a fixed-position full-viewport backdrop (`rgba(0,0,0,0.5)`, `z-index:10000`) with `display:flex`, top-aligned content area and 40 px vertical padding; (2) a centred white box `max-width:800px`, `max-height:85vh`, `overflow-y:auto`, rounded 10 px, `box-shadow:0 12px 40px rgba(0,0,0,0.35)`, padding `28px 32px 32px`; (3) a sticky top-right × close button (32 px square, `float:right`, transparent background, grey `#6b7280` glyph, hover darkens to `#111827` with `rgba(0,0,0,0.06)` background); (4) the methodology card's `innerHTML` is **cloned** (not moved) into the box so the original `#methodologyCard` remains hidden in its drawer slot for PDF export and other callers; (5) three close mechanisms — close button click, Escape keydown (capture phase + `stopImmediatePropagation` so the drawer Esc handler doesn't also fire), and backdrop click (with `e.target === backdrop` guard so clicks inside the box don't close). `showMethodologyModal` is idempotent — reopening while a modal is already open closes the previous instance first, preventing stacked backdrops. Both functions are exposed on `window` so future callers can reuse them. **Verified in preview**: `typeof window.showMethodologyModal === 'function'` ✓; clicking `#side-panel-methodology-btn` creates `#methodologyModalBackdrop` at `z-index:10000` with an 800 px-max-width box containing the cloned content (confirmed via accessibility snapshot — "Close methodology" button + "Methodology" heading + all section collapse chevrons) ✓; close-button click removes the backdrop ✓; Escape key removes the backdrop and leaves the drawer's open/closed state untouched ✓; backdrop click (target === backdrop) removes the backdrop ✓; `#methodologyCard` remains `display:none` throughout so the original drawer DOM is not disturbed ✓; no console errors. **Not touched**: `#methodologyCard` HTML content, `_resonateSidePanelBoot` side panel wiring, `#pdfArea` rescue logic, PDF export, the drawer Escape handler, propagation or criteria logic, save/load.

- **Global keyboard shortcuts restored** — The power-user hotkey handler (`P` point, `L` line, `A` area, `K` building, `1`–`4` receivers, `B` barrier, `N` new building, `G` ground absorption, `T` terrain, `C` contours, `O` buildings, `F` reflections, `R` ruler, `M` noise map, `H` show/hide objects, `Z` zones, `S` search, `E` expand map, `?` quick reference) was accidentally deleted in commit `afd6913` — the `_kbToast`/`_btn`/`_isActive` helpers and the entire `document.addEventListener('keydown', …)` switch block disappeared, leaving all the `.mp-kbd` badges on the toolbar buttons advertising keys that had no handler. **Fix**: restored the handler at the very end of `index.html` (just before `</body>`) based on the original commit `4ea3c90`, and added the new `case 'w': case 'W':` → `drawCortnRoadBtn` path that the original `afd6913` changelog bullet claimed to have shipped but never actually did. Input guards unchanged — skips when target is INPUT/TEXTAREA/SELECT/contentEditable, skips when Ctrl/Meta/Alt is held (so `Ctrl+Z` undo still reaches the undo manager). Each keypress shows a 1.5 s toast pill at the bottom of the viewport. **Verified in preview**: `P` activates `mapMode-addSource` with toast "Point source (P)"; `T` toggles terrain ON with state-aware toast "Terrain ON (T)"; `W` activates `drawCortnRoadBtn` with toast "Road (CoRTN) (W)"; `P` dispatched while `#mapSearchInput` is focused is NOT intercepted (`defaultPrevented === false`); `Ctrl+P` is NOT intercepted either; no console errors. All 22 button IDs referenced by the handler confirmed to exist in the current DOM. **Not touched**: the `.mp-kbd` badges themselves, button click handlers, existing `Ctrl+Z`/`Ctrl+Y` undo hotkeys at [`index.html:19655`](../index.html:19655), the `]` drawer-toggle hotkey at [`index.html:4033`](../index.html:4033), Delete-key barrier shortcut, Escape handlers, propagation or criteria logic.

- **Quick Reference (?) and Suggested Noise Sources (💡) toolbar buttons restored** — Both floating-panel toggle buttons in the side-panel toolbar row were not responding to clicks. **Root causes (two compounding bugs)**: (1) The entire `<script>` block containing the `/* Floating help panel */` IIFE, `PROJECT NAME EXTRACTION FROM PDF TITLE BLOCK`, the Word export helpers (`makeWordTable`, `replaceTableDataRows`, etc.) and `generateReport()` was **truncated mid-expression** at line 35179 — the file literally ended with a stray `v` (start of `var predRow = [...]` inside the per-case music table forEach callback), with no closing braces, no `</script>`, and no `</body></html>`. The browser's HTML parser was lenient enough to still render the page, but the 50 KB script failed to parse (`SyntaxError: Unexpected end of input`) which meant NONE of its IIFEs executed — so the help toggle handler was never attached. The suggest IIFE was in a separate earlier `<script>` tag so its handler attached and would have worked if not for bug #2. The truncation was already present in the working tree when it was bundled into commit `afd6913`; restored the missing tail by replaying it from commit `2f3cbf3` (the last pre-truncation state): music case `forEach` body (`predRow`/`critRow` build + `musicDataRows.push`), `else` branch with `stdRows` standard table, outer `forEach` close, `appendToWordBody`, `zip.file` write-back, blob/download/filename logic, `.then`/`.catch` close, `generateReport` close, `DOMContentLoaded` button wire-up, IIFE close, `</script></body></html>`. (2) Both `#helpFloatPanel` and `#suggestFloatPanel` are declared inside `<div id="pdfArea">` in the source HTML. The drawer-layout refactor at [`index.html:3994`](../index.html:3994) hides `#pdfArea` with `display:none` after moving its other content out — but these two floating overlays were missed. `display:none` on an ancestor excludes ALL descendants from the render tree regardless of `position: fixed` on the descendant (this is render-tree exclusion, not a `display` cascade), so clicking the toggle buttons set `panel.style.display = 'flex'` but the panels remained 0×0 with `offsetParent: null`. **Fix**: added `_rescuePdfAreaFloatingPanels()` inside the drawer-boot function right after `_insertAllGroupAnchors()` that walks `['helpFloatPanel','suggestFloatPanel']` and calls `document.body.appendChild(p)` if the node isn't already under `<body>`. Because the drawer-boot script runs INLINE during HTML parse (at ~line 3477) well BEFORE those panel elements are defined (~line 33595), the rescue function is wrapped in a `DOMContentLoaded` check — schedules the move for after the parser reaches the end of the document, by which time both elements exist and their click listeners (attached by the existing IIFEs during parse) are already in place. `appendChild` preserves event listeners so no handler re-attachment is needed. **Verified in preview** (live test via `preview_eval`): after reload both `helpFloatPanel` and `suggestFloatPanel` report `parentElement: BODY`; clicking `#helpToggleBtn` renders the Quick Reference overlay at 352×452 px (`display: flex`, `offsetParent: BODY`) and adds `.help-active` to the button; clicking `#suggestToggleBtn` renders the Suggested Sources overlay at 320×122 px and adds `.suggest-active`; second click on each closes cleanly; close-button (×) clicks also close the panels and clear the active class; `typeof window._soPdfExtractProjectName === 'function'` (confirms the big script now parses and all its late IIFEs ran); `new Function(helpScript.textContent)` parses OK; the accessibility snapshot shows the Quick Reference panel rendered with its header, × close button, and 6 disclosure groups (Derive criteria, Save images, Modelling Tools, Predictions, Noise mapping, Keyboard shortcuts); no console errors. **Not touched**: the toggle IIFEs themselves, panel HTML/CSS, the `_resonateSidePanelBoot` that moves `.mp-help`/`.mp-suggest` into `#side-panel-toolbar`, the `pdfArea` element itself (kept hidden for PDF export compatibility as before), any propagation or criteria logic, save/load.

- **Onboarding hint — close (×) button** — Added a small `×` close button to the top-right corner of the `#map-guide-overlay` popup ("Search for an address or click the map to place a noise source") so the user can dismiss the hint at will, not only by engaging a tool. **Changes**: (1) the `innerHTML` at [`index.html:3861`](../index.html:3861) now contains a `<button class="map-guide-close" aria-label="Dismiss hint" title="Dismiss">×</button>` inside the `.map-guide-text` box, followed by the existing instruction text; (2) a click listener is attached at [`index.html:3870`](../index.html:3870) that calls `e.stopPropagation()` and then `window._dismissOnboardingHint()` — the same idempotent dismiss path used by every other map action, so the session flag `_onboardingHintDismissed` latches and `updateEmptyStates()` will not re-show the hint for the remainder of the session; (3) CSS additions at [`index.html:1536`](../index.html:1536): `.map-guide-text` got `position: relative` and asymmetric padding `16px 36px 16px 28px` to make room for the button without cramping the text; new `.map-guide-close` rule positions the button absolutely at `top:4px; right:6px`, `20×20 px`, transparent background, grey `#9ca3af` glyph, and — crucially — `pointer-events: auto` because the parent `#map-guide-overlay` has `pointer-events: none` so map clicks pass through; `.map-guide-close:hover` / `:focus` darken the glyph to `#374151` and add a subtle `rgba(0,0,0,0.06)` background for click feedback, with a 0.15 s ease transition. Dismissal is session-only — no save/load serialisation, no `localStorage` — reloading the page reshows the hint. **Verified in preview**: on reload the overlay renders with the × button at the expected bounds (20×20 px, top-right of the text box); clicking the button sets `window._onboardingHintDismissed = true` and adds `.guide-hidden` to the overlay; the search textbox and map click-to-place path remain functional; calling `_showMapGuide()` after dismissal correctly leaves the hint hidden (session-sticky); no console errors. **Not touched**: the instruction text content, search bar, map click-to-place wiring, toolbar buttons, floating panels, save/load JSON serialisation, any propagation or criteria logic.

- **Site plan overlay aspect ratio — initial placement + corner resize** — Fixed two related issues with imported PDF/PNG/JPEG site plan overlays. **(1) Initial placement**: `_soAddOverlay()` at [`index.html:23940`](../index.html:23940) previously calculated initial bounds as 25% of the map view in both lat and lng independently, producing a near-square region regardless of the image's natural dimensions — a landscape A3 site plan arrived on the map squashed into a square. The new-overlay path is now deferred into the `new Image().onload` callback so natural `naturalWidth`/`naturalHeight` are available before bounds are computed. Inside onload we take 25% of the view lat span as `latH`, derive `lngH = latH × (naturalW/naturalH) / cos(centreLat)` so the on-screen proportions match the image (1° lat ≠ 1° lng in metres), and fall back to width-constrained sizing if the derived box would exceed 25% of the view lng span. A new helper `_finishCreate(bounds, naturalW, naturalH)` is split out of `_soAddOverlay()` so both the new and saved paths share the layer-creation / rotation-patch / placement-entry / object-list-render code. **(2) Saved-assessment path preserved**: when `opts.bounds` is provided (load path via `_soLoadAll` at [`index.html:24436`](../index.html:24436)) the bounds are used as-is with NO aspect-ratio recalculation — saved bounds round-trip exactly. Natural dimensions are still measured async in the saved path so proportional corner-resize keeps working after load. An `onerror` fallback uses the old square-ish bounds if the image fails to decode so the user still gets a manipulable overlay. **(3) Corner-drag Shift inverted**: at [`index.html:24146`](../index.html:24146) the `if (shiftHeld && ovr._imgW > 0 && ovr._imgH > 0)` branch became `if (!shiftHeld && ovr._imgW > 0 && ovr._imgH > 0)`, making aspect-ratio-locked resize the default and Shift the free-resize modifier — matches Photoshop/Figma/PowerPoint UX. **Not touched**: rotation logic (`_soApplyRotation`, `_soSnapAngle`), `_soVisualCorners`, `_soRotHandleLatLng`, `_soUnRotate`, centre-drag move handler, rotation handle behaviour, lock/unlock toggle, PDF text extraction / address detection pipeline, image compression dialog, `_soSerialize` / `_soLoadAll` on-disk format (still `{id, name, imageSrc, bounds, opacity, rotation, locked, visible}`), noise canvas, compliance canvas, CoRTN overlays, propagation or criteria logic. UAT cases added in [`uat-tests.md`](./uat-tests.md) §Site plan overlay.

- **RHS panel bars — click anywhere to expand/collapse** — Previously, the collapsible section headers on the right-hand side panel could only be toggled by clicking the small ▲/▼ button. Now clicking anywhere on the header bar (title text, whitespace, or button) toggles the section. Applies to all RHS panels: Objects, Planning & Design Code, MBS 010, Receivers & criteria, Recommended treatments, and all `initCollapse`-managed card sections (Noise sources, Predicted noise, Propagation method, etc.). Existing action buttons (refresh ↻, export ⇩) use `event.stopPropagation()` so they still fire their own action without triggering a collapse. CSS updated: `.card-header` now has `cursor:pointer; user-select:none`. New shared helper `toggleCardSection(bodyId, btnId)` replaces duplicated inline onclick handlers. No logic, calculation, save/load, or criteria changes.

- **RHS drawer panel UI alignment** (CSS only, no logic changes) — Card headings normalised to 12px/600 uppercase with gold left-border accent; card border-radius 14→8px, padding 12→10px, margin-bottom 10→8px; table `<th>` inside drawer changed from solid yellow to warm neutral `#f8f8f6` with 2px gold underline; table cells normalised to 12px; `.h2` class headings normalised; collapse buttons refined (11px, smooth transitions); jump-nav active state changed from blue to gold tint. All scoped to `#drawer-content` — LHS panel, map, header untouched.

- **CoRTN road line style — solid/dashed/solid road appearance** — Changed the CoRTN road map rendering from a single dashed blue polyline (`dashArray: '8,6'`) to a composite two-layer style: a solid dark-blue base line (`weight: 6`) overlaid with a dashed white centre-line (`weight: 2, dashArray: '8,6'`), giving the visual appearance of a road with lane markings. `renderCortnRoadLayer()` now creates a `L.layerGroup([polyBase, polyCentre])` per road instead of a single `L.polyline`. Tooltip, click (open panel), and context menu (edit/duplicate/delete) events are bound to both layers. The draw handler still uses the solid blue `CORTN_STYLE` during drawing (composite rendering appears on completion). **Toolbar change**: Road (CoRTN) button moved from between Line source and Area source to its own separated section below Receiver 4, and renamed to **"Road (CoRTN) – map only"** to clarify it is a map-display feature. **Not touched**: CoRTN calculation engine, CoRTN noise map, save/load, other source types, criteria panels. No console errors.

- **CoRTN road noise map (Phase 5)** — CoRTN roads now participate in a dedicated grid-based contour map, separate from the ISO 9613-2 noise map. New UI: **CoRTN road noise map** button in the Modelling panel (directly under the existing Noise map controls) with Day/Night period pills, LAeq/LA10 metric toggle, height, grid-size, range, interval, and a Levels/Compliance view toggle. New file: [`cortn-worker.js`](../cortn-worker.js) — a Web Worker that sweeps the map grid, computes the per-cell perpendicular distance and angle-of-view to every CoRTN road, runs the full `SharedCortn.calcCortnRoadPeriod` correction chain per road, and energy-sums broadband LA10/LAeq into two `Float32Array` grids. New namespace: `SharedCortn` in [`shared-calc.js`](../shared-calc.js) (bottom of file) — ports `calcCortnFreeField`, `calcCortnRoadPeriod`, `cortnCalculateBarrier`, `cortnDistanceToPolyline`, and `cortnAngleOfViewFromReceiver` as pure, state-free functions so both the main thread and the worker can share a single copy of the math. Main-thread additions in [`index.html`](../index.html): CoRTN map state variables (`_cortnMapOn`, `_cortnMapPeriod`, `_cortnMapMetric`, `_cortnMapHeight`, `_cortnMapGridSize`, `_lastCortnData`, legend + compliance state) next to the ISO map state block; new functions `computeCortnMap()`, `renderCortnCanvas()`, `generateCortnContourLines()`, `showCortnLegend()`, `renderCortnComplianceMap()`, `generateCortnComplianceContours()`, `removeCortnMap()`, `debouncedRecomputeCortn()`; and event handlers for the button / period / metric / grid / legend / compliance / criterion / map-moveend controls. **LAeq ↔ LA10 toggle re-renders instantly** — the worker returns both grids in one pass and the metric switch just picks which `Float32Array` to paint. Period/height/grid changes trigger a debounced recompute. CoRTN road changes (add / edit / delete) trigger a debounced recompute via `window._recomputeCortnMap()` hooked into `recalcAndRefresh`, the draw-complete handler, the panel delete button, and the context-menu delete. **Save/load**: new `data.cortnMapSettings` object persisted in both the main save path and the undo snapshot path via `window._getCortnMapSettings / _setCortnMapSettings`; the "on" flag is deliberately NOT persisted — the map must be re-clicked on load, matching the ISO map policy. Grid size always resets to Auto on load to avoid reloading a slow fine-grid from a previous save. **Simplifications (Phase 5)**: (1) barrier geometry is per-road only — `distToBarrier_m` stays at its UI-configured value for every cell, so cells closer than `distToBarrier_m` correctly fall back to "no screening" via the helper's `applied:false` return, but cells farther away see a geometry-scaled IL rather than true fixed-absolute barrier positioning; (2) reflections are forced to zero in grid mode (no facade geometry per cell); (3) no terrain profile screening; (4) minimum grid is 5 m — 1 m and 2 m are intentionally omitted because broadband CoRTN doesn't benefit from finer spacing. **Distance convention**: the worker sets `clone.distFromKerb_m = perpDist − 3.5` (clamped to ≥ 0.5 m) so that `calcCortnFreeField`'s internal `d_horiz = distFromKerb_m + 3.5` equals the actual perpendicular distance from the grid cell. This interprets `perpDist` as distance to the effective source line rather than to the kerb. **Note**: the existing Phase 4 receiver-point path uses `clone.distFromKerb_m = perpDist` directly (→ `d_horiz = perpDist + 3.5`), so a grid cell placed at a receiver's location will read ~1–1.5 dB louder than the receiver-point value for short distances. Flagged in `references/uat-tests.md` — Phase 4 alignment is future work. **Not touched**: existing ISO 9613-2 noise map (`noise-worker.js`, `computeNoiseMap`, `buildWorkerSources`, `renderNoiseCanvas`, `generateContourLines`, `showNoiseLegend`), existing CoRTN receiver-point calculations (`calcCortnFreeField`, `calcCortnRoadPeriod`, `recalcCortnRoad`, `calcCortnRoadAtReceiver`, `_cortnCalculateBarrier`), CoRTN road UI panel, all other source types, criteria panels, terrain screening. Both noise maps can be active simultaneously — their contours draw as independent Leaflet layers.

- **Methodology moved from RHS drawer to LHS side panel button + centred modal overlay** — Previously the Methodology panel (`#methodologyCard`, ~24 sections of acoustic reference content) lived at the bottom of the RHS drawer and was accessed via a 6th "Methodology" tab in the jump nav. Deb moved it to the LHS side panel. **Changes**: (1) new `#side-panel-methodology-btn` appended to `#side-panel-footer` in `_resonateSidePanelBoot` at [`index.html:1980`](../index.html:1980), positioned directly underneath the existing Show/Hide-assessment-panel drawer toggle; styled in the CSS block at [`index.html:1212`](../index.html:1212) as a full-width dark pill matching the side-panel aesthetic, hidden via `#side-panel.collapsed #side-panel-methodology-btn { display:none }` when the panel is collapsed. (2) new `showMethodologyModal()` function at [`index.html:35513`](../index.html:35513) next to `showShareModal()` — clones `#methodologyCard.innerHTML` into a fixed-position full-viewport backdrop (`rgba(0,0,0,0.5)`) containing a centred white 800 px-max box with `max-height:85vh`, `overflow-y:auto`, a sticky top-right `×` close button, and close-on-backdrop-click + close-on-Escape handlers. Exposed on `window.showMethodologyModal`. (3) Methodology removed from the jump-nav `groups` array at [`index.html:3961`](../index.html:3961) — only Setup/Criteria/Sources/Results remain. (4) `_insertGroupAnchor('group-methodology', …)` removed from `_insertAllGroupAnchors()` at [`index.html:3816`](../index.html:3816). (5) `#methodologyCard` still appended to `#drawer-content` (so `showMethodologyModal` can clone its innerHTML and PDF export still reads it), but immediately set to `display:none` at [`index.html:3737`](../index.html:3737). **Not touched**: methodology card HTML content (`#methodologyCard` inner DOM unchanged — PDF export and modal cloning both see the same content), drawer toggle (Show/Hide assessment panel) functionality, side panel collapse/expand behaviour, jump nav scroll spy for Setup/Criteria/Sources/Results, share modal, propagation or criteria logic, save/load.

- **ISO 9613-2 §7.4 ground-barrier interaction — sub-path Agr** — Implemented the §7.4 insertion-loss form of the combined barrier + ground term `AgrBar = max(Dz, Agr_subpath)`. Previously the calculator used `max(Dz, max(Agr, 0))` with the unobstructed-path Agr, which ignored the fact that when a barrier is present the ground effect should be recomputed along the source→barrier and barrier→receiver legs (the barrier top acts as a pseudo-receiver then pseudo-source). **New function** [`calcAgrBarrier(hS, hR, dp, G, barrierInfo)`](../shared-calc.js) in `shared-calc.js` calls `calcAgrPerBand` twice — once with `(hS, hBar, d1, {Gs, Gr: Gm, Gm})` for the source-side sub-path and once with `(hBar, hR, d2, {Gs: Gm, Gr, Gm})` for the receiver-side sub-path — and returns the per-band sum. **`calcISOatPoint` / `calcISOatPointDetailed`** gained an optional `barrierInfo = {d1, d2, hBar}` parameter (horizontal sub-path distances + barrier top height above ground). When provided the per-band loop uses `max(Ascreen, Agr_bar)` instead of `max(Ascreen, Agr)`. When absent the function falls back to the unobstructed-path Agr (backward compatible). The `Agr ≥ 0` clamp inside the barrier branch was removed: because `Ascreen > 0` inside the branch, `max(Ascreen, Agr)` already prevents negative Agr from reducing the total attenuation, so the clamp was redundant. **`getDominantBarrier`** now returns `d1` and `d2` directly in its result object (previously they were internal locals). **Call sites updated to thread `barrierInfo`**: `noise-worker.js` main loop (ISO Lmax, ISO Leq, and flat-spectrum-with-zones branches, including the over-top/gap energy combine paths — 6 call sites, constructed once as `_barrInfoW` per source-receiver pair); `index.html` line source loop (`bwLS`/`barrInfoLS`); area source loop (`bwAS`/`barrInfoAS`); building source propagation path (`barrInfoBs` in `_bsPropagateSub`); source-pin Leq path (`_barrInfoSP` threaded through the 6 `calcISO9613single` calls); source-pin Lmax path (`_barrInfoLmax` threaded through its 6 `calcISOatPoint` calls, replacing a dead `terrainPerBand` 10th-arg that was silently being dropped before the Deygout terrain refactor). `calcISO9613single` gained an 11th parameter `barrierInfo` that it passes straight to `calcISOatPoint` and `calcISOatPointDetailed`. **Test updates** — `iso17534.test.js` helper `computePerBandLA_barrier` now accepts `barrierInfo` and uses `calcAgrBarrier` when supplied; T08/T09/T11 tests pass hand-computed `{d1, d2, hBar}` from their Cartesian geometry. Tolerances: T01–T03 unchanged at ±0.25 dB; **T09 tightened from ±1.0 dB to ±0.25 dB** (now 32.80 dB vs ref 32.93 — the fix moved it 0.19 dB in the right direction because T09's small barrier IL means Agr_bar dominates some bands); T08 set to ±0.6 dB and T11 kept at ±1.0 dB — in both of those cases the barrier IL is large enough that `Abar > Agr_bar` in every band, so the `max()` always picks Abar and the §7.4 sub-path calculation has no numerical effect; the residual ~0.5/1.0 dB error is from the Fresnel z approximation, the spatially-varying G region-extent averaging, and the lateral-path energy-summation model — not from the ground-barrier interaction. **Verified in preview**: `SharedCalc.calcAgrBarrier` is a function; T01 G=0 → 44.29 (ref 44.29); T02 G=0.5 → 41.52 (ref 41.53); T03 G=1 → 39.13 (ref 39.14) — all unchanged from before; per-band debug for T08 confirmed `Abar[i] > Agr_bar[i]` in every band (0.000 dB diff vs old); T09 improved from 32.61 → 32.80. No console errors on reload. **Not touched**: `calcAgrPerBand` (unchanged — the new function composes two calls to it); `calcBarrierAttenuation` / Maekawa (unchanged — Dz stays the same); terrain screening (the Deygout per-band IL system remains independent — it combines with barrier IL via `Ascreen = max(Abar, Aterr)` as before); reflections, end diffraction, save/load, SA/VIC/NSW criteria. Simple/ISO convergence for G=0 (no barrier) still holds. `references/soundplan-comparison.md` is referenced by CLAUDE.md but does not exist in the current working tree so no update was made there.

- **Hotkey `W` toggles CoRTN road-draw mode** — The Tools-panel `#drawCortnRoadBtn` (UK CoRTN road traffic source with Australian adjustments) previously had no keyboard shortcut — every other placement/draw button around it does (`P` Point, `L` Line, `A` Area, `K` Building source, `B` Barrier, `N` New building, `G` Ground absorption), so CoRTN was the odd one out. A full hotkey audit of the global keydown switch at [`index.html:34233`](../index.html:34233) confirmed `R` was taken by Ruler, `D`/`I`/`U` are reserved by orphaned UI hints on the Cadastral/Aerial/Urban-area buttons, and `W` (for "Way") was the first genuinely-free letter in the user's priority list (`R → D → W → Shift+R`). **Changes**: (1) new `case 'w': case 'W':` at [`index.html:34323`](../index.html:34323) that calls `_btn('drawCortnRoadBtn')` and shows `_kbToast('Road (CoRTN) (W)')` — behaviour mirrors the other draw-mode hotkeys that click through to the existing button's toggle handler. The global switch already ignores `INPUT`/`TEXTAREA`/`SELECT`/`contentEditable` targets at [`index.html:34237`](../index.html:34237) and skips modifier combos, so no new guards were needed. (2) Button label at [`index.html:2116`](../index.html:2116): `title` updated to include `(W)` and a new `<span class="mp-kbd">W</span>` appended to the button text, matching the convention used by every other draw-mode button in the Tools panel. **Not touched**: the CoRTN drawing wiring at [`index.html:29444`](../index.html:29444), `renderCortnRoadLayer()`, the CoRTN calculation engine (`_cortnCalculateBarrier`, `cortnBroadbandToSpectrum`, `_cortnDistanceToPolyline`, etc.), the line-source `L` hotkey, every other hotkey handler, the MBS 010 button (its pre-existing orphaned `<span class="mp-kbd">X</span>` hint was left intact for a future hotkey sweep), the Cadastral/Aerial/Urban-area `D`/`I`/`U` orphan hints. **Verified by code inspection**: new switch case is inside the same `return`-early guard for inputs; `_btn('drawCortnRoadBtn')` simulates a click which triggers the existing CoRTN draw-mode toggle unchanged. **First attempt** wired `W` to `#mbs010ToggleBtn` (MBS 010 screening overlay) based on the phrase "road/traffic noise contour layer" — that was wrong: MBS 010 bundles road + rail + aircraft + air emissions as an SA regulatory screening layer, not the CoRTN prediction tool. The MBS 010 button, title, and click handler have been reverted to their pre-change state. No console errors.

- **Onboarding hint dismisses on any map action** — The `#map-guide-overlay` popup ("Search for an address or click the map to place a noise source") previously only dismissed when a source or receiver existed — `updateEmptyStates()` at [`index.html:15118`](../index.html:15118) toggled `.guide-hidden` based on `hasSource || hasAnyReceiver`. That meant drawing a barrier, enabling terrain, opening a site plan, starting the ruler, or any other non-source/non-receiver action left the hint hanging on the map. **Fix**: added a session flag `window._onboardingHintDismissed` and an idempotent `window._dismissOnboardingHint()` at [`index.html:3724`](../index.html:3724) that hides the overlay and latches the flag so `updateEmptyStates()` never re-shows it. `window._showMapGuide` now respects the flag too. **Hooks**: (1) a delegated click listener wired at [`index.html:3733`](../index.html:3733) attaches to every relevant Tools-panel button — `terrainToggleBtn`, `terrainContourBtn`, `buildingsToggleBtn`, `rulerBtn`, `sitePlanOverlayBtn`, `drawBuildingBtn`, `drawBarrierBtn`, `drawGroundZoneBtn`, `mapMode-addSource`, `drawLineSourceBtn`, `drawCortnRoadBtn`, `drawAreaSourceBtn`, `drawBuildingSourceBtn`, `mapMode-r1..r4` — firing dismiss on the click so the hint is gone the instant the user engages any tool; (2) `setMode()` at [`index.html:22742`](../index.html:22742) also dismisses whenever a non-null placement mode is entered, which covers the keyboard shortcuts (`1`–`4`, `P`, etc.) that bypass the toolbar buttons; (3) `selectNominatimResult()` at [`index.html:22992`](../index.html:22992) dismisses when the user picks an address search result; (4) the load-assessment completion hook at [`index.html:19085`](../index.html:19085) was promoted from `_hideMapGuide` to `_dismissOnboardingHint` so a loaded assessment also latches the flag. **Not touched**: the popup text, its styling, its appearance on initial page load, `updateEmptyStates()` source/receiver derivation logic for the drawer panel empty states, any drawing tool behaviour, panel toggle behaviour. No console errors.

- **Assessment panel toggle — explicit labels + collapsed by default** — The map toolbar's panel toggle button (`#mp-toggle-drawer` / `#drawerPanelBtn`) previously showed ambiguous labels ("Panels" when collapsed, "Expand" when open — the second label was actively misleading since the button no longer enters fullscreen). Relabelled to **"Show assessment panel"** when the drawer is collapsed and **"Hide assessment panel"** when open. The default on-page-load state is now **collapsed** so the map fills the viewport on first visit and the user opts in to the drawer. **Changes**: (1) `#mapMaximiseBtnLabel` initial text at [`index.html:1849`](../index.html:1849) changed from `Expand` to `Show assessment panel` so there's no flash of the old label before JS runs; (2) `setDrawerOpen()` at [`index.html:3787`](../index.html:3787) now writes `open ? 'Hide assessment panel' : 'Show assessment panel'` instead of `open ? 'Expand' : 'Panels'`; (3) the restore-saved-state block at [`index.html:3798`](../index.html:3798) flipped from `setDrawerOpen(saved !== 'false')` to `setDrawerOpen(saved === 'true')` — i.e. the drawer now opens ONLY if localStorage `resonate_drawer_open` is explicitly `"true"`, and the catch-block fallback flipped from `setDrawerOpen(true)` to `setDrawerOpen(false)`. Every existing code path that toggles the drawer (click handler, `]` key, `E` key via `toggleMapFullscreen`, capture-phase Esc listener, receiver-placement auto-open at [`index.html:22791`](../index.html:22791), drawer-edge click at [`index.html:4000`](../index.html:4000), and the drawer's own triangle button) goes through `setDrawerOpen()` so the label and state stay in sync automatically — no changes were needed at those call sites. **Verified in preview**: on fresh load with `localStorage.removeItem('resonate_drawer_open')` the drawer is collapsed and the toolbar button reads `Show assessment panel`; clicking the button opens the drawer and the label flips to `Hide assessment panel`; clicking again closes it and the label reverts; pressing `Esc` while the drawer is open closes it and the label reverts to `Show assessment panel`; pressing `]` and `E` produce the same toggle behaviour; `localStorage.setItem('resonate_drawer_open', 'true')` + reload opens the drawer on startup as expected (explicit user preference still honoured); placing a receiver on the map still auto-opens the drawer (the existing `window._setDrawerOpen(true)` call at the receiver placement handler correctly updates the label). **Not touched**: drawer expand/collapse animation, floating sidebar in maximised mode, any other toolbar buttons or menus, keyboard shortcut keys (Escape still closes, `E` and `]` still toggle), drawer panel content and ordering, responsive breakpoints. No console errors.

- **Building source — Interior Lp library dropdown** — Adds a searchable preset library to the building source floating panel so users can pick an indoor environment (e.g. "Gymnasium — general sporting activity", "Workshop — light manufacturing") instead of typing 24 octave-band Lp values by hand. **Data**: new `BUILDING_LP_LIBRARY` array at [`index.html:6646`](../index.html:6646) with 12 entries across 6 categories (Recreation, Hospitality, Industrial, Childcare, Community, Commercial). Each entry stores `{name, category, lp_dba, spectrum:{63..8000}}` representing **interior Lp** (sound pressure level inside the building), NOT Lw — the existing building-source pipeline still converts Lp → radiated Lw through the wall/roof Rw. `BUILDING_LP_LIBRARY_GROUPED` and `rebuildBuildingLpGrouped()` mirror the line/area patterns and the rebuild is wired into `rebuildAllLibraries()`. **Supabase**: migration adds `'building'` to the `reference_noise_sources_source_kind_check` constraint (drop+recreate so existing deployments pick it up) and seeds the same 12 presets via `INSERT … source_kind='building'`. [`library-loader.js`](../library-loader.js) gains `mapBuildingLpRow()` which reuses `overallLwA()` to compute the broadband dB(A) (the math is identical for Lp and Lw), partitions a fourth `buildingRows` filter, hot-swaps `window.BUILDING_LP_LIBRARY` when ≥1 row returns, and the badge summary now reads `P / L / A / B / C`. **UI**: new "Interior Lp library:" combo (`#bs-lib-combo` + `#bs-lib-dropdown`) inserted in `buildBsPanelHTML()` between the geometry block and the per-period interior noise level cards. The dropdown reuses the point-source search-combo pattern (input/focus/blur listeners, mousedown selection) but iterates `BUILDING_LP_LIBRARY_GROUPED`. **Apply function** `_applyBsLpEntry(entry)` writes `bs.interiorLp[period].octave[b]` for all three periods (day/eve/night), updates the `bs_${period}_lp_${b}` DOM inputs, sets `bs.interiorLp[period].broadband = entry.lp_dba`, stores `bs.lpLibraryEntry = entry.name`, and triggers `_bsRefreshDerived()` + `render()` to recalculate the radiated Lw through the existing wall/roof construction pipeline. Manual band overrides after library selection still stick — the library only pre-fills, it doesn't lock the inputs. **Save/load**: `bs.lpLibraryEntry` (string) added to both save paths and pulled in automatically by `_setBuildingSources()` via its `JSON.parse(JSON.stringify(d))` deep-clone. On reopen the combo is pre-filled from the stored name. **Admin UI**: [`supabase-admin.js`](../supabase-admin.js) gains a 5th `LIB_SPECS.building` tab with name/display_group/source_description fields and the same 8 octave-band inputs as point sources — Deb can now CRUD building Lp presets through the same modal. **Verified in preview**: library loads with 12 entries across 6 categories ✓; `bs-lib-combo` + `bs-lib-dropdown` render in the panel ✓; typing "gym" filters to 2 Gymnasium entries under the "Recreation" header ✓; selecting "Gymnasium — general sporting activity" populates all 24 octave-band inputs (day/eve/night) with `{63:72, 125:75, 250:78, 500:80, 1000:82, 2000:79, 4000:74, 8000:68}` ✓; broadband Lp set to 85 dB(A) for all periods ✓; `bsDerivedLw` recalculates the per-wall + roof radiated Lw using the wall Rw=25 (Colorbond) ✓; manual override of `bs_day_lp_1000` to 99 sticks while the other periods retain the library value (eve_1000=82) ✓; save → clear → `_setBuildingSources([saved])` → reopen panel preserves `lpLibraryEntry`, octave overrides, and `lpSource='octave'` ✓; combo is pre-filled with the stored library name on reopen ✓; Supabase loader logs `120 point / 10 line / 5 area / 0 building / 13 constructions` (0 because the migration hasn't been run on the live DB yet — once it is, the offline snapshot hot-swaps cleanly). **Not touched**: building source calculation pipeline (interior Lp → wall/roof Rw → radiated Lw), construction libraries (walls/roof/openings), point/line/area source dropdowns, manual octave-band entry behavior. No console errors.

- **Terrain screening — Deygout 3-edge method with per-band IL** — Replaced the single-ridge terrain screening in the noise map worker with the Deygout principal-edge algorithm used by SoundPLAN / CadnaA. The new `terrainILPerBand()` at [`noise-worker.js`](../noise-worker.js:385) returns an 8-element octave-band IL array (63 Hz … 8 kHz) instead of a single broadband dB value at 1 kHz. **Algorithm**: `findTerrainEdges()` samples the DEM profile adaptively (~1 sample per 5 m of path, `N = clamp(round(totalDist/5), 20, 100)`) and collects every local maximum above the source→receiver line-of-sight. `deygoutSelectEdges()` picks the principal edge by Fresnel-number proxy `h²·(d1+d2)/(d1·d2)`, then runs the same proxy on the source-side sub-path (source tip → principal tip) and receiver-side sub-path (principal tip → receiver tip), returning 1–3 edges ordered source→receiver. Each selected edge contributes a per-band Maekawa IL via the unchanged `calcBarrierAttenuation()`; band contributions are summed across edges and capped at 25 dB per band (ISO 9613-2 practical terrain limit). **Pre-pass grid**: `terrainSmoothed[si]` changed from `Float32Array(rows*cols)` to stride-8 `Float32Array(rows*cols*8)` with index `(r*cols + c)*8 + bandIdx`. Each octave band is Gaussian-smoothed independently (separable 5-tap kernel, σ=1.0) using reusable per-band scratch buffers before being packed back into the stride-8 output. **Main loop**: terrain IL is now read as an 8-element `terrBands` array per cell. ISO paths (`isLmaxSimple=false && spectrum available`) pass `terrBands` through as the 11th parameter to `calcISOatPoint` — the post-subtract `lp -= (terrIL - buildingIL)` hack is gone. Simple/broadband paths (simple Leq, Lmax-simple, ISO without spectrum) still use the 1 kHz band as the representative broadband value (`terrBands[4]`) so there is no change in those branches. **`calcISOatPoint` / `calcISOatPointDetailed`**: gained an optional 11th parameter `terrainILPerBand`. Inside the per-band loop, the per-band screen is `max(Abar[i], Aterr[i])` and combines with Agr the same way barrier IL does (`AgrBar_f = max(screen, Agr_subpath)` — no double counting with ground). `calcBarrierAttenuation` is unchanged — we just call it more times. Detailed breakdown bands now expose an `Aterr` field. **Verified in preview** (3-ridge synthetic DEM, source at 0/0, receiver grid 320 m deep, Lw 90 dB, simple propagation): flat terrain → avg 36.6 dB (matches no-terrain baseline exactly — zero regression); 1 ridge → min 15.9 / avg 28.8; 2 ridges → min 8.7 / avg 22.1; 3 ridges → min 6.4 / avg 21.7 (screening hits the 25 dB cap). Monotonic in ridge count for both min and avg cells. Max level (102 dB near source) is unchanged across all four scenarios. Unit tests against `calcISOatPoint` directly: `null` terrain ≡ `[0,0,0,0,0,0,0,0]` terrain (31.859 dB, byte-identical), terrain-only reduces level appropriately, barrier-dominated case matches the barrier-only baseline within float epsilon (no double counting). Worker produces no NaN cells and no console errors. **Not touched**: `calcBarrierAttenuation` (core Maekawa), `getDominantBarrier`, building/barrier screening, DEM fetching/caching/`lookupElev` bilinear, Gaussian kernel parameters, ground absorption (`calcAgrPerBand`, `calcAgrBarrier`), criteria panels, save/load format. The index.html per-receiver `calcTerrainDiffraction()` at [`index.html:4813`](../index.html:4813) still uses the single-ridge `detectDominantRidge()` method — a follow-up can align it with the Deygout algorithm once validated against SoundPLAN.

- **CoRTN road traffic source — Phase 4: receiver integration + Predicted Levels pipeline** — The CoRTN engine now contributes to the per-receiver totals in the Predicted Levels panel, energy-summed alongside the existing ISO 9613-2 point/line/area/building source contributions. **New helpers at [index.html:6468ish](../index.html:6468)** above the Phase 2 engine: `_cortnFlatDistM`, `_cortnDistanceToPolyline` (perpendicular distance from receiver to each segment via a local flat-earth ENU projection anchored at the receiver — accurate to <1 m for distances up to a few km), `_cortnAngleOfViewFromReceiver` (angle between the rays from the receiver to the first and last polyline vertices via `acos(v1·v2 / |v1||v2|)` in local metres — exact for straight roads, adequate approximation for curved polylines within CoRTN's broad `angleOfView_deg` bin), and `cortnBroadbandToSpectrum(laeq)` which converts broadband A-weighted LAeq into an 8-band spectrum using the MBS 010 road-traffic shape `[18,14,10,7,4,6,11,11]` shifted so the per-band energy sum reproduces the input LAeq exactly (verified: 75 dB in → spectrum out energy-sums to 75.0). **Per-receiver wrapper** `calcCortnRoadAtReceiver(road, prefix, receiverIdx)` resolves `getReceiverLatLng('r' + (idx+1))`, reads `getReceiverHeight(idx)` (falls back to `iso_receiverHeight`), computes `perpDist` + `angle`, shallow-clones the road with `distFromKerb_m = perpDist`, `angleOfView_deg = angle`, `receiverHeight_m = recvH`, calls `calcCortnRoadPeriod(clone, period)`, caches `{la10, laeq, perpDist, angle}` into `road.receiverResults[rk][period]`, and returns the broadband LAeq for energy summing. Day and night only — eve and lmax return null (CoRTN method doesn't define them). **`calcTotalISO9613` integration**: new `cortnRoads.forEach` loop appended after the building sources loop, gated on `prefix === 'day' || prefix === 'night'`, energy-sums each road's per-receiver LAeq via `totalLin += Math.pow(10, crLaeq / 10)`. This means criteria panels (SA/VIC/NSW) that compare predicted levels against thresholds automatically see CoRTN contributions without any changes — they read from the same `calcTotalISO9613` return value. **Render hook**: `_cortnClearAllReceiverResults()` clears stale caches at the top of `render()`, then `_cortnUpdateAllReceiverResults()` (new idempotent helper) iterates all placed receivers × all roads and calls `calcCortnRoadAtReceiver` to explicitly populate `receiverResults` before `_renderCortnPredDetail()` reads it. This guarantees the detail panel stays in sync regardless of whether the upstream `calcTotalISO9613` path was invoked (which depends on which receivers are placed and which criteria are active). Both calls are cheap: O(roads × 4 receivers × 2 periods). **Predicted Levels panel DOM**: new `<div id="cortnPredDetail">` at [index.html:3178](../index.html:3178) after the existing badge row. `_renderCortnPredDetail()` builds one block per road showing a 7-column per-receiver table (Receiver, Dist (m), Angle (°), Day LA10, Day LAeq, Night LA10, Night LAeq) with a blue-bordered "CoRTN Road Traffic — per-receiver breakdown" header. Hidden entirely when `cortnRoads.length === 0`. Shows "Place receivers on the map to see per-receiver levels." when roads exist but no receivers are placed. Shows "Enter AADT in the road panel to see contributions." for rows with `aadt == null`. **Verified in preview at 1280×800 across 11 scenarios**: single road + R1 at 22 m → perpDist 22.3 m, angle 78.7°, Day LAeq 63.8, total 64 dB; move R1 closer (11 m) → LAeq 68.0, total 68; move R1 farther (145 m then 390 m) → LAeq 48.8 then 36.6; place R2 in addition to R1 → panel shows 4 table rows (2 roads × 2 receivers), R1 total 39, R2 total 64; eve / lmax periods → `calcCortnRoadAtReceiver` returns `null` (no contribution); night period → positive contribution honouring `aadtPctNight` and `nightHours`; delete all roads via `_setCortnRoads([])` + `render()` → detail panel `display: none`, `calcTotalISO9613` returns `""` (no remaining sources); multi-road energy sum: Road A LAeq 54.9 + Road B LAeq 42.6 → total 55 dB matches `10·log₁₀(10^5.49 + 10^4.26)` = 55.15; `cortnBroadbandToSpectrum(75)` → 8-band shape energy-sums back to 75.0 exactly; no console errors throughout. **Not touched**: ISO 9613-2 propagation engine, point/line/area/building source calcs, criteria derivation logic (SA/VIC/NSW — they consume the total predicted level which now automatically includes CoRTN), receivers, noise map grid worker (CoRTN roads still do not participate in the grid — future Phase 5 work). `receiverResults` is NOT serialised to save JSON — it is always rebuilt from scratch on load via `render()` because receiver positions change independently of road config, so persistence would be stale.

- **CoRTN road traffic source — Phase 3: barrier diffraction (Charts 9/9a)** — Adds per-road barrier screening to the CoRTN engine. Separate from the ISO 9613-2 `userBarriers[]` system — CoRTN barriers are defined in the CoRTN floating panel per road, not as standalone barrier objects. **Data structure**: new `barrier = { enabled, height_m, baseRL_m, distToBarrier_m }` object on each `cortnRoads[]` entry, declared in [`_cortnDefaultRoad()`](../index.html:6111) and added to both save paths and the load path. **Helper**: [`_cortnCalculateBarrier(road, recv_h)`](../index.html:6158) derives source RL (`roadHeight_m + 0.5`), barrier RL, source-to-barrier distance (`distFromKerb_m + 3.5 − distToBarrier_m`), path difference `δ = √(rToB² + (brRL − recvRL)²) + √(sToB² + (brRL − srcRL)²) − √((rToB + sToB)² + (recvRL − srcRL)²)`, zone detection via line-of-sight height at the barrier x-location, and attenuation via the Chart 9 shadow-zone polynomial (7 terms, `x < −3 → −5 dB`, `x > 1.2 → −30 dB`) or the Chart 9a illuminated-zone polynomial (5 terms). Returns `{applied, atten, delta, x, zone, srcToBarrier, sourceRL, barrierRL, receiverRL, corr_ground_barrier, error}`. **Integration**: inside `calcCortnFreeField` just before LA10 assembly, if `road.barrier.enabled` and the helper returns `applied:true`, the free-field `corr_ground` is swapped for `corr_ground_barrier` (0 in shadow zone per the spreadsheet's hard-ground-equivalent rule) and `corr_barrier` is added to both LA10 and LAeq. Breakdown object now carries `corr_ground_final`, `corr_barrier`, and the full `barrier` result so the panel's "Show breakdown" view can display the path difference, zone, and per-contribution barrier state. **UI**: new "Barrier" section in the CoRTN panel HTML after Propagation, containing an `Enable barrier` checkbox that toggles visibility of three inputs (`Barrier height (m)`, `Barrier base RL (m)`, `Receiver→barrier dist (m)`) and a blue-boxed derived-values readout showing `Source→barrier`, `Path difference (δ)`, `Zone`, `Attenuation (dB)`, and an error row for the "barrier behind source" condition. All four inputs fire `recalcAndRefresh()` on every keystroke, and `refreshResults()` pulls the first contribution's barrier info from `results.day._breakdown` (falling back to a direct `_cortnCalculateBarrier` call when AADT is still null so users see the derived geometry even before entering traffic data). **Validation**: verified against the SoundSurfer spreadsheet barrier scenario (source RL 0.5 m, barrier 1.5 m tall at 3 m from receiver, receiver RL 1.5 m, src-to-barrier 4.5 m). Computed values: `δ = 0.0434 m` (exact), `x = −1.362` (spec −1.363, Δ 0.001), `zone = Shadow`, `atten = −8.0 dB` (exact). Resulting SoundSurfer day levels: **Day LA10 = 67.7 (expected 67.8), Day LAeq = 64.7 (expected 64.8), Night LA10 = 61.3 (exact), Night LAeq = 58.3 (exact)** — all within 0.1 dB of the spreadsheet (day values carry the 0.1 dB rounding drift from the Phase 2 free-field calculation). **Edge cases verified**: barrier disabled → reverts to free-field 75.7 dB; 0-height barrier → Illuminated zone with atten −0.3 dB; 20 m tall barrier → Shadow zone with atten clamped at −30 dB giving LA10 = 45.7; receiver-to-barrier = 10 m (beyond the 7.5 m source line) → `applied: false` with error "Receiver-to-barrier distance is ≥ source line (barrier behind source)" and LA10 reverts cleanly to the free-field value; save → clear → load round-trip preserves the barrier object identically and auto-recomputes (58.7 dB on a 2.5 m barrier + 1 m base RL test case). **Not touched**: ISO 9613-2 barrier diffraction (completely separate system on `userBarriers[]`), line sources, point/area/building sources, criteria, receivers, noise map worker. No console errors.

- **CoRTN road traffic source — Phase 2: free-field calculation engine** — Implements LA10 and LAeq from UK DoT "Calculation of Road Traffic Noise" (1988) with Australian adjustments. Every correction specified by the method is now live. **Core function** `calcCortnFreeField(road, period, overrides)` at [index.html:6170](../index.html:6170) computes one "source contribution" through the full correction chain: (A) hourly flow = `aadt × aadtPctForPeriod × flowFrac / hoursInPeriod`, (B) basic noise `L_basic = 42.2 + 10·log₁₀(hourlyFlow)` (Chart 2), (C) speed correction with gradient adjust `V_adj = speed − round((0.73 + (2.3 − 1.15·cvPct/100)·cvPct/100)·gradient_pct, 1)` then `corr_speed = 33·log₁₀(V_adj + 40 + 500/V_adj) + 10·log₁₀(1 + 5·cvPct/V_adj) − 68.8` (Charts 4 + 5), (D) gradient `0.3·gradient_pct` (Chart 6), (E) distance `−10·log₁₀(d_slant / 13.5)` where `d_slant = √((distFromKerb+3.5)² + (recv_h − sourceH − roadHeight)²)` (Chart 7), (F) ground absorption with G factor from thresholds and `5.2·G·log₁₀(...)` (Chart 8), (G) angle of view `10·log₁₀(angle/180)` (Chart 10), (H) reflection `1.5·refl_angle/angle`, (I) surface correction, (J) low volume correction `−16.6·log₁₀(D)·(log₁₀(C))²` with banded thresholds (Chart 12, LA10 only), and (K) Australian day/night adjustment. LA10 = sum of all 11 corrections; LAeq = same sum minus low-volume and minus 3 dB. `overrides` parameter allows the outer wrapper to replace `flowFrac`, `distOffset`, `sourceHeight_m`, `applySurface`, `additionalCorrection`, and `cvPctSpeedCorr` for dual-carriageway and 3-source-height sub-sources. Returns `{la10, laeq, breakdown}` where `breakdown` carries every intermediate value for debugging. **Wrapper** `calcCortnRoadPeriod(road, period)` at [index.html:6290](../index.html:6290) handles: **dual carriageway** — two lanes (near at `distFromKerb_m`, far at `distFromKerb_m + laneOffset_m` where default `laneOffset_m = 7` — new field on the road data structure), each with its own `flowFrac` (`trafficSplit` and `1 − trafficSplit`), then energy-sums via `10·log₁₀(Σ10^(Li/10))`; **one-way** — single contribution with full flow; **NSW 3-source-height** — when enabled, replaces the single per-lane source with four sub-sources per lane (Cars at 0.5 m with `surface` ON, no add-corr; CV tyres at 0.5 m with `surface` ON, −3 dB; CV engines at 1.5 m with `surface` OFF, −3.6 dB; CV exhausts at 3.6 m with `surface` OFF, −11.6 dB). The cars sub-source uses `cvPctSpeedCorr = 0` and flow `1 − cvPct/100`; the three CV sub-sources use `cvPctSpeedCorr = 100` and flow `cvPct/100`. All sub-sources energy-sum after the individual CoRTN math. **`recalcCortnRoad(road)`** at [index.html:6370](../index.html:6370) calls the day and night wrapper, writes `road.results.day/night.la10/laeq` and caches `_breakdown` arrays for the panel's expandable view. **Panel wiring** — every input listener in `attachCortnPanelListeners` now calls a local `recalcAndRefresh()` after each change (wrapped into `bindInput` + explicit calls on split/carriageway/period/AADT day-night/surface/3-source-height). The Results section HTML was rewritten to show real day/night LA10/LAeq values (1 decimal place via local `fmt()`) plus an error row ("Enter AADT to compute results.") when `aadt == null`. A new "Show breakdown" toggle button reveals a monospace pre-formatted dump of every intermediate value (L_basic, V_adj, corr_speed, d_slant, corr_distance, G_factor, corr_ground, corr_angle, corr_reflection, corr_surface, corr_lowVol, corr_aust, corr_additional) for each contribution — essential for verifying against the SoundSurfer spreadsheet. Draw-completion + load-restore paths both call `recalcCortnRoad` so cached results are immediately current. **Validation** — `_cortnRunValidation()` hardcodes the spreadsheet's reference scenario (AADT 23600, 60 km/h, 5% CV, 11 day hours / 9 night hours, DGA surface, one-way carriageway, no barrier) and runs it on demand via `?cortn_validate=1` in the URL or `window._cortnValidate = true`. Tolerance 0.3 dB. Reported result at 1280×800: **Day LA10 = 75.73 (expected 75.8, Δ 0.07), Day LAeq = 72.73 (expected 72.8, Δ 0.07), Night LA10 = 69.26 (expected 69.3, Δ 0.04), Night LAeq = 66.26 (expected 66.3, Δ 0.04)** — all four values match the spreadsheet within 0.1 dB (pure rounding drift). **Verified in preview**: validation PASS; switching to dual carriageway with 50/50 split drops day LA10 to 74.5 dB (half traffic in the far lane at +7 m); enabling 3-source-height produces 4 sub-source contributions in the breakdown (Cars 74.0, CV tyres 66.9, CV engines 66.3, CV exhausts 58.2) summing to 75.4 dB; setting AADT = 0 returns `la10: null, laeq: null` gracefully with no NaN; changing speed from 60 → 100 km/h raises day LA10 from 75.7 → 79.4 dB live via the input listener; "Show breakdown" toggle reveals the full correction chain; save → clear → load round-trip preserves every field and auto-recalculates identical results (71.2 / 69.5 / 62.8 dB on the dual + 3-source test case). No console errors. Line sources, point sources, area sources, building sources, ISO 9613-2 propagation, SA/VIC/NSW criteria, receiver functionality, noise map grid, and all exports are untouched.

- **CoRTN road traffic source — Phase 1: data structure, UI panel, map drawing, save/load** — New dedicated source type for UK CoRTN (Calculation of Road Traffic Noise) with Australian adjustments. **Completely separate** from `lineSources[]` — different inputs (AADT / speed / %CV / gradient / surface type) and a different calculation method. Phase 1 ships the scaffolding only — the CoRTN engine comes in Phase 2. **State**: new `cortnRoads = []` array + `_cortnIdCtr` + `_cortnPanel` / `_cortnPanelId` refs declared alongside `lineSources` at [index.html:6011](../index.html:6011), plus helper `_cortnDefaultRoad(verts)` that builds a fresh road with all spec defaults (speed 60 km/h, 5% CV, `la10_18h` period config with `aadtPctDay=0.94 / dayHours=18`, `austAdjDay=-1.7`, `austAdjNight=+0.5`, `distFromKerb_m=4`, `angleOfView_deg=180`, etc.) and `_cortnApplyPeriodDefaults(road, preset)` that flips `aadtPctDay/Night + dayHours/nightHours` when the user switches metric between `la10_18h` / `laeq_15h_9h` / `laeq_16h_8h`. **Map layer**: `cortnRoadLayer = L.layerGroup()` + `CORTN_STYLE = { color: '#1565C0', weight: 4, opacity: 0.85, dashArray: '8,6' }` added right next to `lineSourceLayer` at [27091](../index.html:27091). Dashed dark blue (vs. solid red line sources) so the two road-like sources are visually unambiguous. **Tools menu button**: new `<button id="drawCortnRoadBtn">Road (CoRTN)</button>` inserted after the line source button at [index.html:2116](../index.html:2116), with an SVG road-stripe icon and a `border-left: 3px solid #1565C0` left rail matching the other source-type buttons. **Draw handler**: `drawCortnRoadBtn` click → cancels other draw modes, enables `L.Draw.Polyline` with `CORTN_STYLE`. A dedicated `map.on(L.Draw.Event.CREATED)` listener checks `_cortnDrawHandler` and bails out otherwise (so the line source CREATED listener still works independently). On completion: `_cortnDefaultRoad(verts)` → push → `renderCortnRoadLayer()` → `openCortnPanel(newCr.id)`. **renderCortnRoadLayer()**: clears layer, iterates `cortnRoads.forEach((cr, idx) => ...)`, draws a dashed blue polyline with tooltip (`R{idx+1} — {name} | L = {len} m | AADT: {aadt} | {speed} km/h`), binds left-click to open the panel, binds right-click to a context menu with Edit / Duplicate / Delete, and adds a short `R1/R2/...` label at the polyline midpoint (same pattern as line source `L1/L2` from the earlier refactor). **Floating panel `cortnFloatPanel`**: ~380px wide with a sticky blue-left-border header (`#1565C0`), all sections per spec — Traffic (AADT / speed / gradient / carriageway dual-vs-one-way / traffic split), Commercial vehicles (%CV day/night), Time periods (metric dropdown → auto-sets %AADT day/night + hours, with manual override inputs), Corrections (surface type dropdown with 7 presets including Custom, Australian adj. day/night), Propagation (distance from kerb, road height, ground absorption, mean prop height, angle of view, reflection angle), NSW 3-source-height checkbox, Results placeholder showing "—" (Phase 2 populates), and Delete + Close action buttons. **Panel positioning**: viewport-centred (not click-relative) per the Phase 1 spec — this is the ONLY panel that centres. The existing src / area / line / building panels still use their original click-relative positioning (per the earlier revert). **Draggable header**: `cortnPanelHeader` + mousedown/mousemove/mouseup pattern copied from the line source panel. **Input wiring** (`attachCortnPanelListeners`): `bindInput(id, prop, cast)` helper wires every text/number input back to the `cr.{prop}` field on every `input` event. Period config dropdown additionally calls `_cortnApplyPeriodDefaults` and refreshes the day/night/hour UI fields. Surface type dropdown auto-maps the standard presets to `surfaceCorrection` (DGA→0, Concrete→+3, OGA→-2, SMA→-1, 14mm Chip→+4, 7mm Chip→+2) and shows/hides the custom dB input row. Carriageway radios toggle `disabled` on the traffic-split inputs. **Save/Load**: `cortnRoads` serialised in BOTH save code paths at [index.html:16967](../index.html:16967) and [18509](../index.html:18509) with every field from the data structure; load restores via `window._setCortnRoads(data.cortnRoads || [])` at [18218](../index.html:18218). `_setCortnRoads` clears the layer, migrates each saved row through `_cortnDefaultRoad` (so any missing field gets a sensible default), and rebuilds `_cortnIdCtr` from the max saved id so new roads don't collide. **Window exports**: `window.openCortnPanel`, `window._getCortnRoads`, `window._setCortnRoads`. **Not touched**: `lineSources[]`, point/area/building sources, ISO 9613-2 propagation, criteria derivation (SA/VIC/NSW), receivers, the existing save fields. **Verified in preview**: button appears in Tools menu with text "Road (CoRTN)"; `_setCortnRoads([{id:'cortn_seed', vertices:[[-34.92,138.60],[-34.92,138.61]]}])` fills in all defaults (speed 60, aadt null, carriageway dual, periodConfig la10_18h, aadtPctDay 0.94, dayHours 18); opening the panel centres it on the viewport with all sections present; editing the Name input updates `cr.name` on each keystroke; editing AADT writes the number back; switching period metric to `laeq_15h_9h` auto-flips `aadtPctDay=0.9, dayHours=15` and the UI fields update; switching surface to OGA writes `surfaceCorrection=-2`; save-round-trip serialises → `_setCortnRoads(serialised)` restores every field identically → re-opening the panel shows Fullarton Road / 25000 / OGA with no data loss; delete button (stubbed confirm) removes the road and closes the panel. No console errors. Existing Supabase library badge still reads `Library: Supabase (live) — 120P / 10L / 5A / 13C`.

- **Point source markers: unified red + smaller dot + Objects panel chip recoloured** — Point sources on the map used to cycle through a 6-colour palette (`['#e74c3c','#3498db','#2ecc71','#f39c12','#9b59b6','#1abc9c']`), so every new source got a different colour (red, blue, green, orange, purple, teal). Collapsed the `SOURCE_COLOURS` array at [index.html:7260](../index.html:7260) to a single-element `['#d32f2f']` so the two modulo-indexed consumers ([9959](../index.html:9959) and [21598](../index.html:21598)) always resolve to the same dark red. Updated the fallback constant in the inline default at [18955](../index.html:18955) to match. Dot size tightened in `makeSrcDotIcon()` at [21042](../index.html:21042): 12×12 → 10×10 (`html` inner div, `iconSize`, `iconAnchor` 6→5, `popupAnchor` -10→-8, `box-shadow` slightly reduced 0 1px 3px → 0 1px 2px). Objects panel source chip at [29787](../index.html:29787) had a hard-coded `#2563eb` blue that ignored the palette; changed to `#d32f2f` so it matches the new map dots. Dark-red `#d32f2f` was chosen because it already matches the line-source map label colour — point, line, and Objects list source chips are now all the same red. Area source (amber/orange) and building source styles are untouched. Verified: Objects panel row renders with `background:#d32f2f` in the live DOM; palette edit file-level confirmed via Read; no console errors.

- **Line source map labels: short ID (L1, L2, …) instead of full name** — The map label floated over each line source now shows its short identifier (`L` + position in the `lineSources` array) instead of the stored `ls.name`, matching the `S1`/`S2` pattern used for point sources. Edited in `renderLineSourceLayer()` at [index.html:27523](../index.html:27523): added an `idx` parameter to the outer `lineSources.forEach(function(ls, idx) { ... })` and changed the label HTML at [index.html:27630](../index.html:27630) from `escapeHTML(ls.name || 'Line source')` to `escapeHTML('L' + (idx + 1))`. Only the map label changed — tooltips (`poly.bindTooltip` at [27532](../index.html:27532)), panel titles (`lsPanelTitle` at [27126](../index.html:27126)), card headers ([27668](../index.html:27668)), results table cells ([15922](../index.html:15922)), report equipment names ([28302](../index.html:28302)), and save/load serialisation still use the full `ls.name`. `renderLineSourceLayer()` is the single render path — it clears `lineSourceLayer` and re-adds all layers on every call — so save/load, edit, visibility toggle, and duplicate/delete actions all produce fresh `L1/L2/...` labels automatically. Point source (`S1`, `S2`) labels are unchanged. No calculation, criteria, save/load format, or panel behaviour touched.

- **Side panel padding fix + atom buttons relocated into a top toolbar row** — Two polish items on the new fixed side panel: (1) **padding overflow fix** — `#side-panel-inner` was carrying `min-width: var(--side-panel-width)` (a leftover from the deleted width-transition use-case) plus default `box-sizing: content-box`, which forced the inner to be 320px wide (300 min + 20 padding) and pushed its children 2px past the panel's 300px width. Fixed by setting `box-sizing: border-box` on `#side-panel`, `#side-panel-inner`, every direct child of `#side-panel-inner`, and the search wrapper; removing the stale `min-width`; bumping padding to `12px 14px` (from `12px 10px`). Search wrapper is now a clean 271px wide at `(14, 153)` with real breathing room on the right. Mapping/Tools/Modelling headers got `padding: 10px 12px` (up from `9px 10px`) so the `▼` chevron is 12px clear of the edge. Accordion body `padding: 8px 6px 12px 6px` with `> * + * { margin-top: 4px }` gives each button inside a section 4px of vertical breathing room. (2) **Atom buttons moved into a top-of-panel toolbar** — the five quick-action buttons (`#mp-help`, `#mp-suggest`, `#mp-undo`, `#mp-redo`, `#mp-save-jpg`) previously lived in `#mapPanelContainer` floating over the top-right of the map. They're now the first child of `#side-panel-inner` inside a new `#side-panel-toolbar` flex row, rendered as 36×36 (38×38 including 1px border) icon-only pills with `background: rgba(255, 255, 255, 0.06)`, `border: 1px solid rgba(255, 255, 255, 0.14)`, `border-radius: 6px`, and a hover highlight. The underlying DOM elements keep their `.mp / .mp-hdr` structure and original IDs so the existing click handlers (`helpToggleBtn`, `suggestToggleBtn`, `undoBtn`, `redoBtn`, `saveJpgPanelBtn`) continue to fire unchanged — `_resonateSidePanelBoot()` at [index.html:1870](../index.html:1870) now creates the toolbar div and `appendChild`s each button into it before the search wrapper. The Save JPG button's label `<span>Save JPG</span>` is hidden inside the toolbar (`#side-panel-toolbar #mp-save-jpg .mp-hdr > span { display: none }`) so it reads as icon-only like the others; the export still produces the same 3× JPEG via the unchanged click handler. The suggest-sources badge dot is preserved. `#mapPanelContainer` is now `display: none !important` (it's empty anyway — all 5 atom buttons moved out) which removes the last piece of floating UI from the map viewport. The map now shows only `#noise-map`, `#map-guide-overlay`, and the marker status rows (`#map-status-source`, `#map-status-r1..r4`) plus Leaflet's own zoom/scale controls. **Not touched**: ISO 9613-2 propagation, barrier/terrain/ground-effect, source/receiver placement, zone detection, criteria derivation, Save/Load serialisation, noise map grid/contouring, all exports (Save JPG still exports 3× unchanged), Quick Reference + Suggested Sources content, undo/redo history, the 14 drawer panels, collapse/localStorage/mobile behaviour from the earlier refactor, or the Supabase library plumbing. **Verified in preview at 1280×800**: side panel 300×713 (border-box) at (0, 87); inner 299×713 padded 12px 14px; toolbar 271×38 at (14, 99) with 5 icon buttons at x=14/58/102/146/190 (6px gap); search wrapper 271×32 at (14, 153) — no right-edge overflow; Mapping header 271×34 at (14, 201) padded 10px 12px; Tools accordion open body 256×633 at (14, 273) within the padded area; `#mapPanelContainer` display:none w:0 h:0; map column children = `noise-map`, `map-guide-overlay`, 5 marker status rows only (no floating toolbars); collapse/expand still flips panel 300↔0 and map 300↔0; drawer toggle (`mp-toggle-drawer`) still pinned to `#side-panel-footer` at (14, bottom); Supabase badge unchanged; no console errors.

- **Map toolbar: floating LHS panel → fixed collapsible side panel beside the map** — Replaced the floating LHS tool panel (Mapping/Tools/Modelling + Search, which the two previous refactors had progressively evolved from horizontal-top-row → vertical-left-column → unified-dark-card-overlay) with a fixed collapsible **side panel** that sits *beside* the map rather than overlaying it. The map reflows to the remaining viewport width instead of being partially covered. Pattern matches ArcGIS / Google Earth / Claude Desktop sidebar layouts. **DOM structure** — a new `#side-panel` element is injected at load time by `_resonateSidePanelBoot()` (an IIFE in the existing inline script block at [index.html:1721](../index.html:1721)) as the first child of `#app-layout`, alongside a sibling `#side-panel-backdrop` for the mobile overlay. The boot script then MOVES `#mapSearchWrapper`, `#mp-mapping`, `#mp-tools`, `#mp-modelling`, and `#mp-toggle-drawer` out of the old `#mapPanelContainer` and appends them to a new `#side-panel-inner` flex-column. The drawer-toggle button (`#mp-toggle-drawer` = "Expand / Panels") lives in a bottom-pinned `#side-panel-footer` via `margin-top: auto`. **Layout** — `#app-layout` stays `position: relative; display: block`; `#side-panel` is `position: absolute; top:0 left:0 bottom:0; width: 300px; z-index: 110`; the existing `#map-column` was changed from `inset: 0` to explicit `top:0 right:0 bottom:0; left: 300px`. When the panel collapses, `#app-layout` gets a `.side-panel-collapsed` class which triggers `#app-layout.side-panel-collapsed #map-column { left: 0 }` and `#side-panel.collapsed { width: 0 }` — the map reflows to fill the full viewport width. Transitions on width/left were initially added (0.25s ease) but removed because the preview tool reads styles mid-transition and returns stale values, making automated verification impossible; the functional collapse is now instant. **Accordion mode** — the existing `.mp` markup (with `.mp-hdr` click handler + `.mp-body` content) is reused unchanged, but a scoped CSS override at [index.html:1212](../index.html:1212) renders `.mp-body` as `position: static` inline-expanding content instead of `position: absolute` floating dropdown. `toggleMapPanel()` at [index.html:1820](../index.html:1820) was updated to detect `target.closest('#side-panel')` and only toggle the target (no mutual-exclusion, no max-height measurement) — multiple sections can stay open simultaneously, Claude-sidebar style. The outside-click handler at [index.html:1857](../index.html:1857) was updated to leave accordions inside the side panel open when clicks happen on the map. **Atom buttons** — Save JPG, Help (?), Suggested sources (💡), Undo, Redo stay in `#mapPanelContainer` (which kept its element ID, inline `style` attribute stripped) and are now positioned at the top-right of the *visible* map area via `position: absolute; top: 10px; right: 530px` — the 530px offset avoids the right-side drawer (520px + 10px gap), and a second rule re-homes them to `right: 10px` when `#drawer-panel` carries `.drawer-closed`. Each atom button recovers its original dark-pill `.mp` base chrome (background, border-radius, shadow, backdrop-filter) because the previous task's scoped `#map-column #mapPanelContainer > .mp { background: transparent }` override was deleted. **Collapse toggle + persistence** — a new `#side-panel-toggle` `<button>` sits on the panel's right edge (absolute positioned `right: -14px`), shows `«` when expanded and `»` when collapsed. Click toggles `#side-panel.collapsed` and `#app-layout.side-panel-collapsed` together via `setCollapsed(bool)`, persists the state to `localStorage['sidePanelCollapsed']`, and calls `window._map.invalidateSize()` so Leaflet reflows to the new viewport area. The initial state honours localStorage on desktop, or auto-collapses on mobile (`matchMedia('(max-width: 767px)').matches`) regardless of stored state. A `matchMedia('change')` listener also re-collapses when the viewport crosses into mobile. **Mobile overlay @ <768px** — `@media (max-width: 767px)` gives `#side-panel` `position: absolute; z-index: 1000` (above the map instead of pushing it), uses `transform: translateX(-100%)` with `width: var(--side-panel-width)` for the collapsed state (slide out, not shrink), shows the semi-transparent `#side-panel-backdrop` overlay when the panel is open (tap anywhere to dismiss), and locks `#map-column.left: 0` and `#mapPanelContainer.right: 10px` so the map never reflows on mobile — the panel is always a pure overlay. **Files modified** — [index.html](../index.html) only. CSS block at [index.html:1101](../index.html:1101) rewrote `#app-layout`/`#map-column`/`#side-panel`/`#side-panel-toggle`/`#side-panel-inner`/`#side-panel-footer`/`#side-panel .mp`/`#side-panel .mp-body`/`#side-panel #mapSearchWrapper`/`#side-panel-backdrop` plus the mobile media query and the rewritten `#map-column #mapPanelContainer` atom-button rule. Inline `style` attribute on `<div id="mapPanelContainer">` at [index.html:1763](../index.html:1763) was stripped to empty (all positioning now comes from the stylesheet). Inline script at [index.html:1721](../index.html:1721) gained `_resonateSidePanelBoot()` and the updated `toggleMapPanel` + outside-click handlers. **Not touched** — ISO 9613-2 propagation, barrier/terrain/ground-effect logic, source/receiver placement, SA/VIC/NSW zone detection + criteria, Save/Load JSON format, noise map grid + contouring, PDF/JPG/GeoJSON/KML/CSV exports, and the 14 panels in the right-side `#drawer-panel`. Leaflet `window._map` is untouched; only `invalidateSize()` is called on panel toggle. **Verified in preview at desktop 1280×800**: side panel 301×713 at (0, 87); map column at (300, 87) 980×713; drawer overlays at (759, 87); accordion opens Tools + Mapping independently (tools 23 btns, mapping 5 btns, modelling 8 btns); outside click leaves accordions open; collapse shrinks panel to 0 and map reflows to full 1280 width; expand restores; localStorage round-trips `true`/`false`; all 5 atom buttons render at x=511–750 just left of the drawer; Objects panel DOM still present; Supabase library badge still reads `Library: Supabase (live) — 120P / 10L / 5A / 13C`; no console errors. **Verified at mobile 375×800**: panel auto-collapses on load (translated off-screen via `transform: translateX(-100%)`); open via toggle shows panel at (0, 102) 301×698 + backdrop (display: block); map stays at (0, 102) 375×698 NOT pushed (overlay mode); backdrop tap collapses the panel back. Save/load round-trip not explicitly tested but no serialisation code paths were modified. ISO/TR 17534-3 validation untouched.

- **Map toolbar: unified Claude-Desktop-style side panel** — Follow-up visual refactor of the LHS tool panel. Previously rendered as 10 independent floating dark pills stacked in a column; now a single unified rounded card (one `background: rgba(26,32,44,0.92)`, one `border-radius: 10px`, one `box-shadow: 0 6px 24px rgba(0,0,0,0.45)`, one 6px padding, 1px subtle white border). The chrome moved **up** from each `.mp` tile onto `#mapPanelContainer` itself — tiles inside the panel are now transparent flat rows with a `rgba(255,255,255,0.09)` hover highlight and a stronger `rgba(255,255,255,0.12)` open-state highlight, matching the Claude Desktop sidebar list-item pattern. Row gap tightened from 5px → 2px since tiles no longer need breathing room for their own borders. `.mp-hdr` padding inside the panel tightened from `7px 10px` → `6px 8px`. The search wrapper dropped its solid dark pill (`background: rgba(26,32,44,0.9); box-shadow: 0 2px 12px; backdrop-filter: blur(4px)`) in favour of an input affordance that blends into the panel: `background: rgba(255,255,255,0.06)` with a `1px solid rgba(255,255,255,0.08)` border, 5px radius, 26px height, no shadow. The old `.mp.mp-open .mp-hdr` four-corner-rounding rule was removed (superseded by the new open-state highlight rule in the same selector). The inline `style` attribute on `<div id="mapPanelContainer">` at [index.html:1729](../index.html:1729) was updated in lockstep with the stylesheet (mandatory — the inline attribute silently overrides CSS if left stale). Right-flyout dropdown behaviour from the earlier vertical-column refactor is unchanged: dropdowns still fly out at `top:0; left:calc(100% + 6px)` with their own dark card styling, so they read as self-contained submenus popping out of the unified sidebar. Verified in preview: container computed style matches the 6 expected values; all 9 `.mp` children have `background-color: rgba(0,0,0,0)` and `box-shadow: none`; search wrapper has `rgba(255,255,255,0.06)` fill; hover rule resolves to `rgba(255,255,255,0.09)`; Tools dropdown open state has header bg `rgba(255,255,255,0.12)` and flyout at `(267, 205)` 228×518 with `rgba(26,32,44,0.95)` card; Leaflet zoom at (10, 112) and marker status row at (10, 644) unchanged; Supabase badge still reads `Library: Supabase (live) — 120P / 10L / 5A / 13C`; no console errors.

- **Map toolbar: horizontal strip → locked 200px vertical LHS panel** — The `#mapPanelContainer` toolbar (`?`, Search, Mapping ▼, Tools ▼, Suggested, Undo, Redo, Modelling ▼, Save JPG, Expand) was moved from a horizontal row pinned to the top of the map (`top:10px; left:54px; flex-direction:row`) into a locked-width vertical column pinned to the left edge of the map (`top:10px; left:54px; width:200px; flex-direction:column; align-items:stretch`). All 10 buttons now stack vertically as full-width `.mp` tiles. Both the [index.html:1135](../index.html:1135) stylesheet rule AND the inline `style` attribute on the element at [index.html:1729](../index.html:1729) were updated — the inline style had higher specificity and was silently overriding the stylesheet during an initial pass. Dropdown bodies (`#mp-tools`, `#mp-mapping`, `#mp-modelling`) no longer drop DOWN below their button; they fly out to the RIGHT of the 200px column via `top:0; left:calc(100% + 6px)` with `min-width:220px` so the full-height panel stays visible and one tool's dropdown never covers another tool. The `max-height` on long dropdowns was relaxed from `calc(100vh - 120px)` to `calc(100vh - 40px)` since the vertical space the old horizontal-toolbar used up no longer needs to be reserved. When a `.mp` is open the header keeps all four corners rounded (previously only top-rounded to meet the below-dropdown). The search bar (`#mapSearchWrapper`) dropped its `flex: 1 1 220px / max-width:280px` constraints and now uses `width: 100%` to fill the column. Autocomplete results still anchor `position: absolute; top: 100%; left: 0; right: 0` on the search wrapper so they continue to drop down below the search input — they do NOT need to flyout right. The marker status row at `bottom:24px; left:10px` and the Leaflet zoom controls at `left:10px` are unaffected (10px gap to the panel which starts at `left:54px`). Verified: panel measures 200px × 325px at (54, 112); all 10 children exactly 200px wide; Mapping/Tools/Modelling dropdowns all fly out at x=260 (panel right edge + 6px gap); no console errors; Supabase library badge still reads `Library: Supabase (live) — 120P / 10L / 5A / 13C` unchanged. No calculation, propagation, criteria, or Save/Load logic was touched.

- **Supabase-backed noise source + Rw library** — The four in-app libraries that were previously hard-coded as JavaScript literals in [index.html](../index.html) (`SOURCE_LIBRARY_GROUPED`, `LINE_SOURCE_LIBRARY`, `AREA_SOURCE_LIBRARY`, `CONSTRUCTION_LIBRARY`) now load from an existing Supabase project at page load and hot-swap the in-memory arrays before the dropdown panels render. The hard-coded literals remain in place as an **offline fallback**: if no `supabase-config.js` is present, if the network is unreachable, if the 4-second fetch times out, or if the DB returns zero rows post-migration, the app silently keeps the embedded snapshot and displays a grey/red "Library: offline" badge next to the page title. On success the badge flips to green "Library: Supabase (live)". Point sources, line sources (Lw/m) and area sources (Lw/m²) share one physical table (`reference_noise_sources`, already existed in the shared acoustic DB with 81 rows) — a new `source_kind` discriminator column plus `height_m` and `display_group` columns were added by the migration in [supabase/migration.sql](../supabase/migration.sql), the existing rows were backfilled with `source_kind='point'` and best-effort display-group labels, and 10 line + 5 area + 37 net-new point rows (34 mechanical units, 2 forklift activity, 1 manual car wash) were seeded from the hard-coded arrays. Construction Rw records live in a new `reference_constructions (kind, name, rw, octave_r jsonb)` table seeded with the 13 wall/roof/opening entries from `CONSTRUCTION_LIBRARY`. The cleanup pass that normalised source names (`normalizeSourceLibraryNames`, formerly an anonymous IIFE) is now a named function reused on both the initial snapshot and any Supabase-loaded data. A unified `window.rebuildAllLibraries()` regenerates every derivative map (`SOURCE_LIBRARY_ALL` / `SOURCE_LIBRARY_GROUPED_NON_LMAX` / `*_GROUPED`) and refreshes visible dropdowns after a hot-swap so running-session UIs update in place. Admin editing is gated behind a Supabase magic-link login: clicking the library badge opens a modal login, the one-time email link brings the user back signed in, and a 4-tab CRUD modal (Point / Line / Area / Construction) exposes create/edit/delete against the same tables. RLS policies anchor the write path to an `app_admins(email)` allowlist — the publishable key in [supabase-config.js](../supabase-config.js) (gitignored; see [supabase-config.example.js](../supabase-config.example.js)) only grants SELECT. New files: [library-loader.js](../library-loader.js), [supabase-admin.js](../supabase-admin.js), [supabase-config.example.js](../supabase-config.example.js), [supabase/migration.sql](../supabase/migration.sql), [.gitignore](../.gitignore). In [index.html](../index.html) the only structural changes are: three `<script>` tags added before `</head>`, a clickable `#resonateLibBadge` span added to the `.h1` header, `const SOURCE_LIBRARY_GROUPED` → `var` so window assignment can replace it, the three inline LINE/AREA/CONSTRUCTION grouped-map IIFEs extracted into named rebuilders (`rebuildLineSourceGrouped` / `rebuildAreaSourceGrouped` / `rebuildConstructionGrouped`), and a small boot block that kicks off `window.ResonateLib.load()` after `DOMContentLoaded`. Save/load integrity is preserved because every library entry is still keyed by `name` across the migration — existing saved assessments round-trip unchanged. ISO/TR 17534-3 propagation, state criteria, barrier diffraction, and terrain screening are untouched.

- **Empty states, initial collapse, and map guide overlay** — Five first-run polish items plus one pre-existing bug fix:
  1. **All panels start collapsed on fresh page load.** New `setInitialCollapseState()` runs once after `initCollapse()` completes. Handles both drawer collapse mechanisms: `.card-body.collapsed` for `initCollapse`-managed cards, plus `style.display = 'none'` on the inline-toggle inner bodies (`#critBody`, `#pdcBody`, `#mbs010Body`, `#objectsPanelBody`) that have their own onclick handlers. The drawer opens as a compact list of section headers — users expand what they need. After initial collapse, the user has full manual control for the rest of the session (nothing auto-collapses or auto-expands).
  2. **Expand all on Load Assessment.** New `expandAllPanels()` reverses the collapse on both mechanisms. Called inside `loadAssessment()`'s success path (after the 800 ms render settle) so restored data is immediately visible.
  3. **Empty-state guidance messages inside key panels.** `.panel-empty-state` divs inserted in the drawer IIFE before the Receivers & criteria body, the Day situation source table (`#daySrcTable`), and the Predicted noise levels table (`#predTableDay`). They're hidden by default; the new `updateEmptyStates()` (called at the end of `render()`) adds `.show` when the relevant data is missing, and moves the underlying table off-screen via `position: absolute; left: -9999px` so the table stays in the DOM for Save/Load/Report but doesn't visually compete with the guidance message.
  4. **Map guide overlay.** `#map-guide-overlay` is inserted inside `#mapInnerWrapper` with `pointer-events: none` so map clicks pass through. Shows "Search for an address or click the map to place a noise source" when nothing is placed. `updateEmptyStates()` adds `.guide-hidden` as soon as any source or receiver exists, with a 0.4s opacity fade.
  5. **Contextual compliance strip messages.** Replaced the single `"Place source and receivers to see compliance"` fallback with four contextual states: `"Click the map to place a noise source and receivers"` (nothing placed), `"Source placed — now add receivers at sensitive locations"` (source only), `"Receivers placed — now add a noise source"` (receivers only), `"Configure noise sources to see predictions"` (both placed but no Lw data). Normal pred/crit/margin display takes over once all three ingredients exist.

  Also fixed a pre-existing crash: the drawer `]` keyboard shortcut handler used `e.target.matches(...)` unguarded — when a keydown event fired with `e.target === document` (e.g. after certain focus transitions), `document.matches` is undefined and the handler threw `TypeError: e.target.matches is not a function`. Now guarded with `e.target && e.target.matches && e.target.matches(...)`.

  No calculation, criteria, or Save/Load logic was touched; ISO/TR 17534-3 validation still passes.

- **Header cleanup: logo, title sizing, GIS Export dropdown** — Follow-up polish on the header redesign: (1) Removed the orange text "Resonate" brand name that sat next to the logo image — the logo itself is the wordmark, the text was a duplicate. Only the `<img src="resonate-logo.png">` remains. (2) "Environmental Noise Screening Tool" is now the primary title at 19px font-weight 600 (dark grey `#1f2937`), visually matching the 36px logo height. "v2.0 — March 2026" sits below as an 11px muted version line. (3) Removed the Methodology button from the header action row — methodology is still accessible via the drawer jump nav's "Methodology" t